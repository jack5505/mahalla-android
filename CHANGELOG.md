# История разработки — mahalla-android

Журнал этапов: что сделано, какие грабли встретились, что осталось.
Перенесён из `AGENTS.md` 2026-09-06, потому что файл инструкций грузится
в контекст каждой сессии, а история нужна редко и по конкретному поводу.

**Порядок обратный не наводился** — разделы идут в том же порядке, в каком
их дописывали. Новый раздел — в конец файла.

**Целиком не читать — искать по номеру issue:**
`grep -n '#107' CHANGELOG.md docs/changelog/*.md`.
Разделы до 2026-09-06 (issue #5 … #98, эпики 1–7) — в
[docs/changelog/2026-08-25--2026-09-06.md](docs/changelog/2026-08-25--2026-09-06.md).
Файл перевалил за ~1500 строк — старые разделы уезжают в следующий архив
`docs/changelog/`, текст не правится.

Актуальные правила и окружение — в [AGENTS.md](AGENTS.md).
Решения и их обоснование — в [docs/adr/](docs/adr/).

---

## Этап: карточка места отвечает — лайк, «Избранное», комментарии (issue #75, задача T9)

Контроллер `social` бэкенд отдавал целиком, приложение не использовало его
вовсе: карточка места была только чтением — посмотреть и уйти.

**Контракт снят со стенда** (`https://189-74-96-232.nip.io/v3/api-docs` +
curl'ы; дизайн-репо агенту по-прежнему недоступно). Все семь ручек требуют
Bearer — без токена приходит `401 UNAUTHORIZED`, — значит `SocialApi`
собирается на **основном** Retrofit, а не на «голом» `@RefreshClient`:

| ручка | контракт |
|---|---|
| `GET places/{id}/status` | `{liked, saved, totalLikes}` |
| `POST places/{id}/like` | `{liked, totalLikes}` — переключатель, `DELETE` нет |
| `POST places/{id}/save` | голый `Boolean` в `data` — тоже переключатель |
| `GET places/{id}/comments?page&size` | `PageResponse<CommentResponse>` |
| `POST places/{id}/comments` | тело в схеме — `Map<String,String>`, ответ `CommentResponse` |
| `DELETE comments/{id}` | `ApiResponseVoid` |
| `GET saved-places?page&size` | `PageResponseUUID` — **только идентификаторы** |

- **Домен** `feature/social/domain/PlaceSocial.kt`: `PlaceSocialStatus` (одно
  состояние на обе кнопки — сервер отдаёт их одним ответом), `PlaceComment`,
  `PlaceCommentPage`, `CommentRules`, `SavedPlaceIdsPage`. Вся арифметика
  оптимистичного нажатия — чистые функции: `toggledLike()` двигает счётчик на
  единицу, `withLike(liked, likes)` применяет ответ сервера (счётчик сервера
  сильнее — он знает о чужих лайках, случившихся между запросами), а
  нормализация отрезает невозможные состояния («−1 лайк», «мой лайк есть, а
  лайков ноль»).
- **Оптимистично с откатом**: экран переворачивает кнопку сразу, а отказ
  возвращает **то самое** значение, которое было до нажатия, — не «обратный
  переворот»: ответ, разошедшийся с фактом, оставил бы кнопку в третьем,
  выдуманном состоянии. Пока запрос в пути, повторные нажатия игнорируются
  (`likePending`/`savePending`): два переключателя в полёте кончаются тем, что
  ответы приезжают в произвольном порядке. Причина отказа показывается текстом
  сервера (issue #34) — кнопка, вернувшаяся в прежнее состояние молча,
  читается как сломанная.
- **Неизвестное состояние не рисуется как «не нравится»**: `status` — отдельный
  запрос, и пока он не ответил (или отказал), кнопок нет, а есть объяснение и
  «Повторить» (`SocialRetry`). Иначе человек нажмёт и **снимет** собственный
  лайк, думая, что ставит его.
- **Комментарии — лента без оценки**, отдельно от отзывов: отзыв ставит
  рейтинг месту, комментарий отвечает соседям. Отправленный встаёт первым, а
  черновик очищается **только** после успеха: стереть написанное, не отправив,
  значит потерять работу человека. Имени автора в контракте нет — только
  `userId`, поэтому подпись у чужих записей общая («Пользователь»), а «Удалить»
  показывается лишь у своих (`isMine` = сравнение с id профиля, issue #61):
  предлагать действие, которое сервер заведомо отклонит, нельзя. Ключ поля тела
  (`text`) взят из ответа — другого источника у контракта нет.
- **Карточка, лайк и комментарии — три независимых запроса**: ни отзывов, ни
  лайков не должно хватать, чтобы спрятать место, ради которого человек сюда
  пришёл.
- **«Избранное»** — экран `feature/social/ui/saved/`, маршрут
  `SavedPlacesRoute` вне графа табов, вход из профиля (на карточке кнопка
  только добавляет и убирает). На возврате (`ON_RESUME`) список перечитывается:
  место могли убрать из избранного на его же карточке.
- **N+1 — долг бэкенда, а не выбор клиента**: `GET saved-places` отдаёт
  `PageResponseUUID`, карточек в контракте нет вовсе, поэтому
  `SocialRepository.savedPlaces` дособирает их `CatalogRepository.placeCard`
  (новый метод: одна ручка `places/{id}`, без отзывов, с тем же фоллбэком на
  Room и удалением записи на 404). Параллельность ограничена четырьмя
  запросами (`Semaphore`) — пул OkHttp держит пять соединений на хост, и
  очередь из двадцати отняла бы их у остальных экранов. Порядок сервера
  сохраняется; место, не доехавшее ни из сети, ни из кэша, выпадает из списка,
  но если не доехало **ни одно** — это отказ, а не пустое «Избранное».
  **Вопрос бэкенду** (`jack5505/mahalla`): вернуть с этой ручки карточки.

Проверено: `./gradlew testDebugUnitTest` — **923 теста в 96 классах, 0
падений**; `lintDebug` — `No issues found`; `assembleDebug` и `assembleRelease`
— BUILD SUCCESSFUL. Новые тесты: `PlaceSocialTest` (арифметика лайка, откат,
правила черновика), `SocialRepositoryTest` (MockWebServer: пути и тела,
конверт, дата без зоны, `isMine`, отбрасывание записей без id, сборка
«Избранного» из идентификаторов, дедупликация, «ни одна карточка не доехала»),
`SavedPlacesViewModelTest`, 17 новых случаев в `PlaceDetailsViewModelTest`, три
в `CatalogRepositoryTest` (`placeCard`), новый случай в `GraphAssemblyTest` и
`RoutesSerializationTest`; фейк `FakeSocialRepository`.

**Не сделано / риски:**

- **На живом аккаунте не проверено** (эмулятора в CI нет, а вход требует
  SMS-кода): все семь ручек без токена отвечают `401`, поэтому успешный путь
  проверен только на MockWebServer. **Первое, что смотреть руками** — лайк,
  сохранение и отправка комментария на настоящем стенде.
- **Имя ключа в теле нового комментария (`text`) — предположение**: springdoc
  описал тело как `Map<String,String>` без перечня полей, и взято имя из
  `CommentResponse.text`. Если бэкенд ждёт другое имя, править одну строку в
  `AddCommentRequest`.
- **Порядок ленты комментариев неизвестен**: отправленный встаёт первым,
  исходя из того, что сервер отдаёт новые сверху. Если сортировка обратная,
  после перезагрузки комментарий переедет вниз — косметика, но проверить
  стоит.
- **Счётчик лайков большого порядка рисуется как есть** (`12345`, без
  разрядов): отдельного форматтера чисел в проекте нет, а деньги форматирует
  `MoneyFormatter`, которому здесь не место.
- **«Избранное» стоит N+1 запросов на страницу** — см. выше; на плохой сети
  это заметно. Лечится на бэкенде.
- **Имени автора у комментария нет** — все чужие подписаны «Пользователь».
  Тоже вопрос к бэкенду: `CommentResponse` отдаёт только `userId`.
- Скриншот-тестов по-прежнему нет: строка лайка, лента и «Избранное»
  проверялись глазами по `@ThemeLanguagePreviews`.

## Открытые долги на 2026-09-06 (перенесено из AGENTS.md)

Список из прежнего раздела «Что делать дальше». Сохранён здесь при разрезе
файла; **всё это стоит оформить issue**, документация — не трекер.

- **Сверить онбординг с дизайн-репозиторием** (`TZ-ANDROID.md`, `SCREENS.md`,
  прототип): тексты, порядок блоков, иллюстрации. Вёрстка эпика 3 делалась
  по описанию задачи, потому что design-репо в том прогоне был недоступен.
- **Сверить контракты вертикалей** с реальным бэкендом — см.
  `docs/API-CONTRACT.md`, там ⚠️ у всех, кроме auth, каталога и «Еды».
- **`MAPKIT_API_KEY`** в секретах Actions: без ключа на месте карты объяснение,
  а не тайлы.
- **`SENTRY_DSN`** в секретах Actions: без него падения у пользователей
  невидимы.
- **`exportSchema = false`** в Room включить обратно до первого релиза.
- Открытые замечания ревью PR #21, #22, #23 перечислены в соответствующих
  разделах выше и в issue не заведены.

## issue #137 — картинки не грузились вообще (2026-09-08)

Сообщение: «не открывается рисунок», на скриншоте экран заведения, галерея —
два пустых прямоугольника. Причина оказалась не в галерее и не в бэкенде:
`MahallaAsyncImage` не грузил **ни одной** сетевой картинки в приложении с
самого issue #60. Ни фото заведений, ни обложки, ни аватары.

**Дедлок между Coil и композицией.** Компонент рисовал `Image(painter)` только
в состоянии `Success`. А Coil 2.x в `rememberAsyncImagePainter`, если размер не
задан явно, подставляет `size { drawSize.mapNotNull { it.toSizeOrNull() }.first() }`
— то есть **ждёт первой отрисовки painter'а** (`AsyncImagePainter.onDraw()`
кладёт размер в `drawSize`). Круг замкнулся: запрос ждёт размера, размер ждёт
отрисовки, отрисовка ждёт `Success`. Состояние навсегда оставалось `Loading`,
пользователь видел скелетон. Иконки-фоллбэка при этом нет — это не `Error`,
поэтому и выглядело как «сломанный экран», а не как «фото нет».

Починка: painter всегда в отрисовке, а скелетон и фоллбэк — слоем поверх
(`Box`), в `Success` поверх ничего.

**Грабли, которые это оставило:**

- **Coil-painter вне отрисовки не работает.** Любая ветка `when
  (painter.state)`, где `Image(painter)` стоит в одном из исходов, — тот же
  дедлок. Показывать состояние надо **над** картинкой, а не **вместо** неё.
  Снять дедлок можно и явным size-резолвером в `ImageRequest`, но без размера
  места под фото это `Size.ORIGINAL`, то есть декодирование оригинала — для
  ленты фотографий не вариант; размер из отрисовки Coil сэмплирует сам.
- **Тестов уровня данных для этого не хватает.** `PlaceMappersTest` и
  `MahallaImageLoaderTest` были зелёными всё это время: ссылки правильные,
  подстановка хоста правильная, а в сеть не уходило ничего. Проверять надо
  композицию — появился `MahallaAsyncImageTest` и, ради него,
  `androidx.compose.ui:ui-test-junit4` в `testImplementation` +
  `ui-test-manifest` в `debugImplementation` (unit-тесты собираются из
  манифеста debug-варианта, оттуда `ComponentActivity` для `createComposeRule`).
- **Robolectric не рисует сам.** После `waitForIdle()` композиция есть, а
  обхода иерархии с отрисовкой нет — на исправном коде запрос всё равно не
  начнётся. В `MahallaAsyncImageTest` кадр рисуется руками
  (`decorView.measure/layout/draw` в холст поверх bitmap'а, отсюда
  `@GraphicsMode(NATIVE)`). Без этого тест зелёный на сломанном коде — то есть
  бесполезный; проверено обратным прогоном на коде до починки.
- **Одного «запрос ушёл» мало.** Второй тест сверяет пиксель: фото
  нарисовано, а не закрыто скелетоном сверху. Иначе достаточно было бы
  оставить оверлей и в успешной ветке — симптом из issue вернулся бы один в
  один, а тест на запрос остался бы зелёным (проверено, краснеет только
  пиксельный).
- **`CrossfadePainter` считает время по `SystemClock`**, а не по тестовым
  часам композиции, поэтому `mainClock.advanceTimeBy` его не двигает.
  В тесте заглушка отдаёт результат с `DataSource.MEMORY_CACHE` — на этом
  источнике Coil crossfade не проигрывает вовсе.

Ревью диффа нашло рядом ещё одно: починка оживила путь, на котором белый
список схем `ImageUrl` обходится (`content://`, `file://` доезжают до Coil).
Отдельной задачей — issue #139, здесь не трогал.

**Не сделано / риски:** на устройстве не проверено, эмулятора в CI нет.
Что именно приедет по ссылкам, зависит от каталога стенда (issue #53) — на
момент issue #60 он отвечал `data: []`. То, что запрос теперь уходит с
размером места под фото, тест сверяет числом.

## Этап: перенос записи — последний пункт эпика #11 (issue #11)

Эпик 7 «Бронирования» был закрыт issue #97 почти целиком: услуга → день → слот
→ подтверждение (7.1, 7.2) и «мои записи» с разделами, пагинацией и отменой
(большая часть 7.3). Непокрытым остался **перенос**, им и занялся этот этап.
Ещё два пункта чеклиста бэкенд не поддерживает вовсе — см. риски ниже.

**Своей ручки переноса у бэкенда нет.** Проверено живым `/v3/api-docs` (стенд
к вечеру 2026-09-08 поднялся, до этого отдавал `502` на всё): под
`appointments` ровно пять путей — `POST`, `my`, `{id}`, `{id}/cancel`,
`{id}/status`. Ни `reschedule`, ни `PUT appointments/{id}` там нет. Поэтому
перенос собран из двух **уже сверенных** ручек, а не из выдуманной третьей.

### Порядок: сначала новая запись, потом отмена старой

Это главное решение этапа, и оно не симметрично.

- **Отмена → запись** теряет запись: если слот ушёл, пока шёл второй запрос,
  вернуть отменённое нечем, и человек остаётся ни с чем.
- **Запись → отмена** в худшем случае оставляет **лишнюю** запись — это видно,
  это чинится одной кнопкой, и об этом экран говорит прямо
  (`booking_reschedule_old_kept`, красным на подтверждении).

Отказ отмены отказом переноса не считается: новое время уже за человеком.
Отказ записи, наоборот, отказ — старая запись при этом не тронута, текст
сервера видно на экране (issue #34).

Плата за выбранный порядок: **неизвестно, даст ли бэкенд вторую запись в том
же заведении, пока висит первая.** Контракт про это молчит, а проверить нечем
— всё под токеном. Если не даст, перенос будет отказывать сообщением сервера,
и это ровно тот случай, когда порядок надо обсуждать с бэкендом, а не менять
наугад: обратный порядок молча терял бы запись. Оба вопроса записаны в
`docs/API-CONTRACT.md` рядом с эндпоинтами.

### Реализация

- **Домен**: `Appointment.canReschedule` и `Rescheduled`. Правило переноса
  строже правила отмены — нужны `placeId` и `serviceId`, потому что вторая
  половина переноса это `POST appointments`, а сервер эти поля в
  `AppointmentResponse` называть не обязан. Прошедшее время перенос не
  запрещает, как и отмену: `PENDING`, до которого заведение не дошло, человек
  вправе передвинуть.
- **Репозиторий**: `BookingRepository.reschedule(appointmentId, placeId,
  serviceId, date, time)`. `cancel(appointment)` разложен на `cancelById` плюс
  вывод состояния — переносу нужен только id, собирать ради этого пустую
  запись незачем.
- **Экран переноса — тот же экран записи** (`BookingRoute` получил
  `serviceId` и `rescheduleId`, обе строки с пустым значением по умолчанию, а
  не `String?`: типизированные маршруты кладут аргументы в `Bundle`, и
  необязательная строка потребовала бы своего `NavType`). Второй экран с
  календарём и слотами разошёлся бы с первым при первой же правке. Услуга при
  переносе не выбирается — показывается как напоминание.
- **Услуга из маршрута не подменяется единственной услугой каталога.**
  Автовыбор «одна услуга — выбрана сама» из issue #97 теперь смотрит, не
  выбрано ли уже что-то: иначе человека при переносе переставило бы на другую
  услугу. Тест на это краснеет, если убрать проверку (проверено обратным
  прогоном).
- **Пропавшая из каталога услуга перенос не блокирует.** `canBook` проверяет
  **id** услуги, а не найденную по нему `BarberService`: заведение вправе
  убрать услугу из списка, но время-то за человеком занято, и запрещать
  перенос из-за строки в каталоге значило бы оставить его с записью, которую
  можно только отменить. Слоты по id сервер отдаёт всё равно.
- **Возврат из переноса**: «Мои записи» с экрана переноса снимают из стека
  **прежний** экран списка (`popUpTo<MyAppointmentsRoute> { inclusive = true }`),
  а обычная запись — саму себя. Иначе «назад» вело бы на устаревшую копию того
  же списка. У обычной записи цели для этого `popUpTo` в стеке просто нет, и он
  не срабатывает.
- **Врача переносить нельзя**, и это не забывчивость: экран «моих записей» один
  на обе вертикали (issue #99), но вторая половина переноса у врача другая —
  `POST hospitals/appointments` ждёт `doctorId` и жалобу, и `serviceId` там
  записал бы человека не к тому. `MyAppointmentsState.canReschedule` это и
  проверяет.
- Кнопка «Перенести» стоит **выше** отмены: время можно передвинуть, и
  предлагать это первым честнее, чем сразу отдавать слот другому.

### Что нашло ревью диффа (и почему это стоило прогона)

Субагент в свежем контексте нашёл три дефекта, которые я не увидел, — все три
исправлены до PR:

- **Подтверждение противоречило само себе.** «Прежняя запись отменена» рисовалось
  безусловно, а ниже красным — «отменить не удалось». То есть ровно в том
  единственном случае, ради которого поле `previousCancelled` и заведено,
  человек читал два противоположных утверждения в одной карточке. Теперь
  описание выбирается по флагу (`booking_reschedule_done_description_kept`).
- **Вторая половина переноса отменялась вместе с экраном.** `apiCall`
  намеренно пробрасывает `CancellationException`; уйди человек с экрана между
  двумя запросами — `viewModelScope` отменился бы, отмена старой записи не
  ушла бы, и о двух записях никто бы не сказал. Отмена завёрнута в
  `NonCancellable`: она компенсирующая половина уже совершённого действия, и
  обрывать её нельзя.
- **Двойное нажатие «Перенести»** кладёт в стек два экрана переноса, и «назад»
  после успеха вёл на второй — перенос уже отменённой записи, то есть прямой
  путь к лишней третьей. Добавлен `launchSingleTop`.

Ревью же указало на непокрытый гард «пока идёт отмена, перенос не начинаем» —
тест добавлен (и краснеет, если гард убрать), а фейк получил `cancelGate`:
с обычным фейком отмена заканчивается в тот же момент, в который началась, и
поведение «во время отмены» проверить было нечем.

Проверено: `./gradlew testDebugUnitTest` — **1786 тестов в 173 классах, 0
падений**; `lintDebug` — BUILD SUCCESSFUL; `assembleDebug` — BUILD SUCCESSFUL.
Новые тесты: `AppointmentTest` (перенос требует заведение и услугу, закрытая
запись не переносится, прошедшая — переносится), `BookingRepositoryTest`
(порядок двух запросов, отказ записи не доходит до отмены, отказ отмены не
отменяет перенос, пустой id не доходит до сети), `BookingViewModelTest`
(услуга из маршрута и её слоты, перенос без услуги в каталоге, тело
`reschedule`, оставшаяся старая запись, отказ сохраняет выбор, без
`rescheduleId` экран записывает как обычно), `MyAppointmentsViewModelTest`
(эффект с заведением и услугой, устаревшее нажатие ничего не открывает, пока
идёт отмена — перенос не начинается, у врача переноса нет),
`RoutesSerializationTest`. Фейк `FakeBookingRepository` дополнен переносом и
управляемой задержкой отмены (`cancelGate`). **Все три теста порядка проверены обратным прогоном** — на
перевёрнутом порядке краснеют.

**Не сделано / риски:**

- **Два пункта чеклиста эпика бэкенд не поддерживает** — заглушек под них не
  делал:
  - **«услуга/мастер» (7.1)**: записи к конкретному сотруднику в контракте
    нет. `ServiceResponse.freelancerId` есть в схеме, но стенд его не шлёт
    (проверено живым запросом 2026-09-08), а слоты приходят на заведение
    целиком — выбирать мастера нечем и незачем.
  - **«предоплата» (7.2)**: платежей у записи нет ни в теле, ни в ответе;
    `payments/*` на стенде существуют только для подписок, а `paymentMethod` —
    у заказов. Пока бэкенд не скажет, чем платить за запись, экран
    предоплаты был бы выдумкой.
- **Успешный путь на живом стенде не проверен**: запись, отмена и перенос
  требуют Bearer, а `CONTRACT_REFRESH_TOKEN` в CI нет. Анонимная половина
  сверена заново: `barber-services/places/{id}` отдаёт `{id, name, colorHex,
  price, durationMinutes}` — как в DTO, — слоты остаются массивом строк,
  `POST appointments` без токена по-прежнему `401`.
- **Каталог стенда наполнился** (было `data: []`, issue #53): 25 заведений по
  центру Ташкента, услуги у двух барбершопов. Значит перенос теперь можно
  проверить руками — нужен только вход по SMS.
- **Окно с двумя записями между запросами** переносом не закрывается совсем:
  клиент честно сообщает о нём и больше не теряет отмену при уходе с экрана
  (`NonCancellable`), но смерть процесса ровно между двумя запросами всё равно
  оставит лишнюю запись молча. Закрыть это может только атомарная ручка на
  сервере.
- **При переносе на экране не видно, откуда переносят.** Ни названия
  заведения, ни прежних даты и времени в маршруте нет — `AppointmentResponse`
  имени места не содержит, а старое время маршрутом не поехало. Обычно видно
  хотя бы услугу, но если каталог услуг не ответил или услугу убрали, человек
  подтверждает перенос, видя только новый день и слот. Указало ревью; отдельная
  задача — провезти маршрутом старое время и подпись записи.
- **Единица `price` по-прежнему не подтверждена** (стенд отдаёт `5000000` за
  стрижку) — вопрос из issue #97 открыт.
- На устройстве ничего не проверено (эмулятора в CI нет): режим переноса,
  предупреждение об оставшейся записи и кнопка в списке проверялись глазами по
  `@ThemeLanguagePreviews`.

## Этап: серверный PIN и app-lock (issue #102)

PIN был локальным (Keystore, эпик 3.4), при этом `pin-code-controller` бэкенда
— **0 из 7**, а `auth/session/check` и `auth/pin-resume` не использовались
вовсе. Расхождение записано как осознанное, но незакрытое ещё с issue #42 и
#51: «при появлении app-lock его придётся закрывать». Закрыто.

Работа сделана 2026-09-04 (PR #120) и перенесена на текущий `main` 2026-09-09:
за пять дней приехали подписки (#103), акции (#104), «Кино» (#106), «Мастера»
(#107), «Одежда» (#108), «Игровые зоны» (#98), перенос записи (#11) и переезд
истории из `AGENTS.md` сюда, так что PR стал конфликтным.

**Контракт снят со стенда** (`https://189-74-96-232.nip.io/v3/api-docs` +
пробы; перепроверен 2026-09-09 — совпадает дословно):

| эндпоинт | контракт | без токена |
|---|---|---|
| `GET pin/status` | query `deviceId` (обяз.) → `{pinSet, biometricEnabled, lockedSecondsRemaining, pinChangedAt, lastUsedAt}` | `401` |
| `PUT pin/change` | `{currentPin, newPin, deviceId}` — все обязательны, PIN `^[0-9]{6}$` | `401` |
| `PUT pin/biometric` | `{enabled, deviceId, pin}` — **PIN обязателен** | `401` |
| `POST auth/session/check` | `{device}` → `{sessionValid, pinRequired, user, reason}` | `401` |
| `POST auth/pin-resume` | `{device, pin, lat, lng}` → `AuthResponse` (токены) | `401` |

Коллизии springdoc здесь нет — схемы уникальны, поля взяты как есть. Первая
задача за несколько, где тело запроса не пришлось выводить из ответа.

Что видно только живым запросом:

- **`auth/session/check` и `auth/pin-resume` требуют Bearer** — в отличие от
  анонимных `auth/pin-login` и `auth/setup-pin` (issue #51). Значит оба API
  собираются на **основном** Retrofit, а не на «голом» `@RefreshClient`. Это и
  логично: `pin-resume` — не вход, а продолжение существующей сессии, и чужой
  аккаунт (issue #86) через него приехать не может. Проба `contract/security.sh`
  закрепляет именно это: 401 без токена, и `SecurityContractTest` краснеет,
  если ручка вдруг станет анонимной.
- **Гео-заголовки обязательны у `pin/…`**: без `X-Geo-Lat`/`X-Geo-Lng`
  приходит `403 GEO_PERMISSION_REQUIRED` раньше проверки токена. Их уже ставит
  `GeoHeaderInterceptor` (issue #53) — вопрос закрылся сам.

### Пункт 4 issue: источник истины у PIN — вынесен в ADR 0013

**Сервер — источник истины, локальный Keystore-хэш это его офлайновая копия.**
Инвариант: локальная копия пишется **только** кодом, который бэкенд принял
(`auth/setup-pin`, `auth/pin-login`, `pin/change`, `auth/pin-resume`). Двух
разных PIN в приложении больше нет. Почему не «только сервер» и что делать при
отказе Keystore — в [ADR 0013](docs/adr/0013-server-pin-i-app-lock.md).

### App-lock

- **`AppLockManager`** — синглтон с `StateFlow<Boolean> locked`. Вооружается
  только когда есть и сессия, и локальная копия PIN.
- **Холодный старт запирает так же, как длинный фон.** Отметка фона живёт в
  памяти процесса, поэтому снятое из недавних (или убитое системой) приложение
  приходило бы на передний план без отметки — и замок обходился бы обычным
  перезапуском, а вместе с ним обесценивался бы и лимит попыток. Нашло ревью
  диффа.
- **Отсрочка 30 секунд** (`GRACE`): код из SMS копируют в приложении
  сообщений, вход через Telegram уходит в Telegram (issue #46), пополнение
  кошелька — в браузер провайдера (issue #93). Часы, переведённые назад, дают
  отрицательный интервал — это «измерить не удалось», и тогда запираем.
- **`ProcessLifecycleOwner`** (`AppLockObserver`, новая зависимость
  `androidx.lifecycle:lifecycle-process`), а не колбэки Activity: он один
  отличает уход в фон от пересоздания Activity, а приложение пересоздаёт её на
  смене языка (эпик 1.5) и темы. Подписка встаёт в `MahallaApplication.onCreate`
  — третье исключение из правила «в Application ничего не появляется».
- **Экран блокировки — оверлей поверх всего** (`MainActivity`), а не маршрут:
  замок обязан накрывать онбординг, экран обновления (issue #80) и ввод адреса
  бэкенда (issue #26), не вмешиваясь в back stack и deep links.
- **`init` у `AppLockViewModel` нет**: она привязана к Activity и переживает
  снятие оверлея, поэтому на **втором** запирании подряд `init` уже не
  выполнился бы — ни промпта, ни проверки сессии. Подготовка идёт событием
  `AppLockEvent.Shown`. Грабля неочевидная, тест её закрепляет.
- **«Назад» сворачивает приложение** (`moveTaskToBack`): под оверлеем живой
  экран с деньгами, а проглоченное нажатие читается как сломанная кнопка.
- **Мёртвую сессию ловит `session/check`**: `sessionValid: false` → выход и
  вход заново. Отказ сети сюда **не** доезжает намеренно: «спросить не
  удалось» — не «сессии нет».
- **Сброс входа с экрана блокировки идёт через `RootViewModel`**, а не через
  `Activity.recreate()`, и замок снимает получатель эффекта, а не сам
  `AppLockViewModel`. Две грабли, найденные ревью диффа: снятый раньше времени
  замок убирает оверлей вместе с коллектором эффектов — `AuthRestartRequired`
  уходил в буфер канала, из которого его больше никто не забирал; а
  `recreate()` сохраняет `ViewModelStore`, то есть `RootViewModel` с уже
  зафиксированным стартом, и приложение возвращалось в `MainGraph` без сессии.
  Оба раза человек оставался на экране, где его застал фон, с 401 на каждый
  запрос.
- **Лимит попыток на замке — локальный и персистентный.** Свой счётчик
  запрещён в *серверном* режиме (там считает бэкенд, issue #51), а замок
  проверяет код без сети — считать серверу нечем. Счётчик переехал в DataStore
  (`PinAttemptStore`), как предписывал ADR 0004: в памяти он обходился снятием
  приложения из недавних, и лимит просто не существовал. Нечитаемый счётчик
  даёт **полный** набор попыток, а не нулевой; счётчик, уже стоящий на
  пределе, ведёт в SMS-вход, а не выдаёт «ещё одну» попытку за каждое снятие.
- **Биометрия открывает замок локально**: ручки «продолжить сессию по
  отпечатку» у бэкенда нет, есть только `pin-resume` с PIN.

### Смена PIN и переключатель биометрии

- **Экран «Безопасность»** (`SecurityRoute`, строка в профиле): статус PIN с
  бэкенда, состояние замка, смена PIN, переключатель биометрии. Отдельный
  экран, а не строки в профиле: у PIN и биометрии одно общее правило
  («подтверди кодом»).
- **Смена PIN** (`ChangePinRoute`): текущий → новый → повтор. Новый код,
  равный текущему, отсекается **до** сети. Несовпавший повтор переспрашивает
  только новый код. Отказ сервера показывается его текстом (issue #34), успех
  **не уводит с экрана сам** (issue #49).
- **Переключатель биометрии требует PIN** — так решил бэкенд. Порядок при
  включении: **датчик → код → сервер**. Обратный порядок записал бы
  «биометрия включена» до того, как выяснилось, что датчик не узнаёт хозяина.
  Ревью диффа нашло здесь блокер: `busy` от промпта не снимался, а поле в
  шторке нарисовано как `enabled = !busy` — включить биометрию было нельзя
  вовсе, шторка открывалась с мёртвым полем. Тест шлёт `PinChanged` во
  ViewModel напрямую и этого не видел, поэтому проверка теперь на сам флаг.
- **`enabled` объявлен без значения по умолчанию** — kotlinx.serialization
  выбрасывает из тела поля, равные дефолту, и выключение уходило бы запросом
  без флага (та же грабля, что у `RevokeSessionRequest`, issue #61).
- **Три ручки из семи не подключены осознанно**: `pin/set` и `pin/reset`
  требуют свежего SMS-кода, `DELETE pin` молча выключил бы app-lock,
  `pin/verify` не понадобился — подтверждение кодом делают сами `change` и
  `biometric`.

**Грабля, стоившая сборки:** `` `pin/*` `` внутри KDoc оставляет файл
незакрытым — блочные комментарии в Kotlin вложенные, и `/*` открывает второй;
`kspDebugKotlin` падает с `Unclosed comment`.

**Грабля, которую поймал тест:** `Char.isDigit()` принимает и деванагари, и
полноширинные `１２３４５６`, а бэкенд проверяет `^[0-9]{6}$` и такой код
отвергнет. `ChangePinRules.isWellFormed` сравнивает с диапазоном `'0'..'9'`.

Проверено: `testDebugUnitTest` — **1941 тест в 185 классах, 0 падений,
4 пропущено** (две пробы под токеном снять нечем — нет `CONTRACT_REFRESH_TOKEN`);
`lintDebug` — `No issues found`; `assembleDebug` — BUILD SUCCESSFUL. Новые
тесты: `ServerPinStatusTest`, `AppLockManagerTest`, `SecurityRepositoryTest`
(MockWebServer: query и тела всех трёх ручек `pin`, инвариант «локальный хэш
пишется только принятым кодом», уборка после отказа Keystore, тело
`session/check` и `pin-resume`, ответ без токенов не роняет разблокировку),
`AppLockViewModelTest`, `ChangePinViewModelTest`, `SecurityViewModelTest`,
`PinAttemptStoreTest` (настоящий DataStore под Robolectric),
`SecurityContractTest` (ответы стенда); фейки `FakeSecurityRepository` и
`FakePinAttemptStore`.

**Не сделано / риски:**

- **На живом аккаунте не проверено** (эмулятора в CI нет, вход требует
  SMS-кода): успешный ответ ни одной ручки под токеном никто не видел —
  проверены только `401`, форма запросов и коды отказов. **Первое, что надо
  проверить руками: `PUT pin/change` принимает тело и после смены вход идёт
  новым кодом; `PUT pin/biometric` — что переключатель доезжает до сервера.**
  Пробу `contract/security.sh` для этого достаточно запустить с
  `CONTRACT_REFRESH_TOKEN` — меняющие ручки она намеренно не дёргает.
- **Критерий «app-lock срабатывает при возврате из фона» закрыт тестами, а
  руками — нет**: `ProcessLifecycleOwner`, оверлей поверх навигации, системный
  промпт и `moveTaskToBack` на устройстве не проверялись.
- **Отсрочка 30 секунд выбрана на глаз** по практике банковских приложений;
  настройки у неё нет — переключатель «запирать сразу / через минуту / никогда»
  это решение продукта.
- **`pinRequired` из `session/check` не используется как команда**: замок
  защёлкивается по клиентскому правилу, от сервера читается только
  `sessionValid`.
- **Что бэкенд считает «залоченной» сессией, из схемы не следует**:
  `pin-resume` под токеном никто не звал. Откажет он при незаблокированной
  сессии — человек увидит текст сервера, но замок всё равно откроется, так и
  задумано.
- **Смена PIN не отзывает другие сессии**: `revokeAll` бэкенду не
  отправляется. Вопрос к продукту.
- **PIN, сменённый на другом устройстве, локальную копию не догоняет.**
  Инвариант «пишем только принятое сервером» разъезд ограничивает, но не
  закрывает: замок будет открываться прежним кодом, а `pin-resume` — отвечать
  отказом. Стирать копию по отказу нельзя, не зная кода ошибки: `pin-resume`
  под токеном никто не звал, и «неверный PIN» от «сессия не заблокирована»
  сейчас не отличить, а лишнее стирание молча выключает замок. Заведено
  issue #194 — сначала нужен ответ стенда.
- **Попытка записывается после проверки кода, а не до.** Смерть процесса ровно
  между несовпадением и записью в DataStore попытку не тратит. Порядок
  выбран сознательно: инкремент до проверки отнимал бы попытку и при отказе
  Keystore, а «не тратить попытку за отказ хранилища» — правило дороже, чем
  окно в несколько миллисекунд, в которое надо успеть убить процесс.
- **`.claude/rules/security.md` создать не удалось** — запись в `.claude/`
  запрещена агенту. Правило про источник истины ушло в `AGENTS.md` и ADR 0013,
  но по-хорошему ему место в `rules/`.

### Доделки по ревью PR #195 (2026-09-18)

Ревью PR оставило девять замечаний; пять из них закрылись по ходу переносов на
`main`, оставшиеся — здесь.

- **`FLAG_SECURE` — снимок окна для «недавних»** (замечание 5). Замок
  защёлкивается на **возврате**, а снимок задачи система делает между
  `onPause` и `onStop`: в списке задач оставался кошелёк с балансом, то есть
  дырка мимо самой фичи. Флаг ставится в `MainActivity.onPause` и снимается в
  `onResume`.
  **Постоянно висеть он не может**: `FLAG_SECURE` запрещает скриншот и пока
  приложение на экране, а талон очереди и QR — как раз то, что человек
  показывает и сохраняет.
  **Гейт «есть ли PIN» не годится** — это первая версия правки, и она была
  неверной: PIN обязателен на входе (токены отдаёт только `setup-pin` или
  `pin-login`, при `nextStep = SETUP_PIN`/`ENTER_PIN` их в ответе нет), значит
  «PIN настроен» истинно у любого вошедшего, и гейт означал бы «запретить
  скриншоты навсегда» — ровно то, чего он должен был избежать. Нашло ревью
  диффа.
  **Руками на устройстве не проверено** (эмулятора нет): порядок «onPause →
  снимок» описан в документации, но на отдельных прошивках снимок делают
  раньше. Окажется рано — флаг придётся вешать постоянно и отдельно решать
  судьбу скриншотов талона.
- **PIN больше не уходит в logcat** (находка из мерж-прогона). `Level.BODY` в
  debug печатал тело целиком, а `HttpLoggingInterceptor` умеет прятать только
  заголовки (`redactHeader`) — PIN, код из SMS и refresh-токен идут телом.
  Ручки из `NetworkFactory.SECRET_BODY_PATHS` логируются на `BASIC`: строка
  запроса, код и время ответа остаются, тело не печатается. Уровень выбирается
  **на запрос, двумя экземплярами**, а не переключением у общего: `level` —
  обычное изменяемое поле, и на параллельных запросах переключение успевало бы
  не туда.
  В список входят и **ответы**: `Level.BODY` печатает их наравне с запросами, а
  `auth/telegram/check` кладёт `accessToken` и `refreshToken` в корень ответа
  (issue #46) — вход через Telegram печатал refresh-токен открытым текстом.
  Нашло ревью диффа; там же добавлен `auth/send-otp` (номер телефона, точные
  координаты, `otpToken` в ответе). Завершающий слэш срезается — иначе
  `…/pin/change/` прошёл бы мимо списка.
  Тест — на **собранном клиенте** через MockWebServer с подставным логгером, а
  не на списке путей: проверка одного лишь списка осталась бы зелёной, если
  выкинуть выбор уровня и вернуть `BODY` на всё.
- **`@Volatile` на `backgroundedAt` и честный KDoc у мьютекса** (замечание 7):
  писали его разные потоки, а комментарий обещал защиту `_locked`, которой лок
  не даёт. Теперь сказано, что он держит только чтение хранилищ в `isArmed`, а
  запись `_locked` не гонка — `MutableStateFlow` потокобезопасен и пишется в
  него всегда одно и то же `true`.
- **Третья копия «текст + подробности + кнопка» убрана** (замечание 9):
  `SecurityFailure` заменён общим `OnboardingApiError`, которому добавлен слот
  `action` — по образцу уже существовавшего у `OnboardingNotice`. Побочно экран
  безопасности получил `liveRegion`: TalkBack проговаривает отказ сам.

Что **не** бралось и вынесено отдельно: биометрия без `CryptoObject` и на
`BIOMETRIC_WEAK` (issue #318) и токены сессии открытым текстом в DataStore
(issue #319). Оба меняют поведение за пределами #102 — `BiometricPrompts` общий
с подтверждением оплаты (8.3), `SessionStore` со всем приложением.
---

## Этап: push-уведомления (FCM) — эпик 11 (issue #15)

**Что сделано.**

- **FCM.** `firebase-messaging` через BOM 33.7.0. Плагин
  `com.google.gms.google-services` применяется **условно** — только если рядом
  лежит `app/google-services.json` (файл в `.gitignore`, это ключи проекта).
  Без него сборка проходит, `BuildConfig.PUSH_ENABLED = false`, экран настроек
  уведомлений говорит об этом прямо. То же правило, что у `MAPKIT_API_KEY` и
  `SENTRY_DSN`: один незаполненный секрет не должен ронять сборку всем.
- **Токен.** `PushTokenStore` (DataStore) + `PushTokenRegistrar`: `onNewToken`
  из `MahallaMessagingService` и опрос текущего токена на старте (из
  `RootViewModel` — `onNewToken` срабатывает только при **смене** токена, а
  первый после установки надо спросить самому). Дальше токен подставляет
  `AndroidDeviceInfoProvider` в `DeviceDescriptor.fcmToken`.
- **Каналы.** `NotificationCategory`: заказы, очередь, брони, платежи,
  маркетинг и «прочее». Заводятся лениво — при первом пуше и при открытии
  экрана настроек, а не в `Application.onCreate`. Важность: `DEFAULT` у
  первых четырёх, `LOW` у маркетинга и прочего.
- **`POST_NOTIFICATIONS`** — карточка в центре уведомлений и на экране
  настроек, **не на старте**: право показать системный диалог тратится один
  раз за установку. Когда оно потрачено, кнопка ведёт в системные настройки.
- **Deep links.** `mahalla://order/{orderId}`, `mahalla://subscription`,
  `mahalla://notifications` (запасная цель) плюс прежний
  `mahalla://place/{placeId}`. `MainActivity` стала `singleTop` и разбирает
  `onNewIntent`: без этого нажатие на пуш при запущенном приложении не
  открывало бы ничего.
- **Тихие часы и категории** — новый экран `NotificationSettingsScreen`
  (из профиля и из центра уведомлений), настройки локальные.
- **Подписка стала целью перехода** и в списке уведомлений тоже:
  `SUBSCRIPTION_EXPIRES` ведёт на `SubscriptionRoute`. Цель безопасна тем,
  что `entityId` ей не нужен.

**Контракт сверен с живым стендом** (`GET /v3/api-docs`, 2026-09-09), не
выдуман. Из сверки следует всё остальное:

- **ручки регистрации устройства у бэкенда нет** — токен уезжает полем
  `AuthDeviceInfo.fcmToken` с ближайшим `send-otp` / `verify-otp` /
  `pin-login` / `refresh`. Задержка равна времени жизни access-токена;
- **серверных настроек уведомлений нет** — поэтому категории и тихие часы
  локальные, и на втором устройстве они свои;
- **схемы payload'а пуша нет.** `PushMessage.of` читает `data` по именам полей
  `NotificationResponse` (`id`, `type`, `entityId`, `title`, `body`) — это
  имена самого бэкенда, но **не подтверждённые**. Записано в
  `docs/API-CONTRACT.md`, сверить, когда бэкенд начнёт слать пуши.

Решение целиком — в [ADR 0009](docs/adr/0009-push-firebase-cloud-messaging.md).

**Грабли и решения.**

- **Тихие часы не выбрасывают пуш, а показывают его без звука.** Иначе
  человек, включивший тишину ради сна, утром не узнал бы, что ночью заказ
  отменили.
- **Интервал через полночь** — это основной случай («с 22:00 до 08:00»), и
  наивное `from <= t && t < to` на нём молчит весь день и звонит всю ночь.
  Закреплено `QuietHoursTest`.
- **Совпадение границ считается пустым интервалом**, а не сутками: «с 22:00 до
  22:00» человек набирает по ошибке, а приложение, замолчавшее навсегда,
  разбирается дороже всего.
- **Хранятся выключенные категории, а не включённые.** Иначе новая категория
  после обновления приложения оказалась бы молчащей.
- **Решение «показывать ли» вынесено в чистую функцию `PushGate`** — на
  настоящем `NotificationManager` отозванное разрешение и выключенный канал
  проверить нечем: эмулятора в CI нет.
- **Пуш всегда куда-то ведёт.** Своего экрана нет у очереди (приходит id
  талона, а экран требует `placeId`), у записи (по типу не отличить мастера от
  врача) и у акции — такие ведут в центр уведомлений.
- **`ViewModel` пишет в DataStore асинхронно**, и первая версия теста
  настроек проверяла `state.value` сразу после события — то есть состояние
  *до* него. Проверки ждут значение (`state.first { … }`).

**Проверено.** `./gradlew testDebugUnitTest` — BUILD SUCCESSFUL (1894 теста в
прогоне до новых, после — все зелёные); `assembleDebug` — BUILD SUCCESSFUL;
`lintDebug` — BUILD SUCCESSFUL. Новые тесты: `NotificationCategoryTest`
(тип → канал, включая незнакомый тип и стабильность id каналов),
`QuietHoursTest` (интервал внутри суток и через полночь, совпадение границ,
испорченное значение), `PushMessageTest` (payload → deep link, пустой payload,
блок `notification`), `PushGateTest` (отозванное разрешение, выключенные
уведомления и канал, выключенная категория, тихие часы),
`NotificationSettingsStoreTest`, `PushTokenRegistrarTest` (токен доезжает до
`DeviceDescriptor`), `NotificationSettingsViewModelTest`; дополнены
`NotificationTargetTest` и `RoutesSerializationTest`.

**Не сделано / риски.**

- **FCM работает через сервисы Google.** На устройстве без них пуш не придёт —
  и это то самое, из-за чего в проекте выбраны Yandex MapKit и Sentry.
  Альтернативы нет: канал доставки задан контрактом бэкенда. Запасной путь для
  таких людей — центр уведомлений по обычному HTTP.
- **Ни один пуш не проверен вживую**: `google-services.json` в CI нет,
  эмулятора тоже. Проверены разбор, маршрутизация и фильтры; связка
  «бэкенд → FCM → шторка → экран» ждёт ключей Firebase.
- **Сообщения с блоком `notification` в фоне показывает сама библиотека**,
  минуя наш сервис: ни каналы, ни тихие часы к ним не применяются. Для таких
  задан канал по умолчанию (`other`) и иконка в манифесте, но чтобы работало
  всё, бэкенд обязан слать **data-сообщения** — просьба записана в
  `docs/API-CONTRACT.md`.
- **Токен доезжает не мгновенно**, а с ближайшим продлением сессии: отдельной
  ручки регистрации у бэкенда нет.
- **Настройки не переезжают между устройствами** — по той же причине.
- **Иконка в шторке — временная заглушка** (`ic_notification.xml`, тот же
  круг, что у launcher-иконки): брендового логотипа в проекте пока нет.
- На устройстве экран настроек уведомлений не проверялся, только глазами по
  `@ThemeLanguagePreviews`.

---

## Этап: «Мои активности» — таб «Заказы» ожил, контракт не продублирован (issue #73, #142)

PR #110 писался 4 сентября и до слияния пролежал пять дней. За это время в
`main` приехали вертикали, которые он себе переписал: `gaming/bookings/my`
(issue #98), `appointments/my` (issue #97), `hospitals/appointments/my`
(issue #99), `cinema/tickets/my` (issue #106), общий `GET orders` (issue #108).
Ветка слита с `main`, дубли убраны (issue #142).

**Что получилось в итоге:**

- Таб «Заказы» — больше не заглушка: один список из пяти источников,
  фильтр «Активные / Тарих», частичный отказ (не загрузился один раздел —
  показываются остальные плюс отметка), пагинация по каждому источнику
  отдельно.
- **Своего `*Api.kt` у фичи нет.** `feature/activity/data/ActivityApi.kt`
  удалён целиком: все пять ручек уже объявлены в вертикалях, которым
  принадлежат, и оттуда же берутся DTO (`OrderViewDto`, `GamingBookingDto`,
  `AppointmentDto`, `CinemaTicketDto`). `DefaultActivityRepository` получает
  пять готовых `*Api` через Hilt; свой модуль остался только с `@Binds`.
- `FashionApi.myOrders(vertical: String?)` — параметр стал нулевым: без него
  общий `GET orders` отдаёт заказы всех вертикалей сразу, одним запросом
  вместо пяти. Для одежды запрос не изменился (Retrofit нулевой `@Query` не
  ставит).
- Правило `hasMore` вынесено в `core/paging/PageBounds.kt` и заменило **четыре**
  копии — у заказов одежды, у мастеров, у кошелька и в самой фиче.
- Приватные `parseLocalDate` и `LocalTimeDto.toLocalTime` заменены на
  `core/format/parseServerLocalDate` и `parseServerLocalTime`.

**Грабли, ради которых и заведён issue #142.** Копия `AppointmentDto` успела
разойтись с оригиналом: у оригинала `startTime` — `JsonElement` (springdoc
описывает `LocalTime` объектом, Jackson шлёт строку — принимаем оба), у копии
сначала стоял объектный тип. Жёсткий тип ронял бы разбор **всей страницы**, то
есть из «Моих активностей» разом выпадали бы оба источника записей — и мастер,
и врач, — а выглядело бы это как «источник пропал из списка». Дубль контракта
стоит ровно этого: правка требует двух синхронных редактирований, и забытое
второе не видно ниоткуда.

**Прогнано после мерж-коммита** (а не до — тем и кончались три сломанных
`main` подряд): `testDebugUnitTest`, `lintDebug`, `assembleDebug` — зелёные.

**Не сделано / риски:**

- **Название заведения в списке не показать.** Ни один из пяти ответов его не
  содержит — только `placeId`. Строка живёт подписью вида «Ovqat buyurtmasi» +
  номер заказа / услуга / место в зале. Дозапрашивать место на каждую строку
  значит N запросов на экран; ждём поля от бэкенда.
- **Кликабельны только заказы «Еды»**: экран статуса построен на её домене
  (этапы кухни, «повторить» кладёт в корзину еды). Остальные вертикали станут
  кликабельными вместе со своими экранами.
- **Время сеанса у билета в кино бэкенд не отдаёт** — только `sessionId`,
  поэтому билет сортируется по времени покупки. `GamingBooking` не отдаёт
  `createdAt` вовсе: бронь без `startTime` остаётся без даты, в конце списка.
- **Живым запросом ничего из пяти не проверено**: все ручки требуют Bearer, а
  `CONTRACT_REFRESH_TOKEN` в CI нет. Схемы взяты из уже сверенных вертикалей.

## issue #143 — на пустой вкладке «Моих активностей» пагинация не запускалась

Вкладку «Активные / Тарих» отбирает **клиент** из уже загруженного списка, а
страницы приходят от пяти источников по своим курсорам. Отсюда состояние,
которое выглядит как ответ, но им не является: первая страница целиком
`DELIVERED`/`COMPLETED`, курсор не пуст — на «Faol» человек видит «активных
нет, всё в истории», а активный заказ лежит на второй странице и не приедет
никогда, потому что просить её некому.

**Почему одного хвоста списка не хватило.** Триггер догрузки живёт внутри
`LoadMoreItem`, то есть срабатывает, только когда хвост попал в видимую область
`LazyColumn`. Сначала его вообще не эмитили (`return` сразу после `EmptyTab`) —
это убрано в issue #142. Но и после этого догрузка остаётся заложником вёрстки:
пустое состояние плюс две-три отметки сбойных разделов при `fontScale 1.5`
вытесняют хвост за нижнюю границу, и он не срабатывает ни разу.

**Как сделано.** Пустую вкладку доливает `ActivityViewModel`, а не композиция:
`drain()` в одной корутине идёт по страницам, пока вкладка пуста. Старт — после
первой загрузки (и после `refresh` / `resume`: они возвращают список к первой
странице) и при переключении на пустую вкладку. Хвост списка остался: на пустой
вкладке он показывает, что список доливается, а при отказе — «повторить» с
причиной.

**Четыре условия остановки, и три из них — про бесконечный цикл.** У хвоста
защита была устроена сама собой: `LaunchedEffect(nextPages)` не перезапускается
на том же ключе. У цикла в ViewModel такой защиты нет, а сеть он дёргает сам,
поэтому останов проверяется по списку, а не по курсору:

- вкладка перестала быть пустой — дальше тянет хвост, по мере прокрутки;
- страница не удалась — причина уходит в хвост с кнопкой «повторить»;
- **страница не принесла ничего нового** — дедупликация съела её целиком,
  значит сервер отдаёт тот же хвост. Проверять номер страницы тут бесполезно:
  его считает клиент (`запрошенная + 1`), и на сервере, который всегда отвечает
  `hasMore`, он растёт вечно;
- потолок в 20 страниц за раз — четыреста записей истории подряд без единой
  активной это уже не «вкладка догружается».

Попутно, из ревью диффа в свежем контексте (правило CLAUDE.md):

- **Догрузка не стартует, пока загрузка первой страницы в полёте.** Раньше
  такого пути не было — `FilterSelected` сети не касался вовсе. Тап по пустой
  вкладке во время pull-to-refresh начинал догрузку со **старого** курсора, и
  её ответ приклеивал к новому списку страницу от предыдущего. `load()` теперь
  отменяет и предыдущую загрузку тоже: два ответа на один экран — это список от
  одного запроса с курсором от другого.
- **Тап по уже выбранной вкладке — не переключение.** `selectable` зовёт
  `onClick` и на выбранном элементе, а человек, увидевший «активных нет», тычет
  в «Faol» именно так: каждый тычок запускал догрузку заново и стирал из хвоста
  «повторить».

Тесты (`ActivityViewModelTest`, все проверены обратным прогоном — на старом коде
краснеют): страницы доливаются сами, пока курсор не опустеет, и активное
приезжает; догрузка останавливается, как только на вкладке есть что показать;
страница без новых элементов останавливает цикл; потолок страниц срабатывает;
отказ страницы останавливает догрузку с причиной и оставляет источник в курсоре;
переключение на пустую вкладку доливает её; тап по вкладке во время обновления и
тап по своей же вкладке в сеть не идут.

`FakeActivityRepository` получил три вещи: `pageFeeds` (ответ по самому курсору —
без него вторую страницу не отличить от третьей), `gate` (запрос висит в полёте,
пока тест смотрит на состояние) и `maxRequests` — предохранитель. Последний
завёлся не из аккуратности: цикл догрузки не суспендится по-настоящему, поэтому
виртуальные часы `runTest` в него не вклиниваются, и сломанное условие остановки
даёт не упавший тест, а **повисший** — проверено, тест висел до внешнего
таймаута. С предохранителем падает за секунды и с внятным текстом.

## issue #144 — время слота: одна трактовка вместо двух (расхождение 5 часов)

«Мои активности» разбирали время двух источников по-разному: запись к
мастеру/врачу — `apptDate` + `startTime` в Asia/Tashkent, бронь игровой зоны —
`parseServerInstant(startTime)`, а он читает зоне-менее строку как UTC. Бронь на
13:00 и запись на 13:00 того же дня оказывались в пяти часах друг от друга, то
есть в разных концах порядка «ближайшее сверху» на вкладке «Активные».

**Разделены две сущности, которые различал только контекст.** Зоне-менее строка
из ответа бэкенда — это либо **отметка события** (`createdAt` и родня): её
ставит сервер своими часами, а живёт он в UTC, — либо **время слота**: его
выбрал человек по часам на стене. Одной функцией на оба случая обойтись нельзя,
поэтому в `core/format/ServerInstant.kt` их теперь две:

- `parseServerInstant` — как было, зоне-менее = UTC. На ней остались отметки
  сервера: `createdAt`, `lastActivityAt`, периоды подписок и акций;
- `parseServerSlotInstant` — зоне-менее = Asia/Tashkent. На ней `startTime` и
  `endTime` брони игровой зоны, и она согласована с `appointmentAt()` записи.

**Правка не в одном файле, и это главное.** Игровая вертикаль была замкнута сама
на себя: `gamingRequestTime` **отправлял** слот в UTC (`13:00` уходило как
`08:00`) и тем же UTC читал ответ. Показ сходился, поэтому симптом видели только
в «Моих активностях», где рядом лежит запись. Починить один разбор значило бы
разъехаться с отправкой и получить те же пять часов уже в самой вертикали —
KDoc `gamingRequestTime` про это прямо предупреждал. Поэтому отправка переехала
в Asia/Tashkent вместе с чтением.

Заодно ушла вторая, невидимая половина той же ошибки: бронь на 13:00 хранилась
у бэкенда как `08:00`, и заведение видело у себя не тот час, а бронь, созданную
не этим приложением (бизнес-панель, сам бэкенд), приложение показывало на пять
часов позже.

**Трактовка стендом не подтверждена** — как и тело `POST gaming/bookings`
(issue #98): `401` приходит до валидации, токена в CI нет. Записана в
`docs/API-CONTRACT.md` как первое, что надо проверить, когда токен появится.
Обоснование — тип поля: `LocalDateTime` без зоны у Jackson означает стенные
часы, а не момент, и для записи к мастеру бэкенд трактует их именно так
(`apptDate` + `LocalTime`).

Из ревью диффа в свежем контексте (правило CLAUDE.md): KDoc в трёх местах
называл отменённое правило (`GamingApi.kt` — тело запроса и `startTime` DTO,
`Gaming.kt` — домен брони) — поправлены, иначе следующая правка гейминга пошла
бы по устаревшему тексту. Плюс находка мимо скоупа: `scheduledAt` заказа
мастера — последнее «время, которое выбрал человек» на правиле отметки сервера,
и трактовка зависит от неподтверждённого типа поля — issue #205.

Тесты: новый `ServerInstantTest` (обе функции, включая условие из issue —
`13:00` слота и `apptDate` + `13:00` записи дают один момент);
`ActivityRepositoryTest` — бронь и запись на один час дают один момент, и
бронь на 13:00 стоит **выше** записи на 14:00 на вкладке «Активные» (симптом
issue в том виде, в каком его видит человек), а прежний тест сортировки
перестал фиксировать UTC; `GamingRepositoryTest` — слот уходит и возвращается в
одной зоне (проверено круговым прогоном: что ушло в теле, то и приехало в
ответе).

Все семь новых и правленых проверок прогнаны обратно — на старой трактовке
краснеют. Полный прогон: `testDebugUnitTest` (182 класса) + `assembleDebug` +
`lintDebug` — зелёные.

## issue #145 — «Мои активности»: двойная загрузка на открытии и гонка догрузки

Два замечания авто-ревью PR #110 про `ActivityViewModel`. Первое оказалось
живым, второе к моменту правки было уже закрыто чужим коммитом — но не
закреплено тестом.

**Двойная загрузка при открытии экрана — была.** `LifecycleEventEffect(ON_RESUME)`
срабатывает на первой же композиции, то есть сразу после того, как список
запросил `init`. Защиты `!isLoading && !isRefreshing` для этого мало: она
отсекает дубль только пока стартовая загрузка в полёте. Успела та завершиться
(кэш ответа, локальный стенд, медленная первая композиция) — и экран открывался
двумя одинаковыми загрузками, а источников у экрана пять: **десять** запросов
вместо пяти на каждое открытие таба.

Теперь первый resume пропускается флагом `resumedOnce`. Флаг живёт в ViewModel,
а не в композабле: композабл пересоздаётся при каждом уходе с таба, а ViewModel
держится за запись бэкстека — и её второй resume перечитать список как раз
обязан, иначе экран перестал бы показывать то, ради чего его открывают.

Заодно (из ревью диффа в свежем контексте) починена вторая половина того же
дубля: проверка `!isLoading && !isRefreshing` не отсекала и **два resume
подряд** — диалог поверх экрана, быстрый уход в фон и обратно. Загрузка от
resume идёт молча и ни одного из этих флагов не поднимает, так что защиты не
было вовсе; теперь проверяется `loadJob?.isActive`.

**Гонка догрузки с обновлением — уже не воспроизводится.** Замечание описывало
`loadMore()`, который не смотрел на `isRefreshing`; в #142/#143 там появилась
проверка `loadJob?.isActive` — она строго шире, потому что обновление идёт этим
же job'ом, а активен он дольше, чем поднят флаг. Отдельная проверка
`isRefreshing` из текста issue была бы мёртвым кодом: тест, который бы её
краснел, написать нельзя. Обратный порядок — обновление стартовало **после**
догрузки и ответило раньше — держится на `loadMoreJob?.cancel()` в `load()`:
отменённая догрузка своего ответа уже не применяет.

Поэтому правка второго замечания — тесты, которых на обоих направлениях не
было:

- `load more while the list is refreshing does not touch the network` — хвост
  списка виден и во время обновления, и его первая композиция просит догрузку с
  курсором от списка, который вот-вот уедет;
- `a refresh started after a load more wins` — догрузка висит в полёте,
  обновление отвечает первым, ответ догрузки приезжает после: список и курсор
  должны остаться от обновления.

Оба проверены обратным прогоном (правило `.claude/rules/testing.md`): с
выломанной проверкой `loadJob?.isActive` краснеет первый, без
`loadMoreJob?.cancel()` — второй. Тем же способом проверены тесты первого
замечания: `the first resume does not double the initial load` (снят флаг) и
`a second resume while the silent reload is in flight is ignored` (проверка
job'а заменена на `isRefreshing`). Тест `coming back to the screen re-reads the
list` переписан под новое поведение: первый `ScreenResumed` — открытие экрана,
запроса не даёт, второй — настоящий возврат.

Прогон: `testDebugUnitTest`, `assembleDebug`, `lintDebug` — зелёные.

## issue #151 — «Мои активности»: «Показать ещё» вместо бесконечной автодогрузки

Открытый риск PR #110, отмеченный и авторами, и авто-ревью. Догрузка списка
висела на `LaunchedEffect(state.nextPages)` внутри хвоста `LoadMoreItem`, а
курсор сдвигается после **каждой** удачной страницы — значит эффект
перезапускал себя сам. Останавливала это только вёрстка: список должен был
перерасти экран и вытеснить хвост из видимой области. У человека с парой
активностей он не перерастает никогда, поэтому одно открытие таба вычитывало
все страницы всех пяти источников — пятикратными запросами подряд.

Теперь хвост — кнопка **«Показать ещё»** (`activity_load_more`, uz + ru), одно
нажатие — одна страница. Пока страница в полёте, кнопка показывает спиннер
внутри себя (`ButtonState.Loading`: видима, озвучена, не нажимается), а не
подменяется крутилкой — заодно ушёл случай «крутится навсегда». Провал
догрузки по-прежнему показывает причину с «повторить».

**Что осталось автоматическим и почему.** Пустая вкладка при непустом курсоре
доливается сама (issue #143) — там кнопку нажимать некому: человек видит
«активных нет» и уходит, не узнав, что активное лежало страницей ниже. Этот
случай ограничен и без экрана: остановка на первой же непустой вкладке, на
странице, не принёсшей ничего нового, и потолок `MAX_DRAIN_PAGES = 20`.

Тесты (+7): новый `ActivityScreenTest` — проверка на **композиции**, потому что
и риск был в дереве, а не в ViewModel: события `LoadMore` там приходят от
теста, а не от экрана, и увидеть автотриггер тесты уровня ViewModel не могли.
Robolectric + `createAndroidComposeRule` (готовый образец — `MahallaAsyncImageTest`),
экран 393×852 dp mdpi:

- хвост не просит страниц сам, в том числе после **сдвига курсора** — ровно то
  состояние, на котором старый эффект перезапускался;
- то же на **пустой** вкладке: там хвост в видимой области с первого кадра, то
  есть автотриггер срабатывал бы первым делом, — страницы её доливает ViewModel,
  а не экран;
- одно нажатие — одно событие `LoadMore`;
- пока страница в полёте, кнопка не нажимается и озвучена как занятая
  (`stateDescription`), а не как недоступная;
- пустой курсор не оставляет хвоста вовсе — иначе кнопка предлагала бы
  страницу, которой нет;
- сбойная страница даёт причину с «повторить», а не «Показать ещё».

Плюс в `ActivityViewModelTest` — `each tap on the button is one page, and no tap
is no page`: бесконечная история при непустой вкладке даёт по одной странице на
нажатие и ни одной без нажатий.

Обратный прогон (правило `.claude/rules/testing.md`): с возвращённым
`LaunchedEffect(state.nextPages)` краснеют четыре теста `ActivityScreenTest` из
шести.

Из ревью диффа — два замечания, к скоупу #151 не относящиеся, оформлены issue:
**#213** (возврат на таб перезаписывает список первой страницей и схлопывает
набранные нажатиями страницы — раньше это маскировала та самая автодогрузка) и
**#214** (тот же хвост скопирован в одиннадцать экранов; десять из них ещё на
`LaunchedEffect(itemCount)`).

Прогон: `testDebugUnitTest` (183 класса, 1935 тестов), `assembleDebug`,
`lintDebug` — зелёные.
---

---

## Контракт: перепроверка трёх пробелов эпика 7 и развод коллизии springdoc (issue #154)

Кода не писал: три пункта из #154 упираются в `jack5505/mahalla`, и заглушки
под них были бы выдуманным контрактом. Что сделано — сверка живого
`/v3/api-docs` (`200`, 163 865 Б, 2026-09-10) и запись того, что теперь
**читается**, а не выводится.

**Три пункта подтверждены как пробелы бэкенда** — за сутки не изменилось
ничего:

- **атомарного переноса нет**: под `appointments` пять путей (`POST`, `my`,
  `{id}` только `GET`, `{id}/cancel`, `{id}/status`), поиск по документу на
  `resched|transfer|move` пустой. Вопрос «даёт ли сервер вторую запись, пока
  висит первая» проверить нечем: всё под токеном, `CONTRACT_REFRESH_TOKEN` в
  CI по-прежнему нет;
- **мастера выбрать нечем**: у `AppointmentServiceResponse` нет `freelancerId`
  **в самой схеме**, у слотов ровно три параметра (`placeId`, `serviceId`,
  `date`);
- **предоплаты нет**: ни `prepayment`, ни `paid`, ни суммы к оплате;
  `payments/*` остались подписочными, прибавились `subscription/activate` и
  `transactions`. Пара `purpose` + `purposeId` у `PaymentTransaction` выглядит
  местом, куда запись могла бы лечь, но это догадка.

**Коллизия springdoc разведена — и это меняет способ работы.** Голых
`BookRequest` / `ServiceResponse` / `Response` в схеме больше нет, 255 имён
уникальны. Значит всё, что в клиенте было помечено «имена выведены из схемы»,
теперь проверяется чтением. По чтению проверена вертикаль записи и то, что
попалось рядом; остальные KDoc про коллизии (`CreateRequest`, `OrderResponse`
у еды / одежды / мастеров, `Response` у walk-in) — сквозным проходом в
issue #235. Что проверено:

- **тело `POST appointments` угадано верно**: `AppointmentBookRequest {placeId,
  serviceId, serviceName, date, startTime}`, обязательные — `date`, `placeId`,
  `startTime`. Годовой риск «самое рискованное место вертикали» снят, KDoc
  переписан;
- **тело `POST gaming/bookings` тоже угадано верно**: `GamingBookRequest
  {zoneId, startTime, durationHours}` — раньше это имя занимал больничный
  вариант;
- `AppointmentBookingResponse.status` — enum `PENDING | CONFIRMED | CANCELLED |
  COMPLETED | NO_SHOW`;
- `description` и `freelancerId` у услуги барбершопа не «стенд не шлёт», а
  **не предусмотрены схемой**: оба принадлежат `FreelancerServiceResponse`,
  то есть вертикали мастеров. Отсюда же поломка из #216;
- у `ReviewResponse` нет ни аватара, ни имени автора — подтверждает #192.

**Единица денег закрыта бэкендом.** В `info.description` живого документа
теперь прямо написано: все целые денежные поля — в **тийинах**, 1 сум = 100
тийин, делить на 100 перед показом, `5000000` → `50 000 so'm`. Это ответ на
issue #149, который висел открытым по всем вертикалям: «Еда», бронирование,
гейминг, акции считали сумами и **рисуют суммы в сто раз больше настоящих**.
Ответ и разбор — комментарием в #149; правка сквозная и в скоуп #154 не лезет.

**Ревью диффа субагентом нашло восемь мест, где новый текст противоречил
старому** — и это оказалось самой полезной частью задачи. Записать «коллизия
разведена» в одном файле мало: рядом остаются KDoc и абзацы, которые
утверждают обратное, а агент читает то, что попалось первым. Поправлено по
находкам ревью:

- `GamingApi.book` в KDoc до сих пор требовал «первым делом проверить тело
  руками под токеном» — тело уже прочитано;
- `BarberService` утверждал «единица не подтверждена, делитель наугад не
  меняем» и ссылался на `docs/API-CONTRACT.md`, где теперь написано обратное;
- `WalletAmounts` — «единица целого поля в схеме **не описана**»; описана,
  и `scaleOf` теперь должен стать жёсткой сотней вместо вывода из пары;
- в таблице `GamingApi` строка `POST gaming/bookings` осталась «⚠️ тело не
  проверено» под абзацем «сверено чтением»;
- раздел `FreelancerApi` уверял, что услуги мастеров «переехали на выверенные
  `name`/`price`» — ровно та ошибка, из которой выросла #216;
- KDoc `ServiceDto` привязывал класс к одной схеме, хотя он разбирает две
  (барберскую и мастеров) — отсюда #216 и видна;
- формулировка «`price` — тийины, делить на 100» читалась как описание
  поведения; в коде деления нет, это задача #149;
- «имя автора отзыва всегда пустое» — на экране оно подменяется словом
  «аноним» (#192 от этого не перестаёт быть багом, но пустого поля человек не
  видит).

**Прогнано:** `testDebugUnitTest`, `assembleDebug`, `lintDebug` — зелёные
(изменения только в документации и KDoc, но правило есть правило).

**Не сделано:** три пункта #154 остаются за бэкендом, issue открыт. Делитель
тийинов (#149) — отдельной задачей: он трогает все экраны с суммами.

**Второй круг: авто-ревью PR #217 (`REQUEST_CHANGES`) нашло, что источник
#216 остался нетронутым.** Список «восемь исправленных мест» выше включал
раздел `FreelancerApi` в контракте, а сам KDoc `FreelancerApi.services()` так
и утверждал «та же схема, что у `barber-services`» — то есть отчёт расходился с
диффом, и ошибку читал бы первым тот, кто откроет файл. Поправлено:

- KDoc `FreelancerApi.services()` — теперь `FreelancerServiceResponse`, с
  прямым указанием, что разбирается не той схемой (#216);
- фикстура в `FreelancerRepositoryTest` больше не притворяется барберской:
  `title`/`priceAmount`/`freelancerId`, как в живой схеме. Плюс отдельный тест,
  который **закрепляет баг #216** (пустое название, цена 0) и обязан
  покраснеть, когда появится свой DTO — иначе #216 в CI был бы зелёным
  навсегда;
- KDoc `ReviewDto` — не «два вероятных имени автора», а «автора и аватара в
  `ReviewResponse` нет»; строка в `BookingMappers` — «description нет в схеме»,
  не «бэкенд не отдаёт»;
- фраза контракта «любое „выведено из схемы“ теперь можно проверить» сужена до
  реально перепроверенного, остальное — issue #235;
- KDoc `WalletAmounts` был перестроен (вывод масштаба, затем почему он стал
  избыточным) — правка не дожила до мержа, см. третий круг ниже;
- шапка раздела `FreelancerApi` в контракте («НЕ СВЕРЕН») противоречила самому
  разделу, где ручки сверены curl'ами 2026-09-04 и схемой 2026-09-10;
- комментарий у `GamingBooking.MAX_HOURS` уверял, что у бэкенда ограничения
  нет: в схеме `durationHours` — целое от 1 до 24, наши восемь просто строже.

**Осиротевшие маркеры конфликтов.** В `docs/API-CONTRACT.md` (два) и в
`CHANGELOG.md` (один) остались `=======` от прошлых слияний: внутри markdown
и KDoc они не ломают ни сборку, ни lint, поэтому доехали до ревью. Убраны
вместе со слиянием `main`.

**Третий круг: пока PR ждал, делитель тийинов слился (#233), и часть этой
записи устарела в момент мержа.** Это ровно та цена откладывания сквозной
правки, о которой сказано выше: документация про «живой баг» жила ровно сутки.
При слиянии снято следующее:

- `WalletAmounts.kt` в `main` **удалён** — единицу больше не выводят из пары
  «целое + `*Som`», пересчёт делает `core/format/Money`. Поэтому и KDoc,
  который эта ветка правила, и абзац контракта про `scaleOf` сняты, а не
  перенесены;
- KDoc `BarberService` взят из `main`: `priceSum` теперь **в сумах**, пересчёт
  в маппере. Формулировка ветки («как отдал сервер, без пересчёта») стала
  неверной;
- вводный абзац контракта про тийины оставлен в версии `main` — он полнее
  (перечисляет поля, дробных близнецов и проценты, которые не делятся);
- цена в фикстуре мастера поднята до `15000000`, чтобы пересчёт был виден:
  разбор всё равно идёт барберским DTO, поэтому в домене остаётся 0 (#216).

---

## issue #155 — экран переноса не показывал, что и с какого времени переносят

Находка ревью PR #153. Экран переноса — это экран записи с
`BookingRoute(placeId, serviceId, rescheduleId)`: услуга приезжала только из
каталога (`barber-services/places/{placeId}`), а прежние день и время не
приезжали вообще. Каталог не ответил или заведение убрало услугу из списка —
и на экране оставались заголовок «Перенос записи», подсказка и новый выбор,
то есть перенос подтверждали вслепую.

Провезено маршрутом (`BookingRoute`): `rescheduleLabel` — подпись записи
(`Appointment.serviceName` из той же строки списка, по которой нажали
«Перенести»), `rescheduleDate` и `rescheduleTime` — прежние день и время.

- **День и время едут строкой ISO** (`2026-09-06`, `10:40`), а не готовым
  «06.09.2026, 10:40»: аргументы маршрута переживают и смерть процесса, и
  смену языка, а формат — дело экрана. Разбор мягкий (`runCatching`): битое
  значение читается как «не назвали», падать из-за подписи над календарём
  экран не вправе.
- **Название: каталог первым, подпись маршрута — запасной вариант.** В
  каталоге имя живое, но его может не быть; подпись из «моих записей» есть
  всегда, когда сервер её назвал.
- **Подпись маршрута ушла и в фоллбэк подтверждения**: `booked.serviceName`
  теперь берётся из ответа сервера → каталога → подписи маршрута. Той же
  цепочкой названа услуга в итоговой карточке над кнопкой: раньше при молчащем
  каталоге перенос подтверждали с безымянной карточкой.
- **Оба времени подписаны.** На экране переноса их теперь два в одном формате,
  и различать их порядком на экране нельзя: сверху «Сейчас: …», в итоге —
  «Новое: …» (`booking_reschedule_new`). Указало ревью диффа.
- Три id и три новых поля в лямбде `onReschedule` собраны в
  `RescheduleTarget` — перепутанные местами строки компилятор бы не заметил.
- Строка на экране: `booking_reschedule_current` («Сейчас: %1$s» / «Hozir:
  %1$s») над выбором нового дня, `tnum`. День без времени и время без дня —
  оба случая законны по контракту, показывается то, что есть.

Тесты: `BookingViewModelTest` (подпись и прежнее время из маршрута доезжают в
состояние при молчащем каталоге; битая дата читается как `null`, а не роняет
экран; подтверждение подставляет подпись маршрута, а при живом каталоге —
название из него), `MyAppointmentsViewModelTest` (эффект несёт подпись, день и
время записи; запись без имени услуги уезжает с пустой подписью),
`RoutesSerializationTest` (порядок и имена всех семи аргументов + round-trip).
**Тест приоритета проверен обратным прогоном**: с перевёрнутой цепочкой
«каталог → подпись» краснеет — поэтому подпись в тесте намеренно не совпадает с
названием услуги в каталоге. Прогон: `testDebugUnitTest` — 177 классов
зелёные, `assembleDebug` и `lintDebug` — успешно.

**Не сделано:** названия заведения на экране переноса по-прежнему нет — его нет
в `AppointmentResponse`, вопрос к бэкенду висит в #154. Экран на устройстве не
проверялся (эмулятора в CI нет), только `@ThemeLanguagePreviews`; добавлено
превью «каталог не ответил».

---

## Больницы: отмена записи ушла на свою ручку (issue #167, 2026-09-10)

Ветка `claude/issue-167-20260910-0323`.

`HospitalApi.cancel` слал отмену записи к врачу в общую ручку брони
`POST appointments/{id}/cancel`: на 2026-09-04 (issue #99) своей отмены у
`hospital-controller` не было. В схеме от 2026-09-09 она появилась —
`POST hospitals/appointments/{id}/cancel`, — туда клиент теперь и ходит.

**Схема заодно ответила на два старых вопроса.** Коллизия springdoc вокруг
имени `BookRequest` рассосалась: у каждого пути своё тело
(`AppointmentBookRequest`, `HospitalBookRequest`, `GamingBookRequest`), а
имени `BookRequest` в схеме больше нет. Из этого следует:

- **выведенные когда-то имена полей совпали** — и у брони
  (`{placeId, serviceId, date, startTime}`), и у игровой зоны
  (`{zoneId, startTime, durationHours}`, `durationHours` — 1..24). Правок в коде
  не понадобилось, но `BookingApi`, `GamingApi` и `contract/booking.sh` больше
  не пугают ненаступившим риском;
- **вертикали записи на бэкенде разные**: `HospitalAppointmentResponse` против
  `AppointmentBookingResponse`, у каждой свои ручки. Это и есть довод, почему
  общая отмена больничную запись отменить не может. Утверждение «у бэкенда это
  одна модель и одна ручка отмены» вычищено из `AppointmentsSource`,
  `Appointment`, `Routes` и `HospitalRepository` — оно стояло в четырёх местах.

Проверено: `./gradlew testDebugUnitTest` — **1842 теста в 177 классах, 0
падений**; `assembleDebug` и `lintDebug` — BUILD SUCCESSFUL. Тест
`HospitalRepositoryTest.cancel goes to the hospital endpoint` проверен обратным
прогоном: со старым путём краснеет.

**Не сделано / риски:**

- **Пробы под токеном как не было, так и нет.** `CONTRACT_REFRESH_TOKEN` в CI
  не задан, а `401` приходит **до** маршрутизации: оба пути отвечают им
  одинаково, и даже заведомо несуществующий путь под `hospitals/appointments/`
  отвечает `401` (проверено `curl` 2026-09-10). Значит «отменяет ли старая
  ручка больничную запись» осталось невыясненным — решение принято по схеме, а
  не по ответу сервера. Собственная ручка вертикали в любом случае правильнее.
- **Ответы больниц по-прежнему разбираются DTO брони.** Общих полей хватает на
  всё, что показывает экран, но `doctorId` и `complaint` теряются, а
  `serviceName` у больничной записи не придёт никогда — в «моих записях» у
  каждой записи к врачу заголовок будет заглушкой «Врач не указан»
  (issue #219).
- **У врача появилась ручка слотов** `GET hospitals/doctors/{id}/slots?date=`,
  а приложение по-прежнему рисует сетку времени само (`DoctorSchedule`), то
  есть предлагает занятое время (issue #220).
- **Найдено попутно, не чинилось**: `FashionApi` шлёт заказ телом «Еды»
  (`{placeId, items}`), а в схеме у него свой `FashionPlaceOrderRequest` —
  корзинный, с обязательным `storeId` и без `items` вовсе (issue #221).

---
## Этап: инвентаризация проекта — снимок 2026-09-09 (issue #36)

Кода не менялось: переписаны `docs/UI-INVENTORY.md` (снимок был от 2026-08-30
и врал почти во всём) и разделы AGENTS.md про состояние проекта.

**Прошлый снимок устарел за десять дней настолько, что планировать по нему
значило переписывать сделанное.** Он утверждал: «три из четырёх табов —
заглушки», «продуктовых вертикалей нет ни одной», «в приложении 12 эндпоинтов
против 129 на стенде», «карта — заглушка со списком маркеров», «кошелёк
показывает зашитое `DEMO_BALANCE_SUM`». На 2026-09-09 в `main`: заглушка одна
(«Заказы»), вертикалей девять, эндпоинтов 77, карта — настоящий `MapCanvas`,
кошелёк ходит в `GET wallet`.

### Что сверялось с чем

- **Приложение против живого стенда.** `/v3/api-docs` поднят, снят и разобран:
  **180 эндпоинтов** в 164 путях. Клиент зовёт **77 уникальных** (79
  аннотаций: `orders/{id}` и `appointments/{id}/cancel` объявлены в двух `*Api.kt`
  каждый). Главное здесь — **пустая разница в другую сторону**: ни одного пути,
  которого на стенде нет. Выдуманный контракт, стоивший переделки OTP и «Еды»,
  на сегодня изжит.
- **Разница 103 ручки — не «отставание на порядок».** Около 63 из них —
  бизнес-панель, админка и кабинет мастера. Клиентских незакрытых областей
  **десять**, и четыре из них уже в открытых PR.
- **Открытые PR против issue.** 12 открытых PR, и почти всё «что осталось» —
  в них. Таб «Заказы» написан **дважды** (PR #110 и #152 по одной issue #73),
  а на второй висит одиннадцать issue ревью (#141–#151), включая
  «контракт продублирован — эти API уже есть в `main`» (#142). Отсюда пункт 0
  в рабочем цикле AGENTS.md: прогон начинается с `gh pr list`.
- **Техдолг перепроверен по коду, а не перенесён списком.** Закрылось само:
  `exportSchema = true` с миграциями (#64), `City.fromId` используется в
  четырёх местах, `DEMO_BALANCE_SUM` удалён, `MapCanvasPlaceholder` из графа
  ушёл, количества переведены plural'ами (26 в каждой локали). Осталось:
  `BottomNavItem.route: Any`, мёртвый `SearchEvent.QueryCleared`,
  `synchronized` + `runBlocking` в `TokenAuthenticator`, две a11y-строки со
  склейкой через `%1$d`.

### Найденные расхождения, которых не было ни в одном документе

Каждое заведено issue — документация не трекер: **#167** (отмена у врача),
**#168** (карта), **#169** (аналитика), **#170** (`users/me`).

- **Отмена записи к врачу шлётся на `POST appointments/{id}/cancel`** — и в
  KDoc `HospitalApi` написано почему: на 2026-09-04 своей отмены у `hospitals`
  в контроллере не было. В схеме от 2026-09-09 она есть
  (`POST hospitals/appointments/{id}/cancel`, `hospital-controller`), то есть
  бэкенд её добавил после issue #99. Какая из двух отменяет запись больницы —
  не проверено (всё под токеном).
- **Карта грузит маркеры радиусом, а не областью**: `MapViewModel` берёт их из
  `CatalogRepository` (`places/nearby`), `GET places/map-bounds` не зовёт никто.
- **`POST analytics/track` не зовёт никто** — продуктовой аналитики в
  приложении нет вовсе (а issue #36 названа «аналитика и инвентаризация»).
- **`GET/PUT users/me` не используются**: данные пользователя приходят только в
  ответе на вход, имя и аватар на сервере менять нечем.
- **`.claude/rules/testing.md` называет 163 тест-класса**, их 177. Правка
  агентом не доезжает: локально окружение просит подтверждения на
  `.claude/**`, а в CI каталог входит в `SENSITIVE_PATHS` action'а и
  восстанавливается из базовой ветки поверх ветки PR. Число записано в
  `docs/UI-INVENTORY.md`, файл правится руками.
- **Девять KDoc утверждают, что `GET/PUT users/me` у бэкенда нет** — правда на
  момент issue #61, в схеме 2026-09-09 обе ручки есть. Комментарий в коде
  убедительнее документа, поэтому список файлов записан и в
  `docs/UI-INVENTORY.md`, и в issue #170.

### Что нашло ревью диффа (и почему инвентаризацию тоже надо ревьюить)

Субагент в свежем контексте перепроверил утверждения по коду и нашёл **ложное
число там, где оно опаснее всего** — в файле, который позиционируется как
источник истины:

- **«Ноль `<plurals>`»** — их **26 в uz и 26 в ru**. Ошибка ровно того рода,
  ради которой в файле и написаны команды подсчёта: `<plurals>` лежат в
  отдельном `res/values*/plurals.xml`, а grep шёл по `strings.xml` и честно
  выдавал ноль. Утверждение было повторено трижды и противоречило
  `.claude/rules/i18n.md`; следующий агент завёл бы по нему работу, сделанную
  ещё раньше. Осталась узкая щель — `otp_input_description` и
  `pin_input_description` со склейкой через `%1$d`, так и записано.
- **«Клиентских незакрытых областей шесть»** против собственной таблицы на
  десять строк — поправлено на десять.
- **Команда подсчёта эндпоинтов в файле не давала 77**: паттерн обрывался до
  пути, `sort -u` по префиксам дал бы 4. Заменена на рабочую.
- **«Мастер — не `SearchRoute` с категорией»** — на самом деле оба пути живы:
  плитка «Мастера» ведёт в каталог заведений с категорией `BARBER`, а
  `FreelancersRoute` — в каталог исполнителей.
- **Механизм с `.claude/**` описан неверно**: запрета на правку нет, каталог
  восстанавливается из базовой ветки поверх ветки PR.
- **Долги оплаты (#157, #149) стояли в техдолге `main`**, хотя этого кода в
  `main` нет — раздел разделён на «в `main`» и «в открытых PR».
- **Deep link «из пуша»** — пуша в `main` нет вовсе (Firebase приходит с
  PR #159); в `main` ссылка одна, `mahalla://place/{placeId}`.
- Плюс подсказка, которой в документе не было: **девять KDoc про
  `users/me`** — см. выше.

Проверено: `./gradlew testDebugUnitTest` — **1842 теста в 177 классах, 0
падений**, 2 пропущено (`BookingContractTest` без `CONTRACT_REFRESH_TOKEN` —
не падает, а тихо не проверяет ничего); `./gradlew assembleDebug` — BUILD
SUCCESSFUL. Ревью прогнало оба таргета независимо и получило те же числа.
Правки после прогона — только в `.md`.

**Не сделано / риски:** с дизайн-репозиторием не сверялось ничего —
`DESIGN_REPO_PAT` не задан, каталога `design-repo/` в прогоне нет, поэтому
соответствие 45 экранов макету по-прежнему неизвестно. Состояние экранов взято
из кода и графа навигации, на устройстве не проверялось (эмулятора нет).
Числа — снимок: у 12 открытых PR они изменятся в день мержа, поэтому в файле
рядом с каждым стоит команда, которой оно пересчитывается.

---

## Этап: продуктовая аналитика — `POST analytics/track` (issue #169)

До этого приложение не вызывало аналитику вовсе: `grep -rn 'analytics/track'
app/src` не находил ничего. Про поведение пользователей не было известно
ничего — ни какие экраны открывают, ни где обрывается воронка заказа.

**Главная находка: ручка не та, которую ждала issue.** Контракт снят со стенда
(`/v3/api-docs` + curl'ы 2026-09-10) и записан в `docs/API-CONTRACT.md`:

```
POST /api/v1/analytics/track
TrackEventRequest: {placeId (обязателен, uuid), eventType (обязателен,
  VIEW|LIKE|SAVE|SHARE|CALL|NAVIGATE|BOOK|ORDER|REVIEW), lat, lng, metadata}
```

Это **счётчик взаимодействий с заведением**, а не продуктовая аналитика.
Из списка в issue лёг на контракт только один пункт из четырёх:

| просили | получилось |
|---|---|
| шаги воронки заказа/записи | ✅ в семи вертикалях из девяти: `BOOK` ×5, `ORDER` ×2 |
| открытие экрана | ⚠️ только карточка места (`VIEW`) — у остальных нет `placeId` |
| поиск | ❌ нечем: нет ни `placeId`, ни вида события |
| отказы бэкенда | ❌ нечем, по той же причине |

Выдумывать `eventType` вроде `SCREEN_VIEW` нельзя — перечисление на бэкенде
закрыто, чужое значение он не примет. На недостающее заведена issue #226.

**Что появилось:**

- `core/analytics/AnalyticsEvent.kt` — `AnalyticsEventType` (ровно девять имён
  бэкенда, `serverName` отделён от имени в Kotlin), `AnalyticsVertical` и
  фабрика `AnalyticsEvents`. Строк по экранам нет: событие можно создать только
  через фабрику, иначе одно действие приехало бы в панель под двумя именами.
- `data/network/analytics/` — `AnalyticsApi` + `AnalyticsRepository`.
- `core/analytics/AnalyticsTracker.kt` — «выстрелил и забыл»: не suspend, без
  результата, на своей области с `SupervisorJob`.
- `docs/adr/0006-analitika-bez-seti.md` — событие без сети **теряется**.

**Три факта контракта, из которых следует всё устройство отправки:**

- **токен обязателен** (аноним с гео-заголовками получает `401`, без гео —
  `403`, то есть гео проверяется раньше). Каталог смотрят до входа, поэтому без
  сессии запрос не делается вовсе — иначе `TokenAuthenticator` полез бы за
  refresh, которого нет;
- **батчинга нет** — под `analytics` только `track` и
  `places/{placeId}/dashboard` (бизнес-панель, эпик #16);
- **поля времени события в запросе нет** — момент события для бэкенда это
  момент, когда доехал запрос. Отсюда и ADR: накопленное в Room событие
  встало бы в воронку не туда, где произошло, и тихо испортило бы ровно те
  числа, ради которых аналитику заводили. Пропуск виден как пробел, порча не
  видна никак.

**Грабли, стоившие бы падения приложения:** отправка идёт в `launch` на
области без `CoroutineExceptionHandler`, а исключение из такой корутины уходит
в `Thread.uncaughtExceptionHandler` — то есть роняет приложение из-за события
аналитики. Внутри `launch` стоит `runCatchingCancellable`; тест
`AnalyticsTrackerTest` подставляет в область обработчик-ловушку и требует,
чтобы он молчал (проверено: без `runCatchingCancellable` тест краснеет).

**Где размечено** (семь вертикалей + карточка): `VIEW` — открытие карточки
места один раз на ViewModel, а не на каждый `Retry`; `CALL`/`NAVIGATE` — вместе
с эффектом, а не вместо него (нет телефона — действия не было); `REVIEW` —
после подтверждения сервера; `BOOK` — запись (`booking`), врач (`hospital`),
игровая зона (`gaming`), талон (`queue`), билет (`cinema`); `ORDER` — еда
(`food`) и одежда (`fashion`, у неё `storeId` и есть `placeId`). Вертикаль
уезжает в `metadata.vertical`, потому что `BOOK` у бэкенда один на все.
Отказ сервера события не создаёт — иначе воронка показала бы заказы, которых
не было; на это есть тест в каждой из семи вертикалей.

Проверено: `./gradlew testDebugUnitTest --rerun-tasks` — **1876 тестов в 180
классах, 0 падений**, 2 пропущено (`BookingContractTest` без
`CONTRACT_REFRESH_TOKEN`); `assembleDebug` и `lintDebug` — BUILD SUCCESSFUL.
Ревью диффа субагентом в свежем контексте нашло шесть правок, и все внесены:
ложное обещание в KDoc («в лог только машинные поля», а `error.toString()`
уносил туда и reason phrase сервера, и сообщение исключения — теперь
классификация собирается вручную), клиентский пропуск без сессии,
неотличимый в логе от настоящего `401` (стал `ANALYTICS_NO_SESSION`),
мёртвый `NoopAnalyticsTracker`, `lat`/`lng` как вывод из правила про
query-параметры вместо честного допущения, «во всех семи вертикалях» при
девяти в приложении и **отсутствие теста на саму область видимости** —
подмена её на `viewModelScope` не покраснела бы нигде, теперь есть тест,
который берёт трекер из `AnalyticsModule` и требует доставки (проверен
подменой области на отменённую — краснеет).

**Не сделано / риски:** `lat`/`lng` в теле не заполняются в расчёте на то, что
бэкенд возьмёт гео события из `X-Geo-*`, — **это допущение**: правило «не
дублировать координаты» в контракте написано про query-параметры. Не возьмёт —
события приедут без координат, молча. Тело успешного ответа под токеном не
проверено —
`CONTRACT_REFRESH_TOKEN` по-прежнему не задан, поэтому статус ручки в
контракте «✅ частично». **Содержимое `metadata` не сверено**: схема объявляет
его свободным объектом, но что бэкенд с ним делает, из схемы не следует —
если он окажется строгим, `BOOK`/`ORDER` начнут отказывать `400`, и различать
вертикали придётся иначе. Двух вертикалей из девяти в разметке нет: заказ услуги
мастера (`freelancers/{id}/orders`) — у ручки нет `placeId`, а он обязателен;
аптека — своего оформления заказа у неё пока нет вовсе. Доля потерянных без
сети событий неизвестна и не измеряется (для этого понадобилась бы та самая
очередь, от которой ADR отказался).

Второе ревью (issue #228) и третье (перед approve) блокеров не нашли. Одна
правка поведения: перенос записи из «Моих записей» приходит на экран с пустым
`placeId` (`Appointment.placeId` nullable), и `BOOK` на него репозиторий
отбрасывал бы молча — теперь заведение берётся из ответа сервера, тест есть.
Остальное — правки текста: KDoc `AnalyticsModule` больше не называет `SupervisorJob`
«обязательным» (до Job ничего, кроме `Error`, не доходит — его ловит
`runCatchingCancellable`) и проговаривает, что провал refresh на фоновом
событии закрывает сессию так же, как на запросе человека — принято осознанно;
`BOOK` на талон со статусом `PENDING` — намеренно, KDoc и комментарий это
теперь говорят; комментарий к `lat`/`lng` в тесте перестал ссылаться на правило
про query-параметры. Инвариант «событие только через `AnalyticsEvents`» лёг в
`.claude/rules/network.md`. Открытыми в #228 остались: отдельный клиент для
аналитики без authenticator'а (решение — за командой), тест области на
`Dispatchers.IO` с опросом по стенным часам и слои `core` → `data`.

---

## Карта грузит маркеры областью, а не радиусом (issue #168)

Карта брала маркеры из `CatalogRepository`, то есть из `GET places/nearby` —
круга вокруг человека. Сдвиг и зум камеры не догружали ничего: `CameraMoved`
только запоминал позицию в состоянии. Заведение на другом краю видимой области
на карте отсутствовало, хотя `GET places/map-bounds` у стенда есть и не звался
из приложения вовсе.

**Контракт снят с живого стенда** (2026-09-10, `/v3/api-docs` + два запроса) и
записан в `docs/API-CONTRACT.md`: `minLat`/`minLng`/`maxLat`/`maxLng`
обязательны, `category` — нет, ответ тот же `ApiResponse<List<PlaceSummary>>`,
что у `nearby`, вместе с `distanceMeters` (сервер считает его по заголовкам
`X-Geo-*`, а не по прямоугольнику). Пагинации нет.

Сделано:

- `CatalogApi.mapBounds` + `CatalogRepository.placesInBounds`. **Без фоллбэка
  на Room**: кэш — срез радиуса вокруг человека, и подставить его вместо
  области значит нарисовать маркеры не там, куда человек смотрит. Ответ не
  фильтруется и не сортируется повторно — маркеры не список, порядок им ни о
  чём не говорит, а локальный фильтр оставил бы дыры там, где заведение видно
  глазом.
- **Область приходит от полотна** (`MapBounds` из `map.visibleRegion`), а не
  считается по центру и зуму: камера не знает ни размера полотна, ни его
  пропорций, и посчитанный по ней прямоугольник расходился бы с кадром как раз
  по краям — там, где маркеров и не хватало. Крайние значения берутся по всем
  четырём углам: жест поворота у SDK включён, и у повёрнутого кадра «левый
  нижний» угол перестаёт быть самым западным.
- Дебаунс 500 мс на `MapEvent.VisibleBoundsChanged` через `collectLatest` +
  `delay`, а не `debounce`: новая область отменяет не только ожидание, но и уже
  начатый запрос по старой.
- Область запрашивается с запасом в четверть кадра, загруженное запоминается:
  приближение и короткий сдвиг ничего не запрашивают.
- `nearby` остался первым кадром — до первого ответа полотна области нет, а на
  сборке без `MAPKIT_API_KEY` её не будет вовсе, и радиус остаётся
  единственным источником. Ответ первого кадра, приехавший после области,
  отменяется, иначе он затирал бы её собой.

**Грабли:**

- **Своё же движение камеры полотно наверх не отдаёт** (защита от кругового
  подтверждения позиции), и повесить область на `onCameraChanged` значило бы
  оставить без маркеров кадр после кнопок масштаба и полёта к «моему
  местоположению». Поэтому область — отдельный коллбэк, который срабатывает
  всегда.
- **Вывернутый прямоугольник стенд отдаёт как `200` с пустым `data`**, а не
  ошибкой: пустая карта читалась бы как «рядом ничего нет». Область
  проверяется до запроса (`MapBounds.isValid`).
- **Неудачная догрузка не снимает показанные маркеры** — снять их из-за
  провалившегося запроса значит наказать за жест. Ошибка во весь экран
  остаётся только когда показывать больше нечего.
- **Судьбу `MarkerClusterer` решать было нечего** — он удалён ещё в issue #65
  (`docs/adr/0002-yandex-mapkit.md`), кластеризует сам MapKit. Задача
  ссылалась на устаревшую запись в `docs/UI-INVENTORY.md` — пока ветка жила, в
  `main` приехал свежий снимок инвентаризации (#36), где этой записи уже нет.
- **`FakeCatalogRepository` до сих пор жил без гейтов** — гонку первого кадра с
  областью проверять было нечем; добавлены `pagesGate` и `boundsGate`.
- **Ревью диффа субагентом нашло три вещи, которые я не додумал** (и это
  четвёртый раз, когда ревью в этом проекте ловит настоящий баг):
  - **первый кадр нельзя отменять** — сначала загрузка по области убивала
    `nearby`, а именно его `fit` приводит камеру в город человека. При
    латентности больше дебаунса житель Самарканда получил бы ташкентские
    маркеры вокруг дефолтного центра, и `locate(silent = true)` его бы не
    спас: там `silent && markers.isNotEmpty() -> camera`. Теперь область
    **дожидается** первого кадра (`join`), а не отменяет его: приведя камеру,
    `fit` сообщит новый кадр, и `collectLatest` отменит ожидание сам;
  - **пропуск загруженной области нельзя делать до дебаунса**: кадр,
    вернувшийся внутрь загруженного быстрее 500 мс («дёрнул и вернул»),
    отсеивался — и потому не отменял запрос по кадру, из которого человек уже
    ушёл. На карту приезжали маркеры чужого кадра, и починить это могло только
    следующее движение камеры. Проверка переехала внутрь `loadVisible`, после
    дебаунса;
  - **вырожденная область считалась годной**: `visibleRegion`, прочитанный до
    того, как окно карты промерено, отдаёт все четыре угла в одной точке —
    запрос уходил бы в Гвинейский залив, а пустой ответ читался бы как «рядом
    ничего нет». `isValid` теперь требует ненулевого разброса
    (`MIN_SPAN_DEGREES`, как у `MapCameraFit`).

**Не сделано / риски:**

- **На устройстве не проверено**: эмулятора в CI нет, а `visibleRegion` — это
  как раз то, что появляется только на живой карте. Проверено, что область
  доезжает до запроса и что по ней приходят маркеры (JVM-тесты
  `MapViewModelTest`, `MapBoundsTest`, `CatalogRepositoryTest`), но
  соответствие `visibleRegion` тому, что видит глаз, — за MapKit.
- **Прогресс догрузки на экране не показывается.** Пока маркеры едут, кадр
  выглядит законченным: старые маркеры на месте, новых ещё нет. Отдельной
  плашки «догружаю» не добавлял — это новый элемент поверх карты, а задача про
  источник данных.
- **Меридиан 180° не поддержан** — та же оговорка, что у `MapCameraFit.fit`.
- **Отзум «на весь мир» запоминается как загруженная область**, и дальше любой
  кадр в неё вкладывается — приближение к району не догрузит ничего до
  «Повторить». Сейчас это безвредно: `map-bounds` отдаёт всё найденное одним
  списком, без лимита и без признака «есть ещё». Как только сервер начнёт
  обрезать выдачу, инвалидация по одному прямоугольнику станет неверной — но
  чинить это на клиенте, не зная, обрезан ли ответ, значит гадать. Указало
  ревью; отдельная задача — попросить у бэкенда признак усечения.
- **Фильтры discovery на карте по-прежнему не применяются**: `map-bounds`
  зовётся с пустыми `DiscoveryFilters`, как раньше `nearby`. `category` в
  запросе поддержан, но выбирать категорию на карте пока негде.

---

## Деньги: бэкенд считает в тийинах, приложение показывало в сто раз больше (issue #149, 2026-09-10)

**Что было.** Целые денежные поля бэкенда (`price`, `totalAmount`, `balance`,
`monthlyPrice`, …) приложение читало как сумы. Единица в схеме описана не
была, и вопрос повторялся трижды — в «Еде» (#9), кошельке (#62) и «Моих
активностях» (#73). Кошелёк и подписки выводили делитель из пары «целое +
дробный близнец `*Som`» (`WalletAmounts.scaleOf`) и потому показывали верно;
остальные девять вертикалей делителя не имели и рисовали «5 000 000 so'm» за
стрижку.

**Ответ бэкенда.** В `info.description` живого `/v3/api-docs` появилось:
все целые денежные поля — **тийины**, делить на сто, дробные значения и суммы
в сумах не принимаются. Проверяется одной командой:
`curl -sk https://189-74-96-232.nip.io/v3/api-docs | jq -r '.info.description'`.

**Что сделано.**

- `core/format/Money.kt` — одно правило на проект: `tiyinToSom` (половина
  вверх, целочисленно, `null` остаётся `null`) и `somToTiyin` для тел запросов.
  Домен, экраны и Room по-прежнему в целых сумах; пересчёт — только в мапперах
  DTO → домен. Ни один экран и ViewModel единицу бэкенда не знает.
- Переведены мапперы всех вертикалей: еда (цены меню и модификаторов, суммы
  заказа), аптека, акции (только денежные поля, проценты не трогались), «Мои
  активности», бронь и записи, больницы, игровые зоны, кино, одежда (каталог,
  варианты, серверная корзина), мастера.
- **Кошелёк и подписки**: вывод делителя из пары убран, `WalletAmounts` и
  `SubscriptionAmounts` удалены, `Wallet.amountScale`/`TopUpState.scale`
  исчезли, `topUp(amountSum, provider)` без делителя. Минимум пополнения —
  константа `WalletTopUp.MIN_AMOUNT_SUM = 1 000` сум (серверные `100000`
  тийинов). Дробные близнецы `*Som` игнорируются: тест проверяет, что даже
  лгущий `balanceSom` единицу не меняет.
- **Room v3 → v4**: в кэше корзины и заказов лежали тийины под именем сумов,
  миграция делит `priceSum`/`deliverySum`/`totalSum` на сто. Без неё
  перенесённая корзина показывала бы цены в сто раз больше только что
  загруженного меню и складывалась бы с ним. Схема столбцов не меняется.
- `docs/API-CONTRACT.md`: правило вынесено в «Общее для всех запросов», снят
  «открытый вопрос» у BookingApi, у WalletApi описан `TopUpRequest.amount`.

**Тесты.** Новый `MoneyTest` (округление, знак, `null`, переполнение).
Во всех тестах мапперов входные суммы стали тийинами, ожидаемые — сумами, и в
каждом есть явный кейс «5 000 000 → 50 000». `WalletAmountsTest` заменён
`WalletEnumsTest` (перечисления остались), тесты делителя у подписок сняты.
Миграциям добавлен кейс v3 → v4 и проверки сумм в кейсах v1/v2.

**Грабли.** Единицу нельзя было вывести «по здравому смыслу»: `WalletAmounts`
из отсутствия дробного близнеца делал вывод «тийины», а `BarberService` из
того же отсутствия — «сумы». Пара `balance` + `balanceSom` была полезной
самопроверкой, но она же породила механизм с делителем, который тянулся через
домен и UI. Теперь единица — факт контракта, а не догадка кода.

## CI: пуш агента в открытый PR вставал в `action_required` (issue #225, 2026-09-10)

**Симптом.** Прогон `ci.yml` от пуша агента в ветку уже открытого PR
заводился и сразу замирал: `conclusion=action_required`, ноль job'ов, лога
нет. После закрытия PR такие прогоны переписываются в `failure` — искать по
`action_required` найдёшь только висящие на открытых PR. Проходили только
прогоны от открытия PR (`claude[bot]`), после ручного Approve (`attempt=2`,
`triggering_actor` — `jack5505` или `kuvandikov`) и после close + reopen PR
(обход из PR #201). С 2026-08-31 от пушей агента было 29 прогонов: 21
застрял насовсем, 8 ушли в работу только после ручного Approve.

**Причина — не настройки репо и не событие, а токен пуша.** `PushEvent` в
Events API на ветках `claude/*` показывает `actor=github-actions[bot]`, то есть
git пушил `GITHUB_TOKEN`, а не токен приложения. Цепочка:

- `actions/checkout@v6` (с `persist-credentials` по умолчанию) пишет
  `http.https://github.com/.extraheader` с `GITHUB_TOKEN` в отдельный файл в
  `RUNNER_TEMP` и подключает его через `includeIf.gitdir:<repo>/.git.path`;
- `claude-code-action@v1` в `replaceCheckoutCredentials` снимает заголовок из
  локального конфига и из файлов `include.path` — но не `includeIf.*.path`.
  В логе прогона: «No existing authentication headers to remove». Это
  известный баг: upstream anthropics/claude-code-action#1721, фикс — PR #1722,
  на 2026-09-10 не влит;
- токен приложения action прописывает в URL remote, но до него дело не
  доходит: `extraheader` git шлёт в каждом запросе, а логин и пароль из URL —
  только в ответ на 401. Сервер принимает заголовок, 401 не бывает.

Гипотеза «дело в событии, а не в токене» из PR #201 опиралась на то, что
`GH_TOKEN` и URL remote были токеном приложения, — это правда, но пуш шёл не
ими. Там же и прямое подтверждение: пуш «явно токеном приложения» всё равно
дал прогон `34390775224` от `github-actions[bot]`. Настройка approval в репо
уже минимальная (`first_time_contributors_new_to_github`), ослаблять нечего.

**Починка.** `persist-credentials: false` у основного checkout в `claude.yml`,
`claude-dev.yml` и `contract-check.yml`: сохранённого `GITHUB_TOKEN` нет,
git авторизуется только токеном приложения, пуш идёт от `claude[bot]`.
Никакой шаг этих workflow, кроме самого action, git-авторизацией не
пользуется.

**Держится на публичности репо.** В режиме `@claude` action делает fetch
ветки PR (`setupBranch`) до подмены токена — с этой настройкой анонимно.
Станет репо приватным — `claude.yml` упадёт на `could not read Username`
(upstream #1236). Тогда вернуть сохранение токена и ждать #1722 либо
лечить иначе. Когда #1722 вольют, настройку можно оставить: вреда от неё нет.

**Как проверить.** `claude.yml` на `issue_comment` берётся из ветки по
умолчанию, так что фикс заработает только после мержа в `main` — сам PR
этого не покажет. Дальше — первый же `@claude` в открытом PR, который пушит
коммит: у `PushEvent` в Events API `actor=claude[bot]`, прогон `ci.yml`
стартует без Approve. Признак старой беды — ноль job'ов при
`action_required` (или `failure` после закрытия PR).

---

## Этап: «не авторизован» посреди приложения (issue #138, #239, ветка claude/issue-138-*)

По скриншоту: экран «Hamyon» показывает `Kirish uchun autentifikatsiya talab
qilinadi` дважды — и вместо баланса, и вместо истории, — с кнопкой «Qayta
urinish», которая ничего не могла изменить. Текст чужой, это ответ сервера на
401 (issue #34 довозит его до экрана); проблема не в кошельке — так вёл себя
любой экран под токеном. Issue #239 — тот же экран через несколько часов
простоя, и там же сказано, чего ждали: окна входа.

Почему «через несколько часов»: access-токен живёт 15 минут, refresh — 30
дней (`application.yml` бэкенда), так что после простоя первый же запрос
идёт через refresh. В `main` его провал **по любой причине** — в том числе
обрыв сети, пока телефон просыпается, — стирал сессию
(`runCatching { … }.getOrNull()`), а приложение оставалось внутри.

Дыра была в том, что **смерть сессии никто не обрабатывал**. `TokenAuthenticator`
стирал мёртвую пару токенов и возвращал 401 наверх, а приложение продолжало
жить в основном графе: ни один экран не знал, что ходить в сеть больше нечем.
Явный выход (issue #61) это учитывал — сбрасывал флаг онбординга и уводил на
welcome, — а вот истёкшая сессия оставляла человека внутри приложения, целиком
состоящего из красных плашек. `RootViewModel` при следующем запуске пускал его
туда же: он смотрел только на `onboardingCompleted`.

- **`SessionExpiry`** (`data/network`) — одно событие «сессия кончилась не по
  воле пользователя»: отправляет `TokenAuthenticator`, слушает корень.
  `MutableSharedFlow` без `replay`: новый подписчик (пересоздание activity)
  иначе получил бы прошлую смерть сессии и выкинул на вход человека, который
  уже вошёл заново. Явный выход событие не отправляет — экран профиля уводит
  на вход сам, и второе сообщение «сессия истекла» там было бы неправдой.
- **`MahallaApp`** на событие уходит на `WelcomeRoute` и показывает
  `error_unauthorized` («Sessiya tugadi, qaytadan kiring») в снекбаре — экран
  входа, возникший сам собой, иначе читается как сброс приложения. Стек
  чистится целиком (`popUpTo(navController.graph.id)`), а не до `MainGraph`:
  без сессии в нём не осталось работающих экранов, включая детали, открытые
  из уведомления мимо основного графа. Событие игнорируется только на экранах
  **до** входа: адрес сервера, обновление, welcome, телефон, Telegram, код и
  PIN — там сессии ещё нет, а welcome посреди ввода кода стёр бы шаг. Шаги
  после PIN (биометрия, гео) и анкеты последнего шага исключением не считаются:
  сессия там уже есть, и незаполненная анкета потеряется — отправить её всё
  равно нечем. Снекбар-хост появился в корневом `Scaffold` — до этого
  компонент эпика 2.3 в проде не использовал никто.
- **`RootViewModel`**: `withOnboarding = !onboardingCompleted || !authorized`.
  Пройденный онбординг сам по себе в приложение больше не пускает — сессия
  могла умереть между запусками (refresh не прошёл, устройство отозвали в
  «моих устройствах»). Это же страхует потерянное событие.
- **Заодно: refresh больше не убивает сессию при обрыве связи.** Было
  `runCatching { … }.getOrNull()` — то есть таймаут, 5xx и битый JSON стирали
  живые токены наравне с отказом сервера. Теперь ответ разбирается через
  `apiCall`, и сессию заканчивает **только 401**. Это сверено с кодом
  бэкенда (jack5505/mahalla, `BankAuthService.refreshToken`, `JwtService`,
  `GlobalExceptionHandler`): каждый отказ «сессии больше нет» там —
  `UnauthorizedException` (`TOKEN_EXPIRED`, `TOKEN_INVALID`, `TOKEN_HIJACK`).
  Остальное сессию не трогает: 403 там — это `GEO_*` или блокировка, 400 —
  форма запроса (новое обязательное поле разлогинило бы всех), 429, 404, 5xx,
  обрыв и таймаут — «спросить не удалось», а 2xx без токенов или с `success:
  false` бэкенд на refresh не отдаёт вовсе. Первая версия PR заканчивала
  сессию и на 400/422/403/`Business` — ревью справедливо назвало это
  догадкой. Сбор устройства и координат переехал внутрь `apiCall`: раньше его
  сбой выходил исключением из `Authenticator`, а OkHttp пробрасывает такое
  в поток диспетчера — то есть ронял приложение.
- **Обрыв и таймаут refresh экран называет своими словами.** Если refresh
  не дошёл до сервера, `authenticate` бросает `IOException` /
  `SocketTimeoutException` (тело 401 закрывает сам), и `apiCall` отдаёт
  экрану «нет сети» / «таймаут». Иначе при живой сессии экран показывал бы
  исходный 401 — тот самый текст из issue #239.
- **Экран «до входа» проверяется вместе с экраном под ним.** Адрес бэкенда
  открывается ещё и из профиля; отброшенное там событие не повторилось бы
  (следующие 401 приходят уже без сессии и молчат), и человек, вернувшись
  «назад», застрял бы без токена до холодного старта. `currentBackStack`
  помечен `@RestrictTo`, поэтому смотрим `previousBackStackEntry` — с
  экранов до входа вглубь уходят только на такие же.

Проверено: `./gradlew testDebugUnitTest` — **2034 теста в 189 классах, 0 падений**;
`assembleDebug` — BUILD SUCCESSFUL; `lintDebug` — BUILD SUCCESSFUL.
Новые тесты: `SessionExpiryEffectTest` (compose-тест под Robolectric на
настоящем `NavHost`: экран основного графа, деталь вне него, шаг биометрии и
адрес бэкенда, открытый из профиля, уходят на вход с пустым стеком; welcome,
телефон, Telegram, PIN, адрес бэкенда до входа и обновление не трогаются;
второе событие не даёт ни второго welcome, ни второго сообщения),
`NetworkStackTest` (401 на refresh — в том числе с телом бэкенда
`TOKEN_HIJACK` — стирает сессию **и** сообщает об этом; 403 `GEO_*`, 400,
429, 503, 200 без токенов, `success: false` при 200 и сбой сбора координат
сессию сохраняют и молчат; обрыв и таймаут refresh доходят до экрана как
`NoConnection` / `Timeout`; удачный refresh и 401 без сессии молчат),
`RootViewModelTest` (пройденный онбординг без сессии всё равно начинается с
входа, с сессией — с основного графа, корень отдаёт событие, а не пустой
flow). **Каждый новый тест проверен обратным прогоном**: с убранной правкой
краснеет — в том числе при подмене `popUpTo(graph.id)` на `popUpTo(MainGraph)`,
при прежнем разборе отказа (400/422/403/`Business`/2xx без токенов), без
`IOException` из `authenticate`, без проверки экрана под текущим и без PIN и
Telegram в списке экранов до входа.

**Не сделано / риски:**

- **Почему refresh не прошёл именно у этого пользователя, не установлено.**
  Логов нет, на скриншоте только текст сервера (`error.unauthorized` —
  ответ Spring Security на запрос без живого токена, он одинаков для всех
  причин), а `SENTRY_DSN` до сих пор не задан. Кандидаты по коду: обрыв или
  таймаут refresh (в `main` стирал сессию), `TOKEN_HIJACK` при смене
  `osVersion` после обновления Android, потерянный ответ ротации. В
  debug-сборке ответ `auth/refresh` виден в Chucker — `@RefreshClient`
  проходит через инспектор. Закрыта та дыра, которую видно по коду: мёртвая
  сессия больше не оставляет человека внутри приложения.
- **Один путь 401 остался необработанным намеренно**: refresh **удался**, а
  повторный запрос с новым токеном снова получил 401 — `attemptCount >=
  MAX_ATTEMPTS`, и `authenticate` выходит, ничего не стирая. Сессию здесь не
  заканчиваем: 401 вместо 403 на одной ручке (роль, подписка) выкидывал бы из
  приложения человека, у которого всё остальное работает. Значит если бэкенд
  отказывает свежему токену, экран по-прежнему покажет ошибку сервера — но
  вход в этом случае и не помог бы. Оформлено issue #197. По коду бэкенда
  этот путь почти закрыт: фильтр access-токена без состояния (подпись и
  срок), так что свежий токен отвергнет только отдельная ручка.
- **Неразбираемый или «пустой» 2xx на refresh сессию не заканчивает** —
  битое тело, ответ без токенов, `success: false` при 200 (раньше
  `runCatching{}.getOrNull()` стирал её и здесь). Ставка на то, что это
  подменённый ответ публичного Wi-Fi, а не отказ бэкенда. Если же так
  сломается контракт (а он ломался четырежды за месяц), приложение останется
  в состоянии «везде 401» до ручного выхода в профиле. Оформлено issue #198.
- **Сессию на 403 блокировки не заканчиваем** — заблокированный аккаунт
  увидит на экранах текст сервера о блокировке, а не экран входа: вход заново
  ему всё равно не поможет.
- **На устройстве не проверено** (эмулятора в CI нет): переход на вход и
  выбор экранов-исключений закрыты compose-тестом под Robolectric
  (`SessionExpiryEffectTest` на настоящем `NavHost`), но сам снекбар — его
  вид, длительность и то, не закрывает ли он кнопку на welcome, — проверялся
  только чтением кода.
- **Сообщение можно не увидеть.** Событие обрабатывается и когда приложение в
  фоне: снекбар отыграет свои секунды в невидимом окне, и человек вернётся на
  экран входа без объяснений. `Long` вместо `Short` — компромисс;
  `Indefinite` рискует закрыть кнопку входа, а `repeatOnLifecycle` потерял бы
  событие совсем (у `SessionExpiry` нет replay).
- **Экраны под токеном по-прежнему показывают «повторить» на 401** — при
  смерти сессии это доля секунды до перехода на вход. Но на 5xx и 429 от
  refresh сессия жива, и экран показывает исходный 401 с текстом сервера,
  пока повтор не удастся: бросить здесь `IOException`, как на обрыве,
  значило бы соврать «нет сети». Отдельного состояния «сервер не продлил
  сессию» у экранов нет.
- **Истёкшая сессия стоит полной регистрации заново** — телефон, платный SMS,
  PIN, биометрия, гео, анкета: `resumeOnboardingAtPin` здесь не помогает по
  определению (`atPin = withOnboarding && authorized`, а сессии нет). Это то
  же, что происходит после явного выхода (issue #61), поэтому оставлено как
  есть, но решение стоит записать: короткий путь «вход по PIN на знакомом
  устройстве» бэкенд поддерживает (`auth/pin-login`), и он был бы уместнее.
- `ProfileViewModel.logout()` теперь сбрасывает флаг онбординга «на всякий
  случай»: после правки `resolveStart` стартовый экран и так решает по
  наличию сессии. Оставлено — вреда нет, а флаг честно означает «онбординг не
  пройден».
## Агент: меньше истории в контексте, Sonnet по умолчанию (2026-09-10)

Лимит подписки стал кончаться заметно быстрее, чем в начале проекта. Причины
по убыванию веса:

- **`CHANGELOG.md` дорос до 6552 строк / 616 КБ**, и таблица в `AGENTS.md`
  отправляла агента читать его за «граблями прошлых этапов». Первые 46
  разделов (issue #5 … #98) уехали в
  `docs/changelog/2026-08-25--2026-09-06.md` без правок текста (сверено
  `diff` со старой версией — идентично), здесь осталось ~1200 строк / 111 КБ.
  В шапке и в `AGENTS.md` — «grep по номеру issue, целиком не читать».
- **Везде был Opus.** `claude.yml` теперь берёт `vars.CLAUDE_MODEL`, по
  умолчанию `claude-sonnet-5`; в `claude-dev.yml` Sonnet — первый вариант
  выбора, Opus остался в списке.
- Сам проект вырос (647 `.kt`, ~96 тыс. строк), а каждый прогон в CI
  начинается с нуля — это не лечится, только точными задачами: файл и место
  в тексте issue экономят агенту разведку.

Проверено и **не** трогали: вывод Gradle. Зелёный `testDebugUnitTest --rerun`
печатает 47 строк / 2 КБ, `compileDebugKotlin --rerun` — 22 строки и ноль
предупреждений; `-q` сэкономил бы копейки и спрятал бы имена упавших тестов.

Граница архива выбрана так, чтобы не добавить конфликтов открытым PR: они
дописывают свой раздел около строки 5591 своей базы, а вырез кончается на
5363. Пробное слияние (`git merge-tree`) открытых #156, #158, #159, #161, #164,
#173, #195, #196 и #240 с `main` до и после
разреза — те же конфликтующие файлы, те же места. Исключение — старый #78: его
раздел вставлен около строки 5064, внутри выреза, и конфликт в `CHANGELOG.md`
вырос с 400 до ~5150 строк. Разрешать так: взять версию `main` и дописать
раздел #78 в конец файла.


## Эпик 8, задача 8.3 — оплата из кошелька (issue #12, 2026-09-08)

8.1 (баланс и история, issue #62) и 8.2 (пополнение, issue #93) уже были в
`main` — этот заход закрывает эпик третьей задачей.

**Ручки «списать с кошелька» у бэкенда нет.** Кошелёк — это
`paymentMethod = WALLET` внутри запроса вертикали (`POST food/orders` и
далее), деньги списывает сервер при создании заказа. Поэтому 8.3 на клиенте —
не свой эндпоинт, а то, что происходит **вокруг** этого запроса и одинаково
для всех вертикалей: `feature/wallet/ui/pay/WalletPaymentFlow` (проверить
«доступно», спросить PIN или биометрию, отправить один запрос, разобрать
отказ) + общая шторка `PaymentConfirmSheet`. Подключено к оформлению заказа
«Еды»; остальные checkout'ы (одежда, кино, запись, подписка) — тем же
`WalletPaymentFlowFactory.create`, отдельными задачами.

**Идемпотентность держит клиент.** Ключ создаётся один раз на оплату и
переживает повторы: «повторить» после таймаута отправляет тот же ключ, потому
что соединение могло оборваться уже после списания. Второй запрос при
неответившем первом не уходит вовсе, и после успеха — тоже. Заголовок
`Idempotency-Key` уходит с `POST food/orders`, но **серверную поддержку
бэкенд не подтверждал** (issue заведён, в `docs/API-CONTRACT.md` помечено).
До этого от двойного оформления защищал только флаг `isSubmitting` в
состоянии экрана — то есть ничего после отказа и повтора.

**Грабли:**

- **Подтверждение локальное, и это надо было решить, а не забыть.** Серверного
  подтверждения платежа в контракте нет (`auth/session/check`, `pin-resume` —
  issue #102), так что PIN здесь защищает от чужих рук на разблокированном
  телефоне, а не служит вторым фактором для бэкенда. Проверка PIN — тот же
  `PinStorage` (PBKDF2 + Keystore), поэтому у неё свой шаг `Checking`: на
  слабых устройствах это сотни миллисекунд с запертыми ячейками.
- **Подтверждать может быть нечем.** PIN не настроен и биометрии нет — оплата
  идёт сразу, как было до 8.3. Иначе человек запирался бы в шторке, из которой
  нет выхода. Закрытый промпт биометрии — не отказ оплаты, а переход на PIN.
- **Коды отказа кошелька не сверены** — набор `INSUFFICIENT_FUNDS`,
  `WALLET_BLOCKED` и синонимов написан по догадке, стенд на 2026-09-08 отвечал
  `502`. Незнакомый код показывается текстом сервера, а не подменяется
  «пополните кошелёк».
- **Отказ оплаты кошельком больше не уходит под форму.** Он остаётся в шторке
  рядом с суммой и кнопкой «повторить»; `submitError` в состоянии checkout'а
  теперь только про наличные. Старый тест на это и покраснел — переписан на
  два, по способу оплаты.
- **`showBiometricPrompt` был приватным в `BiometricScreen`** — вынесен в
  `core/ui/biometric/BiometricPrompts.kt`. Копия в шторке оплаты означала бы
  две трактовки отмены и два набора аутентификаторов.

**Проверено:** `testDebugUnitTest` (1792 теста, зелёные; 16 из них —
`WalletPaymentFlowTest`), `assembleDebug`, `lintDebug`. Тесты проверены на
обратный прогон: повтор с новым ключом и снятая проверка блокировки кошелька
краснеют.

**Не сделано / риски:** на устройстве не проверено (эмулятора в CI нет) —
системный промпт биометрии и `ModalBottomSheet` поверх формы проверялись
только превью и JVM-тестами. Оплата подписок и билетов пока идёт прежним
путём, без подтверждения.
## Язык и роль: у кого какое значение главное (issue #237, 2026-09-10)

**Вопрос.** `MeResponse` отдаёт `language` и `role`, а у приложения по обоим
уже есть своё локальное значение (`AppLanguage` в настройках; `roleId`,
issue #84). Кто из двух главный — было неизвестно, и брать серверные «заодно»
с профилем было нельзя.

**Ответ снят чтением живого `/v3/api-docs` стенда** (2026-09-10, 255 схем), и
он разный для двух полей:

- **`language` — выбора нет.** Слово `language` встречается во всей схеме
  **один раз**, в `MeResponse`. Ни одно тело запроса его не принимает
  (`UpdateMeRequest` — только `fullName` и `avatarUrl`). Про `Accept-Language`
  схема молчит, но по ней этого и не узнать: springdoc не показывает заголовки
  из фильтров — в схеме нет ни `X-Geo-*`, ни `Authorization`; проверять
  пробой, issue #242. В тела запросов язык не положить, значит серверное
  значение недоступно на запись, а читать его — значит молча сбрасывать
  выбор человека. Не разбираем. `AppLanguage` к тому же богаче: у него есть `SYSTEM`, у сервера
  только `UZ`/`RU`.
- **`role` — это другое значение.** Серверных ролей четырнадцать
  (`FOOD_OWNER`, `FREELANCER`, `ADMIN`, …), меняет их только админ через
  `PUT admin/users/{id}/role`. Локальных две, и они про анкету. Приводить к
  одному источнику нечего — надо было развести имена.

Решение целиком — `docs/adr/0007-yazyk-i-rol-istochnik-istiny.md`, контракт
обеих ручек `users/me` — в `docs/API-CONTRACT.md` (раздел помечен ✅).

**Что починилось по дороге.** Роль и статусы бэкенд отдаёт в блоке `user`
ответа на вход, `UserDto` их разбирал — и все три молча выбрасывались.
Следствие: «Мои заведения» показывались только по локальному переключателю
анкеты, и настоящий владелец кафе, который анкету не заполнял, своего
заведения в приложении не находил вовсе. Теперь `UserProfile` хранит
`serverRole`, `verificationStatus` и `accountStatus` (строками, разбор — в
домене: `ServerRole`, `VerificationStatus`, `AccountStatus`), строка «Мои
заведения» показывается по анкете **или** по серверной роли
(`ProfileState.showMyPlaces`), а блокировка и непройденная проверка видны в
шапке профиля словами.

**Судьба остальных полей.** `telegramLinked` не разбираем — привязки в
приложении нет, строка «привязан» ни на что не отвечает; `lastLoginAt` не
разбираем — `auth/sessions` уже показывает время активности по каждому
устройству.

**Осталось бэкенду** (issue заведена в `jack5505/mahalla-android`, дальше — в
бэкенд-репо): принимать `language` в `UpdateMeRequest` либо читать
`Accept-Language`, иначе `error.message`, SMS и push приходят на языке, который
человек не выбирал, и починить это на клиенте нечем.

**Грабли.** «Два источника у одного значения» оказалось верным только для
языка; для роли — одно слово на двух разных значениях, и это дороже: путаница
не расходится в данных, а тихо прячет от человека его же заведение. Прежде
чем сводить два поля в одно, стоит проверить, одно ли это поле.

## Сотрудники заведения: `places/{placeId}/staff` (issue #189, 2026-09-11)

Первый кусок эпика #16 («Бизнес-панель»), не зависящий от остальных: до этой
задачи в приложении не было ни строки `place-staff-controller` — ни одной из
четырёх ручек.

**Контракт снят со стенда** (`/v3/api-docs`, 2026-09-11), а не выдуман: риск
из issue был именно в наборе ролей. Он закрытый — `STAFF`/`MANAGER`/`OWNER`,
и это то же самое перечисление, что уже приезжает в `Mine.role` у «моих
заведений» (issue #94) — заводить второй домен-тип под тот же смысл не стали,
переиспользован `PlaceStaffRole`. Схемы `PlaceStaffResponse`, `AddRequest`,
`PlaceStaffChangeRoleRequest` в `/v3/api-docs` встречаются по одному разу,
коллизии springdoc (см. общий раздел `docs/API-CONTRACT.md`) здесь нет.

**Ключ записи — `userId`, не `id` схемы.** Обе точечные ручки
(`PUT`/`DELETE .../staff/{staffUserId}`) адресуют сотрудника по `userId`, а
не по `id` записи `PlaceStaffResponse` — доменная модель `PlaceStaffMember`
хранит только то, чем реально пользуется приложение.

**Добавление — по ID, введённому вручную.** Контракт не даёт способа найти
пользователя по телефону (`GET users` с поиском в схеме нет), поэтому форма
принимает UUID текстом; формат проверяется на клиенте до отправки — иначе
опечатка каждый раз доезжала бы до ответа `400`.

**Экран** — `PlaceStaffScreen` (`feature/role/ui/staff`): список, смена роли
чипами прямо в карточке, удаление с подтверждением (`MahallaDialog`,
необратимо). Открывается только с «Моих заведений» и только владельцу
(`MyPlace.canManageStaff`) — менеджеру и сотруднику бэкенд эти действия не
даст, кнопка для них не рисуется вовсе, тем же приёмом, что и переключатель
доступности (issue #94).

**Список после успеха перечитывается у сервера**, а не правится на месте —
как и отзыв сессии устройства (issue #61): провал не должен молча
вычеркнуть или дорисовать сотрудника, которого бэкенд на самом деле не тронул.

Тесты — `PlaceStaffRepositoryTest` (MockWebServer), `PlaceStaffViewModelTest`
(Robolectric — `SavedStateHandle.toRoute()`), плюс новые случаи в
`MyPlaceTest` и `MyPlacesViewModelTest` на `canManageStaff`.

**Не сделано.** Показ `geoExempt` в самом экране — поле разобрано и хранится
в домене, но UI его не выводит: issue не просил, а лишняя строка в карточке
без задачи, которую она решает, — это как раз то, чего просили не делать.

## issue #243 — анкета прежнего человека после входа под другим номером (2026-09-10)

Найдено ревью PR #245. Выход и вход под другим номером чистили профиль, сессию
и PIN, а анкета (роль и адрес доставки, issue #84) лежит в настройках, и её
не трогал никто. Новому человеку показывалась строка «Мои заведения»
прежнего, а в оформление заказа подставлялся **чужой адрес доставки**.

**Решение — анкета помнит `id` владельца** (`FormOwnership`, ключ
`settings_form_owner_id`). Каждый успешный вход (`AuthRepository.signIn`:
SMS, оба PIN-шага, Telegram) сверяет `user.id`: тот же — анкета остаётся,
другой, пустой или владелец неизвестен — роль и адрес стираются одной
транзакцией DataStore. Выход анкету не трогает, город не стирается никогда.
Почему не «стирать при выходе» — `docs/adr/0008-anketa-pomnit-vladeltsa.md`:
через `logout()` идут «Забыли PIN» и лимит попыток, и тот же человек заново
заполнял бы анкету после каждого сброса PIN.

**Грабли и что осталось:**

- Анкета, заполненная до этой правки, владельца не имеет и сотрётся при
  первом входе. Релиза не было, миграцию не делали.
- После выхода анкета прежнего человека лежит в DataStore до следующего
  входа (не показывается — гостевого режима нет).
- Та же болезнь у взятых талонов очереди (`queue_walkin_tickets`): очистки у
  хранилища нет вовсе, новому человеку до 12 часов виден талон прежнего —
  issue #257. Флаг биометрии не болеет: шаг биометрии есть в каждом входе и
  перезаписывает его.

## Больницы: карточка врача и свободные слоты (issue #181, дубль #220, 2026-09-10)

**Проблема.** `HospitalApi` не спрашивал занятость врача вовсе: время приёма
выбиралось из сетки, которую строил клиент (`DoctorSchedule`), а отказ сервера
человек видел только после нажатия «Записаться». Сверка #92 (снимок 2026-09-09)
нашла у `hospital-controller` шесть новых ручек, среди них настоящие слоты —
`GET hospitals/doctors/{id}/slots?date=` (`ApiResponseListString`, тот же вид
ответа, что у слотов брони, issue #97), а заодно карточку врача
(`GET hospitals/doctors/{id}`) и карточку записи (`GET hospitals/appointments/{id}`).

**Решение.** `DoctorSchedule` убран целиком вместе со своим тестом — сетку
теперь считает сервер. На его месте `DoctorSlots.available()`
(`feature/hospital/domain/Doctor.kt`) — почти буквальная копия
`BookingSlots.available()`: то же самое правило «не предлагать наступившее
время сегодня», тот же разбор через `parseServerLocalTime`. Экран и
`DoctorBookingViewModel` под капотом теперь устроены как `BookingScreen` /
`BookingViewModel`: запрос слотов уходит на каждую смену врача **и** дня (а не
только дня, как у брони, — занятость своя у каждого врача), прежний запрос
отменяется.

**Риск из issue #144 учтён буквально.** Строка слота (`"09:00"`/`"09:00:00"`)
не парсится в `LocalTime` и не собирается обратно через `toServerTime()` —
`DoctorSlot` хранит `raw` и `time` порознь, и в `startTime` записи уходит
именно `raw`, ровно в том виде, в каком его отдал сервер. Разница с бронью
(там слот идёт через `LocalTime.toServerTime()`, что всегда дописывает
`:00` секунд) — намеренная: лишний шаг «разобрали → собрали заново» уже один
раз стоил вертикали брони пяти часов расхождения между UTC и Asia/Tashkent, и
здесь его нет вовсе. Тест `HospitalRepositoryTest` это закрепляет: слот без
секунд в ответе (`"09:30"`) уходит в теле POST без секунд же, а не как
`"09:30:00"`.

`DoctorAppointmentDraft.time: LocalTime?` заменён на `slot: DoctorSlot?` — по
той же причине: черновик должен нести и разобранное время (для сравнений и
подсказок на экране), и исходную строку (для отправки).

`doctor(id)` и `appointment(id)` объявлены в `HospitalApi`/`HospitalRepository`
по контракту, но экраном пока не используются — карточки врача и записи
показывать отдельно продолжит issue #183; здесь важно было не выдумать
контракт заранее и не оставить ручки необъявленными.

**Дубль.** Тот же разбор запрашивала issue #220 (независимая находка по той же
сверке `/v3/api-docs`, issue #167) и `docs/TASKS-BACKLOG.md` (задача T16, тоже
отметила дубль). Обе issue описывают одну и ту же ручку слотов и одно и то же
решение — закрыты обе.

Тесты: `DoctorSlotsTest` (бывший `DoctorScheduleTest`) — разбор слота, дубликаты
по значению, мусорная строка, прошедшее время, пустой ответ, граница суток по
Ташкенту; `HospitalRepositoryTest` — слоты по врачу и дню на `MockWebServer`
(путь, разбор, пустой день, отказ 404), карточка врача (с падением на
запрошенный `id`, если сервер его не назвал), карточка записи, буквальная
передача строки слота в `startTime`; `DoctorBookingViewModelTest` — слоты
запрашиваются на выбор врача и на смену дня/врача с сбросом выбора, пустой
день — `ScreenState.Empty`, отказ показывается и повторяется.

Полный прогон: `testDebugUnitTest` (2092 теста) + `assembleDebug` + `lintDebug`
— все три зелёные.

## Отзывы: имя и аватар автора не выдумываем, ответ заведения теперь виден (issue #192, 2026-09-11)

Ревью сверки #92 (после развода коллизии `Response`, issue #217) подтвердило:
разведённый `ReviewResponse` не содержит ни `userName`, ни `userAvatarUrl` —
`ReviewDto` три года гадал алиасы (`author`/`authorName`, `avatarUrl`/`userAvatar`)
под поля, которых в схеме не было никогда. Оба поля с дефолтами, поэтому
разбор не падал — он молча отдавал пустое имя и `null`-аватар, и заметить это
можно было только на живом стенде.

**Что сделано.** `ReviewDto` (`CatalogApi.kt`) больше не разбирает
`userName`/`userAvatarUrl` и их алиасы — гадать имя автора, которого у
бэкенда нет вовсе, бессмысленно. Вместо них добавлены три поля, которые в
схеме есть, но раньше не разбирались: `isVerified`, `helpfulCount`,
`ownerReply`. Домен `Review` лишился полей `author`/`avatarUrl` — экран
показывает отзыв под плейсхолдером «гость» (`place_review_anonymous`), а не
угаданное и всегда пустое имя. Ответ заведения (`ownerReply`) теперь доходит
до домена и выводится под текстом отзыва отдельным блоком.

Решение по имени автора — «честно показать отзыв без имени», а не резолвить
автора отдельным запросом: у бэкенда есть только `userId`, восстанавливать по
нему имя было бы отдельной задачей на бэкенд, которой нет в скоупе.

`docs/API-CONTRACT.md` обновлён: пометка «живой баг, issue #192» снята,
раздел описывает решение, а не открытый вопрос.

**Тесты:** `PlaceMappersTest` — разбор реального `ReviewResponse` (проверены
`isVerified`, `helpfulCount`, `ownerReply`), отзыв без `ownerReply` даёт
`null`, а не пустую строку. `CatalogRepositoryTest` и
`PlaceDetailsViewModelTest` избавлены от полей `author`/`userName`, которых
в контракте не существует.

**Прогнано:** `testDebugUnitTest`, `assembleDebug`, `lintDebug` — зелёные.


## issue #214 — хвост списка: один компонент вместо одиннадцати копий

Находка ревью диффа по issue #151: `LoadMoreItem` был скопирован в одиннадцать
экранов (`Wallet`, `MyPlaces`, `Pharmacy`, `Notifications`, `GamingBookings`,
`MyFreelancerOrders`, `Freelancers`, `Search`, `MyTickets`, `MyAppointments`,
`ActivityScreen`) плюс `FashionUi.FashionLoadMore`, общий для двух экранов
«Одежды». Десять из двенадцати копий побайтово совпадали (разнились только тип
состояния и события).

**Решение по п.1 задачи** (кнопка «Показать ещё» — для всех списков или только
для многоисточникового): не для всех. Кнопка — фикс конкретно под баг
`ActivityScreen` (issue #151): там автотриггер был ключом на курсор
`nextPages`, который сдвигается на каждой странице и перезапускал сам себя,
вычитывая разом все страницы всех пяти источников. У остальных десяти ключ —
`itemCount` (число строк), а не курсор: он не меняется, пока не пришла новая
страница, значит эффект не перезапускает сам себя вне ответа на реальный рост
списка. Это не тот же баг, а самоограничивающийся автотриггер — сознательный
выбор для длинных списков (в коде `SearchScreen` уже было обоснование: кнопка
на каждую страницу браузингового списка раздражает).

**Что сделано.** Два переиспользуемых компонента в
`core/ui/components/LoadMore.kt`: `LoadMoreAuto` (десять экранов + оба
Fashion) и `LoadMoreButton` (`ActivityScreen`), с общим приватным блоком
провала (текст + `MahallaErrorDetails` + «повторить») и строкой
`action_load_more` (переименована из `activity_load_more`, была только у
Activity). `LoadMoreAuto` заодно унаследовал приём `SearchScreen`, которого не
было у остальных девяти: место под спиннер держится всегда (`Spacer`, когда
`isLoading == false`), иначе список дёргается на высоту индикатора каждый раз,
когда страница догрузилась. Одиннадцать копий `LoadMoreItem` и
`FashionLoadMore` удалены вместе с приватными константами `LOAD_MORE_INDICATOR`
— общий размер теперь `MahallaComponentDefaults.loadMoreIndicatorSize`.

**Тесты:** новый `LoadMoreTest` (`core/ui/components`) — один раз на оба
компонента, а не по разу на экран: триггер один раз при композиции, не
повторяется при неизменном `itemCount`, повторяется при росте списка, провал
не ретриггерит автоматически и даёт кнопку «повторить»; для кнопки — тап даёт
одно событие, занятость во время запроса, провал прячет кнопку за причиной.
`ActivityScreenTest` не тронут по существу — только ссылка на переименованную
строку.

**Прогнано:** `testDebugUnitTest` (2168 тестов), `assembleDebug`, `lintDebug` —
зелёные.

## Услуги мастеров разбирались схемой барбершопа (issue #216, 2026-09-11)

Побочная находка ревью контракта по issue #154: у бэкенда `FreelancerApi.services`
(`GET freelancers/{id}/services`) и `BookingApi.services`
(`GET barber-services/places/{placeId}`) — разные схемы (`FreelancerServiceResponse`
с `title`/`priceAmount` против `AppointmentServiceResponse` с `name`/`price`), а
клиент разбирал обе одним барберским `ServiceDto`. У каждой услуги мастера было
пустое название и цена 0. Не всплывало на стенде, потому что каталог мастеров
там пуст (`GET freelancers` → `content: []`) — пустой список не отличить от
списка пустых карточек.

**Что сделано.** Заведён свой `FreelancerServiceDto` (`FreelancerApi.kt`) со
своими именами полей и свой маппер в домен `BarberService` (переиспользуется —
набор полей на экране совпадает, а `freelancerId` там не нужен: он уже известен
вызывающей стороне из аргумента запроса). `docs/API-CONTRACT.md` и KDoc
`ServiceDto`/`BarberService` обновлены — раздел `FreelancerApi` больше не
описывает баг как живой.

Живую контрактную фикстуру для `freelancers/{id}/services` снять не вышло:
пока каталог мастеров на стенде пуст, услугу для сверки взять неоткуда —
блокирует не код, а данные на стороне бэкенда. Регрессия закреплена тестом на
`MockWebServer` (`FreelancerRepositoryTest`).

Из смежных пунктов issue #154 — коллизии `BookRequest`/`GamingBookRequest` и
`OrderResponse` у еды/одежды/мастеров — не тронуто: `BookRequest` и
`GamingBookRequest` уже перепроверены по чтению схемы (см. `BookingApi ✅` и
`GamingApi` в контракте), а сквозной проход по `OrderResponse` и остальным
DTO, где имена всё ещё выведены, заведён отдельной issue #235 — дублировать
его здесь не стали.

**Прогнано:** `testDebugUnitTest`, `assembleDebug` — зелёные.



---

## Эпик 12 — Бизнес-панель: дашборд, очередь, заказы, меню (issue #16)

**Что сделано.** Отдельный раздел приложения для владельцев заведений:
`feature/business/` — четыре экрана, домен с правилами переходов и
`BusinessRepository` поверх шести ручек бэкенда. Вход один — кнопка «панель»
в карточке своего заведения в «моих заведениях» (issue #94).

- **12.1 Дашборд** — метрики дня из `GET analytics/places/{id}/dashboard`,
  «пауза» (переключатель «открыто сейчас») и список разделов.
- **12.2 Очередь** — живой список из `GET walkin/barber/dashboard?placeId=`,
  «вызвать следующего» над списком плюс `accept`/`start`/`complete`/`decline`
  по каждому талону.
- **12.3 Заказы** — `GET food/places/{id}/orders` с фильтром по статусу и
  пагинацией, смена статуса через `PUT .../orders/{orderId}/status`.
- **12.4 Меню** — стоп-лист переключателем прямо в строке
  (`PUT food/items/{id}/toggle`) и форма новой позиции
  (`POST food/places/{id}/items`).
- **Ролевой доступ** — `BusinessAccess` поверх `GET places/my`.

**Контракт сверен, а не выдуман.** Живой `/v3/api-docs` на 2026-09-09 доступен;
каждый путь дополнительно проверен curl'ом (`401` = существует и требует
Bearer). Полная таблица и оговорки — в `docs/API-CONTRACT.md`, раздел
`BusinessApi`.

### Грабли и решения

- **Дашборд отдаёт словарь без схемы.** `ApiResponseMapStringLong` —
  `additionalProperties`, имён ключей нет вовсе. Разложить его по фиксированным
  полям значило бы получить пустой экран на первом же расхождении, поэтому
  клиент показывает **то, что приехало**: перевод — только у знакомых ключей,
  незнакомый выводится из имени (`total_revenue` → «Total revenue»).
- **Ключ `status` в теле смены статуса выведен, а не угадан.** У ручки тело —
  безымянная `Map<String, String>`, но ровно эту операцию у двух соседних
  вертикалей описывают настоящие схемы (`UpdateOrderStatusRequest`,
  `ModerateRequest`), и в обеих обязательное поле называется `status`. То же
  решение, что для `CreatePlaceRequest` в issue #84.
- **`accept` и `decline` уходят пустым объектом.** Их тела — тоже безымянные
  карты (`Map<String, Integer>` и `Map<String, String>`), и угаданный ключ
  бэкенд молча выбросит: `Map.get` вернёт `null`. Поэтому встречного времени и
  причины отказа в панели нет вовсе — поле, текст которого не доедет, обещает
  человеку разговор, которого не будет.
- **Набор разделов задаёт категория, а не только роль.** Очередь живёт в
  `walkin/*`, заказы и меню — в `food/*`. Показать парикмахерской «входящие
  заказы» значило бы позвать ручку еды за чужую вертикаль и получить отказ
  вместо списка. У аптеки, больницы, кино, игровых зон и одежды бизнес-ручек у
  бэкенда нет — разделов ноль.
- **`Mine.role == null` считается владельцем, а не сотрудником.** Все поля
  `Mine` необязательны (issue #94), и молчание сервера о роли не повод запереть
  человека в его собственном заведении: отказать всё равно успеет бэкенд.
- **Переходы статуса — только вперёд и на шаг.** Заказ, вернувшийся из «готов»
  в «готовится», клиент уже увидел готовым, и его телефон об этом сообщил;
  откат ломает не данные, а обещание. Отмена уходит из кнопок после начала
  готовки — то же правило, что у клиента.
- **Незнакомый статус талона не даёт заведению ни одного действия** — в
  отличие от клиентского `canCancel`, где он отмену разрешает. Клиенту нельзя
  запирать выход, а заведению нельзя предлагать действие, последствий которого
  никто не знает.
- **Строка не исчезает сразу после действия.** Обслуженный талон и заказ,
  выпавший из фильтра, остаются в списке до следующего обновления: строка,
  пропавшая ровно в момент нажатия, читается как «нажал не туда».
- **Доступ ищется перелистыванием `places/my`** — фильтра по `id` у ручки нет.
  Предел в 20 страниц есть затем, чтобы один открытый deep link на чужое
  заведение не означал обход всего списка. Отказ сети при этом отдаётся как
  есть, а не превращается в «доступа нет»: «проверьте интернет» и «это не ваше
  заведение» — разные вещи.

### Что нашло ревью субагентом (и что из этого починено)

Ревью в свежем контексте нашло девять дефектов, все починены до PR:

- **«Добавить первую позицию» не работало.** `Empty` выставлялось по «нет
  блюд», а список разделов для формы читался только из `Content` — то есть
  кнопка «добавить» была структурно недостижима ровно в том состоянии, ради
  которого написана. Теперь `Empty` — это «нет **разделов**» (в них и кладут
  позицию), а «блюд ещё нет» — строка внутри контента.
- **«Доступа нет» показывалось как «технический сбой».** Ветка со внятным
  текстом была недостижима: `ScreenState.Empty` для прав не выставлялся
  никогда, а `ApiError.Business` без текста сервера рисуется общим
  `error_unknown` — да ещё с кнопкой «повторить», которая гарантированно
  перелистает `places/my` и снова откажет.
- **Догрузка страницы откатывала только что смененный статус**: `loadMore`
  брал снимок списка до запроса и склеивал результат с ним.
- **«Пауза» писала поверх свежего `access`** — тем же снимком, откатывая заодно
  роль и статус модерации.
- **Вечный спиннер в хвосте заказов**, если страница целиком из дубликатов:
  список не растёт, автотриггер висит на его длине, `hasMore` от сервера
  остаётся `true`.
- **Ни один `load()` не отменял предыдущий** — «повторить» поверх
  pull-to-refresh давал два запроса, и выигрывал ответивший последним.
- **KDoc обещал тест, которого нет**: имена аргументов маршрутов панели не
  проверялись `RoutesSerializationTest`. Переименование поля сделало бы
  `placeId` пустым, и панель молча перестала бы открываться.
- **Единственный вход в панель был без тестов** (`MyPlace.canOpenBusinessPanel`,
  `MyPlacesViewModel.openBusinessPanel`).
- **`createItem` в репозитории не покрыт ничем** — ни клиентская отсечка формы,
  ни перечитывание меню, ни то, что пустые необязательные поля уходят
  *отсутствующими*, а не `null`.

Тесты после починки: **1961, 0 падений** (185 классов). Мутационная проверка
пройдена: внесённые дефекты в `BusinessAccess.canManageMenu` и
`BusinessOrderStatusFlow.nextStatuses` тесты краснят.

### Что осталось

- **Расписания работы у бэкенда нет вовсе** — пункт «расписание» из задачи 12.4
  не реализован. `UpdateRequest` заведения принимает имя, описание, адрес,
  координаты, город, телефон и сайт; единственный признак работы — «открыто
  сейчас», он же «пауза» на дашборде. Экран часов работы без ручки обещал бы
  кухне то, чего сервер не примет.
- **Имена ключей `accept` и `decline` не подтверждены** — из-за этого нет ни
  встречного предложения времени, ни причины отказа.
- **Состав метрик дашборда не подтверждён**: пока не приедет живой ответ,
  переведены только четыре предполагаемых ключа, остальные показываются
  по-английски.
- **Успешный путь на живом стенде не проверен**: все ручки панели требуют
  Bearer, а `CONTRACT_REFRESH_TOKEN` в CI нет — сверены только пути.
- На устройстве ничего не проверено (эмулятора в CI нет): экраны смотрелись
  глазами по `@ThemeLanguagePreviews`.

### Правка после issue #236 (мердж #233)

`BusinessMappers.kt` до этой правки читал `itemsAmount`/`deliveryAmount`/
`discountAmount`/`totalAmount`/`unitPrice`/`totalPrice`/`price` как сумы
напрямую — после #233 это тийины, и без правки дашборд, заказы и меню
показывали суммы в сто раз больше настоящих. Заодно нашлось симметричное:
`CreateMenuItemRequest.price` уходил в сеть как сумы, введённые в форме, — со
следующим мерджем это завело бы позицию меню в сто раз дешевле задуманной.
Обе стороны переведены на `Money.tiyinToSom()`/`somToTiyin()`; денежные
метрики дашборда (`MONEY_HINTS`) делятся на сто той же функцией, счётные —
нет. Числа в тестах, которые раньше совпадали с сырыми тийинами один в один,
пересчитаны на сумы (`84_000` тийин → `840` сум и так далее); тест исходящей
цены обновлён на `somToTiyin("32000") == 3_200_000`.
**Прогнано:** `testDebugUnitTest`, `assembleDebug`, `lintDebug` — зелёные.

### Правка по issue #301: всплеск параллельных 401 не должен считаться как N провалов контракта подряд

Ревью PR #302 (issue #198) отдельно заметило: провалившийся refresh не
обновляет `session.accessToken` (обновить нечем), поэтому короткая проверка
«токен уже обновил параллельный запрос» после такого провала не срабатывает.
Несколько экранов, разом упёршихся в 401 (например, на холодном старте), по
очереди берут `synchronized`-лок и каждый делает **свой** сетевой вызов
`auth/refresh`. Если в этот момент ответ подменяет чужой прокси (issue #138),
он может отдать одинаковое тело на все эти близкие по времени запросы — и
счётчик `consecutiveAmbiguousRefreshFailures` дошёл бы до
`MAX_AMBIGUOUS_REFRESH_FAILURES` не потому, что контракт сломан надолго, а
потому что несколько запросов от одного и того же прокси-события пришли
почти одновременно.

Выбран первый из вариантов issue #301: `TokenAuthenticator` считает ответ
отдельным провалом только если он пришёл не раньше `MIN_AMBIGUOUS_REFRESH_GAP`
(5 секунд) после предыдущего засчитанного — это отсекает всплеск, но не
редкие (например, раз в неделю) повторяющиеся глюки прокси, которые расходятся
по времени сами. Второй вариант (пускать по одному live-refresh за раз даже
после провала) не выбран — это отдельная переработка `synchronized`-блока, а
не строка рядом со счётчиком.

**Тесты.** `NetworkStackTest`: часы теста (`fixedClock`) заменены на
подвижные (`MovableClock`, тот же приём, что в `QueueViewModelTest`) — иначе
сценарий с разнесённым по времени контрактом было бы нечем отличить от
всплеска. Существующий тест на три подряд провала теперь двигает часы между
ответами; добавлены два новых — всплеск из пяти близких ответов не кончает
сессию, и всплеск, засчитанный один раз, продолжает счёт при следующих
по-настоящему разнесённых провалах. Оба новых теста проверены обратным
прогоном (без фикса — красные).

**Прогнано.** `testDebugUnitTest` — **2684 теста, 0 падений** (пересчитано с
временно отменённой дублирующей `PromotionsRepository.check()`, issue #300 —
без этого main и эта ветка не собираются вовсе, но правка issue #300 не входит
в эту задачу); `assembleDebug` — BUILD SUCCESSFUL при том же временном снятии
дубликата.

## Этап: подписки — состояния, продление, история списаний (issue #13, эпик 9)

Эпик 9 был закрыт наполовину: тарифы и оформление (9.1, 9.2 частично) сделаны
в issue #103, а в списке задач оставались продление, состояния подписки и
история списаний. Сделано ровно это — вертикаль не переписывалась.

**Контракт перепроверен по живой схеме стенда**
(`https://189-74-96-232.nip.io/v3/api-docs`, 2026-09-08):

- семь ручек `subscriptions/*` совпадают с `SubscriptionsApi` до полей; ответ
  про подписку в схеме зовётся `UserSubscriptionResponse` (в issue #103 он
  назван `SubscriptionResponse`) — состав полей тот же, DTO не менялся;
- **отдельной ручки «история списаний по подписке» у бэкенда нет.** Есть
  `GET payments/transactions` — вся история платежей человека, без фильтра по
  назначению. Списания за подписку отбираются по `purpose`, на клиенте.

### Что появилось

- **`SubscriptionStage`** — состояние подписки так, как его читает человек:
  активна / **истекает** / истекла / отменена / неизвестно. Отдельно от
  `SubscriptionStatus`: тот пересказывает поле `status`, а «истекает» такого
  статуса у бэкенда нет вовсе — оно выводится из `daysRemaining` (≤ 3 дней) и
  `inGracePeriod`. Порядок правил важен: `EXPIRED` от сервера сильнее остатка
  дней, `CANCELLED` сильнее «ещё работает» (доступ до конца срока остаётся, но
  продлевать её не будут), а `isActive == false` без грейса — «истекла» даже
  при незнакомом статусе. Своего расчёта от `expiresAt` нет по-прежнему:
  остаток дней считает сервер, у него грейс-период.
- **Продление (9.2)** — кнопка в карточке подписки той же ручкой
  `POST subscriptions/subscribe`, но тарифом и периодом **уже оплаченной**
  подписки: человек, нажавший «продлить», просит то же самое, и переспрашивать
  это незачем. Кнопка живёт только там, где подписка сама не продлится: срок
  вышел, идёт грейс-период или подписка отменена (`Subscription.canRenew`).
  **У действующей подписки кнопки нет намеренно** — контракт не обещает, что
  `subscribe` *прибавит* срок к оплаченному, а «продление», обнулившее
  оплаченные дни, это потерянные деньги; действующая продлевается
  автопродлением, у него своя ручка. Заодно починилось: у отменённой подписки
  её же тариф в списке был без кнопки вовсе (`isCurrentExactly` не смотрел на
  состояние) — вернуть свой тариф было нечем.
- **Даты продления (`SubscriptionRenewal`)**. Дата следующего списания — это
  не расчёт, а `expiresAt` сервера, взятая тогда, когда списание вообще будет
  (автопродление включено и подписка ещё действует); у истёкшей и в грейсе она
  не показывается — «следующее списание» прошлой датой было бы ложью.
  Единственный свой расчёт — **прогноз «продление доведёт примерно до»**: этой
  даты до самого запроса не существует нигде, `subscribe` возвращает срок
  только вместе со списанием. Считается календарно и в зоне приложения (31
  января + месяц = 28 февраля, 29 февраля + год = 28 февраля), от конца
  оплаченного срока, а у истёкшей — от «сейчас». Часы приходят из графа
  (`Clock`), момент лежит в состоянии: `Instant.now()` в `@Composable` сделал
  бы недетерминированными и экран, и превью.
- **История списаний (9.3)** — `PaymentsApi` (одна ручка) плюс
  `SubscriptionCharge`/`ChargeStatus`/`ChargeProvider` в домене. На экране:
  дата, сумма, платёжная система, бейдж исхода (прошедшее списание бейджа не
  получает — он и так очевиден) и текст ошибки сервера у непрошедшего платежа.
  Названия платёжных систем переиспользуют строки пополнения кошелька и
  checkout'а «Еды»: это те же бренды, свои копии разъехались бы при первой
  правке.
- **Фильтр клиентский — значит пустая страница ещё ничего не значит.** Порция
  истории подписки может занять несколько серверных страниц (у человека подряд
  шли пополнения кошелька), поэтому репозиторий пропускает пустые страницы —
  но не более пяти за запрос, у истории платежей предела нет. С какой страницы
  продолжать, возвращает сам ответ (`SubscriptionChargePage.nextPage`):
  считать «страница + 1» на экране было бы неверно уже после первой догрузки.
  Догрузка — кнопкой «показать ещё», а не по достижению конца списка (как в
  кошельке, issue #62): под историей идут тарифы, и автотриггер срабатывал бы
  у каждого, кто просто доскроллил до них.
- **История — третье независимое `ScreenState`** рядом с тарифами и подпиской:
  её отказ не прячет подписку, за которую человек платит. Секция целиком
  спрятана, пока не о чем говорить (нет ни подписки, ни списаний): заголовок с
  пустотой под ним на самом частом состоянии экрана — шум. После оформления,
  продления и пробного периода история перечитывается: списание, которого в
  ней нет, — самый быстрый способ заставить человека сомневаться в платеже.

### Что нашло ревью диффа (субагент, свежий контекст)

Шесть находок, все починены в том же коммите:

- **хвост истории был недостижим у пустого списка**: репозиторий намеренно
  отдаёт `items = []` с `hasMore = true`, когда за пять страниц платежей ни
  одного подписочного не нашлось, а кнопка «показать ещё» рисовалась только в
  ветке `Content` — человек с сотней пополнений кошелька видел бы «за подписку
  не списывали» и не смог бы продолжить. Кнопка теперь есть и у пустой истории;
- **курсор пагинации сбрасывался до прихода ответа**: «показать ещё» поверх
  идущей перезагрузки уходило за нулевую страницу и не добавляло ни строки.
  Догрузка теперь ждёт, пока история грузится с нуля (`chargesJob`, `loadJob`);
- **`ScreenResumed` не видел уже идущей фоновой загрузки** (она не ставит ни
  скелетон, ни `isRefreshing`): два `ON_RESUME` подряд давали по два запроса в
  каждую ручку и состояние от того, кто ответил последним. Появился `loadJob`;
- **прогноз продления у отменённой подписки был оптимистичным**: считался от
  `expiresAt` в будущем, то есть обещал срок на период больше, если сервер
  начнёт отсчёт от оплаты. У отменённой отсчёт теперь всегда от «сейчас» —
  округляем вниз, как выгоду годовой оплаты;
- **у истёкшего тарифа кнопка называлась «сменить период оплаты»** (а бейдж
  «ваш тариф» был зелёным): менять там нечего, это оформление заново;
- **провал перечита истории после продления стирал показанные списания** —
  ровно в тот момент, когда человек ищет своё новое списание. Теперь список
  остаётся (`keepShownOnFailure`).

Из замечаний ревью не принято одно: тест «четыре состояния подписки на экране»
оставлен, но переписан — он проверяет не чистую доменную функцию, а её
следствия в состоянии экрана (есть ли что продлевать, какой тариф найден). ТЗ
эпика просит ViewModel-тест именно на состояния, и следствия — то, чем он
отличается от `SubscriptionStageTest`.

Проверено: `./gradlew testDebugUnitTest` — **1894 теста в 180 классах, 0
падений, 0 ошибок**; `lintDebug` — `No issues found`; `assembleDebug` — BUILD
SUCCESSFUL. Обе гонки и прогноз у отменённой подписки проверены снятием
исправления: тесты краснеют (правило из `.claude/rules/testing.md`). Новые тесты: `SubscriptionStageTest` (четыре состояния и приоритет
правил, грейс, незнакомый статус работающей подписки, отсутствие своего
расчёта от `expiresAt`), `SubscriptionRenewalTest` (следующее списание и три
случая, когда его нет; отсчёт от срока и от «сейчас», конец месяца, 29
февраля, зона приложения, период подписки против выбранного),
`SubscriptionChargeTest` (фильтр по назначению, разбор исхода и провайдера);
новые случаи в `SubscriptionRepositoryTest` (путь и query истории, фильтр
пополнений, сумма в тийинах, пропуск пустых страниц и его предел, `nextPage`,
хвост по `last` и по `totalPages`, платёж без `id`, `401` остаётся отказом) и
`SubscriptionViewModelTest` (четыре состояния на экране, следующее списание
только там, где оно будет, прогноз продления у истёкшей, продление оплаченным
периодом и перечит истории, запрет продления действующей, отказ продления,
загрузка и отказ истории, «показать ещё» с дедупликацией и продолжением с
серверной страницы, продолжение пустой истории, запрет догрузки поверх
перезагрузки, второй `ON_RESUME` не заводит вторую загрузку, отказ перечита
после продления не стирает список, повтор истории не дёргает тарифы).

**Не сделано / риски:**

- **Значения `purpose` не подтверждены.** В схеме это `string`, поэтому фильтр
  ищет подстроку `SUBSCRIPTION`. Если бэкенд обозначает подписку иначе
  (`PLAN`, `TARIFF`), история будет пустой при непустых платежах. Уточнить у
  `jack5505/mahalla`.
- **Проходит ли оплата подписки через `payments/*` вообще** — неизвестно.
  `subscribe` не возвращает ни `paymentUrl`, ни `transactionId` (риск из issue
  #103 открыт), и деньги, похоже, списываются с кошелька на сервере. Тогда
  списания за подписку могут лежать в `wallet/transactions` с
  `referenceType`, а не в `payments/transactions` — и история окажется пустой.
  Замены на кошелёк здесь нет намеренно: у `wallet/transactions` нет ни
  `purpose`, ни причины отказа платежа, а гадать между двумя ручками без
  живого ответа — то же самое, что выдумывать контракт.
- **Успешный путь на живом стенде не проверен**: все ручки требуют Bearer, а
  SMS-кода в CI нет. Проверены форма запросов по схеме и коды отказов.
- **Порог «истекает» — три дня**, выбран приложением: у бэкенда такого понятия
  нет. Если у него другое правило для уведомления `SUBSCRIPTION_EXPIRES`,
  бейдж и письмо разойдутся.
- **Продление отменённой подписки может её не «оживить», а начать новую**:
  прибавит ли сервер срок к остатку оплаченного, из контракта не следует.
  Поэтому и прогноз назван прогнозом.
- На устройстве ничего не проверено (эмулятора в CI нет), скриншот-тестов
  по-прежнему нет: карточка подписки с продлением, бейдж «истекает» и история
  списаний проверялись глазами по `@ThemeLanguagePreviews`.

## issue #257 — талон очереди прежнего человека после входа под другим номером (2026-09-12)

Продолжение #243 (ADR 0008): анкета владельца помнит, а талон живой очереди —
нет. Хранилище `queue_walkin_tickets` общее на устройство, `active(placeId)`
отдавал первый живой талон заведения, кто бы его ни взял. Человек B, вошедший
после A, видел чужой номер в очереди вместо формы записи и мог нажать
«отменить» — `cancel(ticket.id)` ушёл бы с токеном B.

**Решение — талон помнит `id` аккаунта** (`StoredTicket.ownerId`), а `active()`
сравнивает его с тем, кто вошёл сейчас (`UserProfileStore.current().id`). Чужие
живые талоны не стираются: вернувшись, владелец находит свой на месте — «Забыли
PIN» и лимит попыток через выход его не теряют. Замена по заведению работает
только внутри одного владельца. Решение и цена — `docs/adr/0009-talon-pomnit-vladeltsa.md`.

**Грабли и что осталось:**

- Талон, записанный до этой правки, владельца не имеет и не показывается
  никому: миграции не делали (релиза не было), безопасная сторона та же, что в
  ADR 0008.
- Вход, в ответе которого сервер не назвал `user.id`, талон не покажет вовсе —
  человек увидит форму записи. Случай редкий, выбрана безопасная сторона.
- Чужой талон лежит на устройстве до 12 часов (до конца своего срока жизни):
  не показывается, но и не удаляется.
- История поиска (`discovery_search_history`) владельца по-прежнему не имеет —
  отдельная задача.

## issue #170 — GET/PUT users/me подключены: профиль читается с сервера, имя и аватар редактируются (2026-09-13)

Контракт `GET`/`PUT users/me` был снят ещё в issue #237 (`docs/API-CONTRACT.md`),
но приложение обе ручки не звало: профиль жил только тем, что приехало в ответе
на вход, и одиннадцать KDoc в коде (не девять, как решили по итогам #61 —
нашлись ещё `CustomerFormScreen.kt` и `GraphAssemblyTest.kt`) утверждали, что у
бэкенда этих ручек нет вовсе.

**`feature/profile/data/ProfileApi.kt` + `ProfileRepository`** — новый слой на
основном Retrofit (запросы требуют Bearer, как и `SessionsApi`):
`refresh()` перечитывает `GET users/me` и переписывает `UserProfileStore`
целиком, `updateProfile(fullName, avatarUrl)` шлёт `PUT` и делает то же самое
ответом сервера. Источник истины теперь один — локальный профиль всегда то,
что последним подтвердил сервер (или то, что принёс вход, пока `GET` не
успел).

- **Профиль перечитывается при открытии экрана и при возврате на него**
  (`ProfileViewModel.refreshProfile`, тот же `onScreenResumed`, что у списка
  устройств). Отказ `GET` не трогает состояние: `UserProfileStore` уже хранит
  то, что сохранил вход, и шапка не пустеет и не заменяется ошибкой — она
  просто отстаёт до следующей удачной попытки.
- **Имя редактируется прямо в шапке**: иконка рядом с именем открывает поле
  (`NameEdit` в состоянии — черновик, а не `TextFieldValue` в композиции,
  правило `.claude/rules/compose-ui.md`), «Сохранить» шлёт `PUT` с одним
  изменённым полем. Пустое имя не отправляется — сервер читает пустую строку
  как «снять значение», а стереть имя случайным нажатием не должно быть можно.
- **Аватар после загрузки (`media/upload`, issue #101) теперь привязывается к
  профилю** тем же `PUT`: раньше адрес писался только в локальный
  `UserProfileStore` и терялся при следующем входе. Файл на сервере лежит
  независимо от исхода `PUT` (issue #101 уже это сделал) — отказ здесь
  показывается в `avatarUpload.failure`, а не считается «загрузка не удалась».
- **Три места, где `GET`/`PUT` могли обогнать друг друга** — все три отвечают
  профилем целиком, и опоздавший ответ стёр бы то, что сохранил другой:
  `refreshProfile()` не зовётся резюмом экрана, пока идёт сохранение имени или
  аватара; сохранение имени и загрузка аватара не стартуют одно поверх
  другого (проверялось снятием каждой из трёх защит: тест краснеет).
- **Одиннадцать устаревших KDoc переформулированы**: `ProfileViewModel` (класс
  и `uploadAvatar`), `ProfileContract`, `AuthRepository`, `RoleRepository`,
  `PreferenceKeys`, `UserProfileStore`, `AuthRepositoryTest`,
  `PlaceDetailsViewModel`, `CustomerForm`, `CustomerFormScreen`,
  `GraphAssemblyTest`.

**Не сделано / вне скоупа этого issue:**

- `CustomerForm` (анкета покупателя) по-прежнему не отправляет `fullName` на
  сервер через `RoleRepository.saveCustomer` — это issue #234, отдельный
  источник того же поля.
- `language` и `role` в `PUT` не отправляются — контракт их не принимает
  (issue #237, #242, `docs/adr/0007-yazyk-i-rol-istochnik-istiny.md`).
- Удаление аватара (`avatarUrl: ""`) в UI не добавлено — контракт это
  позволяет, но issue просил только загрузку и редактирование имени.

Проверено: `./gradlew testDebugUnitTest` — **2327 тестов в 208 классах, 0
падений**; `lintDebug` — без замечаний; `assembleDebug` — сборка успешна.
Новые тесты: `ProfileRepositoryTest` (чтение и запись на настоящем сетевом
стеке — `MockWebServer`, PATCH-семантика тела, пустая строка снимает аватар,
отказ не трогает локальный профиль) и добавленные случаи в
`ProfileViewModelTest` (профиль читается при открытии и возврате на экран,
отказ чтения не опустошает шапку, редактирование и отказ сохранения имени,
пустое имя не уходит на сервер, отмена редактирования не трогает сеть, аватар
после загрузки уходит в `PUT`, отказ `PUT` после успешной загрузки).

## issue #252 — владелец заводит акцию заведения из «Моих заведений» (2026-09-14/17)

Второй кусок issue #252 (первый — витрина аптеки, PR #260, уже в `main`):
владелец или менеджер заведения заводит акцию (`POST
promotions/places/{placeId}`) прямо из списка «Мои заведения», без захода в
неслитую бизнес-панель (PR #161) — тем же приёмом, что и у аптеки.

- `PromotionsApi.create` + `CreatePromotionRequest`, `PromotionsRepository.
  createPromotion` (сумы → тийины, отказ до сети на заведомо невалидном
  черновике). `NewPromotionDraft`: обязателен заголовок, вид акции требует
  своего числа — процент для `PERCENT_OFF`, сумма для `FIXED_OFF`,
  `FREE_DELIVERY` числа не требует.
- Кнопка «Добавить акцию» — доступна **любой категории** заведения (в
  отличие от «управлять товарами» у аптеки), владельцу и менеджеру, только
  для заведений, прошедших модерацию (`MyPlace.canManagePromotion`).
- Форма создаёт три вида акции из шести — `BUY_X_GET_Y`, `HAPPY_HOUR`,
  `FLASH_SALE` требуют условий, для которых на клиенте нет ни поля, ни
  подтверждённой схемы.
- Тело `POST promotions/places/{placeId}` не проверено живым запросом —
  нужен Bearer владельца заведения, а `CONTRACT_REFRESH_TOKEN` в песочнице не
  задан. Поля выведены из уже подтверждённого ответа `Promotion` того же
  контроллера, а не угаданы по аналогии с другой вертикалью.

**Правки по итогам ревью** (`docs/API-CONTRACT.md` не менялся): тест на
`FIXED_OFF` — единственная ветка, где сумма скидки реально конвертируется в
тийины, была не покрыта; после успеха форма закрывалась без явного сигнала
(снекбар `MyPlacesEffect.PromotionCreated` — на этом экране акции заведения
никак иначе не видны, и без сигнала успех и смахнутую шторку не отличить, а
типичный исход — повторный POST и дубль акции); заголовок шторки был именем
заведения, а не «Новая акция» (имя заведения теперь подзаголовком); `MyPlace.
canManagePromotion` дословно повторял `canToggleAvailability` — стал алиасом;
`minOrderAmount = 0` теперь не уходит на сервер, симметрично чтению.

Что осталось из #252: **еда** (`option-groups`/`options`) ждёт мержа PR #161
(там же появится экран меню); **одежда** — `FashionApi` целиком не сверена с
бэкендом, отдельная issue.

Проверено: `./gradlew testDebugUnitTest` — **2354 теста в 209 классах, 0
падений**, 2 пропущено; `lintDebug` — без замечаний; `assembleDebug` — сборка
успешна.
---

## issue #234 — имя из анкеты покупателя доезжает до сервера (2026-09-14)

После #170 источник истины по профилю — сервер: `ProfileRepository.refresh()`
(`GET users/me`) при каждом открытии вкладки «Профиль» переписывает
`UserProfileStore` целиком. Анкета покупателя (`RoleRepository.saveCustomer`,
issue #84) при этом писала `fullName` только локально — первое же открытие
профиля после онбординга откатило бы имя обратно на пустое, полученное с
сервера, который об этом имени ещё не знал.

**Решение — не сеть из анкеты, а отложенная отправка:** анкета по-прежнему не
делает сетевых запросов сама (первый же отказ сети иначе запирал бы человека
на форме — она уходит с экрана только по успеху локального сохранения).
Вместо этого `saveCustomer` помечает `UserProfile.fullNamePendingSync = true`.
`ProfileRepository.refresh()` видит флаг и вместо `GET` шлёт `PUT users/me` с
этим именем; успешный ответ снимает флаг (как любой `GET`/`PUT` — маппинг
DTO→`UserProfile` не знает о нём и по умолчанию кладёт `false`), отказ —
оставляет как есть, следующее открытие профиля повторит попытку.

- **`AuthRepository.saveProfile`** (вызывается на каждом входе — SMS, оба шага
  PIN, Telegram) чинился отдельно: он строил `UserProfile` с нуля из ответа
  на вход и без изменений стёр бы ещё не подтверждённый `fullName` при первом
  же PIN-входе после анкеты, даже не открывая профиль. Флаг и имя переносятся,
  только если `id` совпадает с уже сохранённым — вход другого аккаунта на том
  же устройстве не должен унаследовать чужую анкету.
- **`ProfileViewModel.saveName()`** теперь не стартует поверх ещё не
  завершённого `profileRefreshJob` — раньше проверялся только `avatarJob`, а
  автоматический `PUT` от `fullNamePendingSync` мог обогнать ручное
  редактирование имени и переписать его обратно.
- Оказалось, что `MeDto.merge` с фолбэком на локальное значение, который
  просила убрать исходная задача, никогда не существовал: #170 сделала
  `refresh()`/`updateProfile()` безусловной заменой профиля ответом сервера
  без частичного слияния. Убирать было нечего.

Файлы: `RoleRepository.kt`, `ProfileRepository.kt`, `UserProfileStore.kt` +
`PreferenceKeys.kt` (новый ключ `profile_full_name_pending_sync`),
`AuthRepository.kt`, `ProfileViewModel.kt` (только доки), `docs/API-CONTRACT.md`.

Проверено: `./gradlew testDebugUnitTest` — **2337 тестов в 208 классах, 0
падений**; `lintDebug` — без замечаний; `assembleDebug` — сборка успешна.
Новые тесты: `RoleRepositoryTest`, `ProfileRepositoryTest` (успешная и
неудачная отправка отложенного имени через `PUT` вместо `GET`),
`DataStoreUserProfileStoreTest`, `AuthRepositoryTest` (вход того же аккаунта
сохраняет отложенное имя, вход другого — нет).

---

## Этап: редизайн под макет B «Focus» — фиолетовая тема (PR #283)

Хэндофф пришёл HTML-прототипами и README с токенами
(`design_handoff_mahalla_focus/`, в репозиторий не коммитился). Задача —
воссоздать экраны в Compose своими средствами, не перенося HTML.

**Тема и кит.** `ColorScheme` переведён на фиолетовый seed (`primary
5b3fc4`, `primaryContainer e8deff`, `surface fdf8ff`, `surfaceVariant f1ecf7`),
тёмная выведена из того же seed. Радиусы: 28 фокус-карточка и шторка, 24
талон и «пусто», 16 плитки, 20 кнопки-pill. `Spacing`: поля экрана 20dp,
онбординг 24dp, поле ввода 56dp. Инверсные роли схемы заведены под снекбар
(`#2b2440` / `#fdf8ff` / `#b9a3f0`). В кит добавлены `MahallaDivider`,
`MahallaPinDots`/`MahallaPinPad`, варианты кнопки `OnColor`/`OnColorGhost`,
радиус у `MahallaCard`, `showSelectedIcon` у чипа, знак M, кикер и мета у
`MahallaTopBar`. Пустое состояние (`StateMessage`) — карточкой r24.

**Экраны.** Онбординг: приветствие полноэкранным градиентом (0a), счётчик
«Шаг N из 5» и заголовок 26/600 в общем каркасе, ячейки кода 64×68 (0c),
PIN точками и нампадом (0e), город отмечается и подтверждается кнопкой (0d).
Главная: фокус-карточка «что мне сейчас» (талон либо ближайшее открытое
место), места строками с разделителями (1a). Карточка места: название в
теле с кикером, действия в ряд, адрес/часы/телефон одной таблицей (1b).
Очередь: место числом 48/600, ряд цифр под линией (2a). Кошелёк: баланс
40/600, история рядами, карточка «Mahalla+» с переходом на подписку (2c).
Профиль: крупное имя, город кикером, сегменты вместо чипов (2d).
Бронирование: сетка слотов 4×N, время в кнопке (1c).

**Грабли.**

- **Слоты `Typography` менять нельзя.** `titleMedium`/`labelLarge`/
  `bodyMedium` держат кнопки, поля и карточки на всех 45 экранах и в
  открытых PR; смена размера сдвинула бы вёрстку вертикалей вне скоупа.
  Крупные элементы новых экранов — отдельные стили `Focus*` в `Type.kt`.
- **`surface` и фон экрана совпали** (#fdf8ff) — `MahallaCard`, плитки и
  нижние панели стали невидимыми. Заливка контейнеров — `surfaceVariant`;
  дорожка сегмента — обводка. Правка палитры без прогона превью не видна.
- **Макет опережает бэкенд.** Нет `GET walkin/my` — «Обновить очередь»
  нельзя; нет номера талона («A-15») — показана позиция; нет ручки перевода
  — нет кнопки «Перевести»; каталог не знает, принимает ли место очередь —
  фокус-карточка ведёт на место, а не «Взять талон»; в выдаче нет строки
  очереди и счётчиков мест по районам. Выдумывать не стали — заведены
  issue #284, #285, #286.
- **Код PIN нельзя собирать в композиции.** `state.pin.code + digit` читал
  снимок последней отрисовки: два нажатия до рекомпозиции теряли цифру, а
  на границе «задайте → повторите» ломали ввод. Собирает ViewModel из
  своего состояния (`PinEvent.DigitPressed`).
- **`itemsIndexed` + `if (index > 0) MahallaDivider()`** — линии между
  строками рисует список, иначе они появляются под последней строкой и
  у одиночной карточки на карте.
- Ревью субагентом в свежем контексте нашло шесть настоящих дефектов
  (гонка PIN, чтение декоративного числа TalkBack'ом, распёртые чипы
  слотов, дыра от пустой ячейки, потерянная прокрутка приветствия, минуты
  вместо часов) — все исправлены до мержа.

**Второй заход по макету (тот же PR).** Общая шапка таба (знак M, кикер,
мета — «9:30 · вторник» на главной, срок подписки в кошельке, «180 м ·
09:00 – 18:00» на карточке места). Карточка «Mahalla+» в кошельке из
`payments/subscription`. Блок «Услуги» с ценами на карточке места из
`barber-services` — только у мест с записью; ноль у цены — «не названа», а
не «бесплатно». Таблица меты вместо двух секций. Строки мест с
разделителями, «открыто N из M», плитка «Все», линия с рейтингом/ожиданием
под фокус-карточкой и «обновлено 9:30» на талоне. Тумблер «Вход по
отпечатку» в профиле — включается только после системного промпта, как в
онбординге; два других тумблера макета — системные разрешения, их не
делали. Город в онбординге подтверждается кнопкой, код страны — плашкой.
Снекбар — инверсные роли схемы, пустое состояние — карточкой.

Ещё грабли: `spacedBy` списка + разделитель внутри ячейки дают линию,
прижатую к верху строки (20dp над, 0 под) — строки секции лучше одной
ячейкой либо без `spacedBy`; тест, ждущий запись ViewModel в файловый
DataStore без эффекта-маяка, мигает — брать `FakePreferencesDataStore`;
предохранитель «коммит только при зелёном gradle» в zsh нельзя строить на
`PIPESTATUS` — пустое значение там равно нулю.

**Осталось (issue).** #284 — бейдж талона на табе (упирается в то, что
талона нет в данных «Моих активностей»); #285 — бронирование шторкой (нужен
`navigation-material` или подъём состояния); #286 — плитка карты на карточке
места (нет `MAPKIT_API_KEY`), мёртвый `TicketFormatter`. Экран «Готово,
Чиланзар» (0f) не делали: его текст держится на числе открытых мест рядом,
которого до входа в каталог нет.

---

## Этап: две формы услуги — заказ и выставление (issue #71)

Issue просил «два формы: форма заказа и форма выставления услуги». Пока задача
ждала, половина уже приехала мимоходом: заказ услуги у мастера сделан вертикалью
«Мастера» (issue #107). Недоставало второй стороны — кабинета самого мастера,
который issue #107 прямо оставлял бизнес-панели.

Первый подход к этому же issue (PR #72 от 31.08, **не влит**) делал обе формы с
нуля и поверх другого бэкенда — заказ через `walkin/send`. К сентябрю `main`
уехал: `walkin` стал вертикалью «Очередь» (issue #96), а заказ услуги —
вертикалью «Мастера». Влить тот PR теперь значило бы завести третий экран
заказа, поэтому он заменён целиком; решение и альтернативы —
[ADR 0010](docs/adr/0010-dve-formy-uslugi.md).

**Сделано:**

- **Кабинет мастера** — экран «Мои услуги» в профиле
  (`feature/freelancer/ui/me`): анкета исполнителя (`GET`/`POST freelancers/me`),
  свои услуги (`POST`/`PUT`/`DELETE freelancers/me/services`) и переключатель
  «принимаю заказы» (`PUT freelancers/me/toggle-availability`).
- Порядок на экране вынужденный: **сначала анкета, потом услуги**. Списка «мои
  услуги» у бэкенда нет — они читаются общей ручкой
  `freelancers/{id}/services`, а `id` берётся из анкеты. Без анкеты кнопки
  «добавить услугу» на экране нет вовсе: она кончилась бы отказом сервера.
- `404` на `GET freelancers/me` — это «анкеты ещё нет», а не ошибка: экран
  показывает пустую форму. Ответ без `id` — тоже «нет»: по нему грузятся услуги.
- Ничего не кэшируется, после каждой правки список перечитывается: `PUT
  toggle-availability` нового значения не сообщает (инвертирует сам), а `DELETE`
  не говорит, снял он услугу или выключил.

**Попутно найденный баг (проверен живым запросом):** услуги мастера
разбирались `ServiceDto` барбершопа (`name`/`price`) «по той же схеме», а стенд
отдаёт `FreelancerServiceResponse` (`title`/`priceAmount`) — **в форме заказа у
каждой услуги мастера пропадали название и цена**. Заведён
`FreelancerServiceDto`, старое поведение теперь ловит тест.

Проверено: `./gradlew testDebugUnitTest` — **1890 тестов в 180 классах, 0
падений**; `lintDebug` — BUILD SUCCESSFUL; `assembleDebug` — BUILD SUCCESSFUL.
Новые тесты: `FreelancerProfileFormTest` (обязательны имя и специальность,
длины из схемы, телефон необязателен но проверяется, число вне `int32` — ошибка,
а не молчаливый ноль), `FreelancerServiceFormTest` (название и цена
обязательны, ноль ценой разрешён, цена вне `int64` — ошибка, правка несёт `id`),
`MyServicesViewModelTest` (пустая анкета открывает заполненную форму, анкета с
сервера показывает карточку и грузит услуги, перечитывание после сохранения,
отказ не теряет набранное, отмена возвращает сохранённое, доступность
перечитывается, новая услуга и правка, снятие через подтверждение), плюс
`FreelancerRepositoryTest` (поля услуг с живого ответа, `404` и пустая `data`
как «нет анкеты», отказ с кодом — всё-таки отказ, тела `POST freelancers/me` и
`POST`/`PUT me/services`, пустой `ApiResponseVoid`).

**Ревью диффа субагентом** нашло ошибку, которую тесты не ловили: у услуги с
нулевой ценой форма правки открывалась с пустым полем цены, и валидатор не
давал сохранить ни описание, ни длительность, пока мастер не наберёт «0»
руками, — притом что ноль ценой в этом же коде объявлен допустимым. Исправлено
(`FreelancerServiceForm.of` кладёт явный ноль), тест добавлен. Оттуда же — два
риска ниже и снятый мёртвый гард `canManageServices`.

**Не сделано / риски:**

- **Принять заказ мастеру из приложения нечем.** `GET freelancers/me/orders` и
  `PUT freelancers/orders/{orderId}/status` не подключены — это бизнес-панель
  (эпик #16). Выставить услугу можно, заказ придёт, а ответить на него — нет.
  Это следующая задача по вертикали.
- **Тела `POST freelancers/me` и `POST`/`PUT freelancers/me/services` под
  токеном не проверены**: стенд отвечает `401` до валидации тела, а
  `CONTRACT_REFRESH_TOKEN` в CI нет. У `FreelancerCreateRequest` коллизии
  springdoc нет (одна ссылка), а вот имя `ServiceRequest` делят три пути, и
  третий — чужой (`POST barber-services/places/{placeId}`): именно такая
  коллизия уже разводила поля `ServiceResponse`. Оба тела закреплены тестами —
  правка после проверки на живом аккаунте будет видна одной строкой.
- **Очистить необязательное поле анкеты, возможно, нельзя.** Стёртые
  `bio`/`city`/`phone`/`hourlyRate`/`experienceYears` уходят отсутствующими
  ключами (`explicitNulls = false`), и если сервер понимает это как «не
  менять», а не «затереть», текст вернётся в поле после перечитывания. Указало
  ревью диффа; проверять вместе с телом запроса на живом аккаунте.
- **Выключенные услуги в кабинете, возможно, никогда не покажутся.** Кабинет
  читает их анонимной ручкой `freelancers/{id}/services`, а она вправе
  отсеивать `isActive: false` сама — своей ручки «мои услуги» у бэкенда нет.
  Приложение их не фильтрует и помечает бейджем; если сервер их не отдаёт,
  бейдж просто не встретится. Тоже находка ревью.
- **Что именно делает `DELETE me/services/{id}`** — снимает строку или ставит
  `isActive: false` — из контракта не следует. Приложение на это не
  закладывается: список перечитывается, а выключенная услуга в кабинете видна с
  пометкой (клиенту она по-прежнему не показывается).
- **Единица цены не подтверждена** — та же открытая история, что в issue #97 и
  #107: мастера стенда стоят `8000000` за час, что как сумы неправдоподобно.
  Приложение считает сумами и в форме, и в каталоге — одинаково, но, возможно,
  одинаково неверно.
- **Город — свободный текст**, а не `City` приложения: бэкенд хранит и
  показывает здесь строку. Значит фильтр каталога по городу к нему не
  привязать, пока сервер не скажет, в каком виде он его ждёт (вопрос из
  issue #107 остаётся открытым).
- На устройстве ничего не проверено (эмулятора в CI нет): экран смотрели
  глазами по `@ThemeLanguagePreviews`.

---

## Входящие заказы мастера — issue #190, поверх кабинета из #71 (2026-09-11)

PR #255 реализовал весь кабинет мастера (анкета, услуги, входящие заказы)
своими новыми файлами (`FreelancerCabinetApi`/`Repository`, `ui/cabinet/*`),
не заметив открытый PR #173 (issue #71), который к тому моменту уже давал
анкету и CRUD услуг в существующих `FreelancerApi`/`FreelancerRepository`/
`ui/me/MyServicesScreen` — и попутно чинил живой баг #216. Оба PR объявляли
одинаковые классы (`FreelancerServiceDto`, `FreelancerCreateRequest`,
`FreelancerServiceRequest`) в одном пакете — слияние обоих в `main` не
скомпилировалось бы. Ревью диффа #255 нашло это блокирующим и предписало
сузить #255 до того, чего у #173 нет, и положить это поверх ветки #173, а не
рядом с ней; владелец репозитория подтвердил этот путь.

**Что сделано.** Ветка #255 слита с веткой #173 обычным merge-коммитом,
дублирующие файлы кабинета/анкеты и их тесты удалены (`git rm`). В
`FreelancerApi.kt` и `FreelancerRepository.kt` (уже существующих, из #173)
добавлены только два новых метода: `incomingOrders`
(`GET freelancers/me/orders`) и `updateOrderStatus`
(`PUT freelancers/orders/{orderId}/status`) — с телом `UpdateFreelancerOrderStatusRequest{status}`.
Новый экран `ui/orders/MyFreelancerIncomingOrders{Contract,Screen,ViewModel}`
мирит пагинацию `MyFreelancerOrdersViewModel` (issue #107) с действиями:
принять / отклонить / отметить выполненным, по одному заказу за раз
(`pendingOrderId`), с баннером отказа последнего действия, который сам
чистится в начале следующей попытки. Новая строка в профиле — сразу после
«Мои услуги», без условий видимости (так же, как и она).

**Урок из ревью #255, применённый здесь:** `updateOrderStatus` возвращает
`ApiResult<Unit>`, а не разобранный `FreelancerOrder` — ответ `PUT .../status`
без `id` был бы неотличим от отказа, хотя статус на сервере уже сменился (та
же логика, что у `saveMyService` из #173). Источник правды после действия —
список, перечитанный у сервера, а не оптимистичная правка на клиенте.

`docs/API-CONTRACT.md` и `docs/UI-INVENTORY.md` обновлены: входящие заказы
подключены, но черновиком — ни путь, ни тело смены статуса живым запросом не
проверены (`CONTRACT_REFRESH_TOKEN` не был задан).

**Тесты:** `FreelancerRepositoryTest` — страница входящих заказов, тело и
путь смены статуса, ответ без `id` не считается отказом, пустой `orderId` не
уходит в сеть, `401` — отказ. `MyFreelancerIncomingOrdersViewModelTest` —
загрузка/перечит/догрузка/дедуп (тот же контракт, что у
`MyFreelancerOrdersViewModelTest`), смена статуса для всех трёх действий,
повторный клик во время ожидания игнорируется, отказ действия не стирает
список, баннер отказа чистится с началом новой попытки.

**Прогнано:** `testDebugUnitTest`, `assembleDebug`, `lintDebug` — зелёные.

---

## «Еда»: доставка в корзине — с сервера, а не ноль (issue #179, 2026-09-10)

**Что было.** `CartTotals` объяснял ноль в строке доставки тем, что «до
оформления бэкенд не сообщает стоимости доставки заведения». На 2026-09-09 это
перестало быть правдой: `GET food/delivery-fee?itemsAmount=` отвечает
анонимно и зависит от суммы позиций. Человек подтверждал заказ, видя итог без
доставки, а платил больше — для чека это не косметика.

**Что сделано.**

- `FoodApi.deliveryFee()` — `GET food/delivery-fee`. Схема ответа —
  `ApiResponseMapStringLong`, то есть `data` это **карта**: сумма берётся по
  ключу `deliveryAmount`, отсутствие ключа — «доставка неизвестна», а не
  ошибка разбора. Обе суммы в тийинах (issue #149): `itemsAmount` уходит через
  `Money.somToTiyin`, ответ читается `tiyinToSom` в
  `DefaultDeliveryFeeRepository` — и только там.
- `DeliveryFeeLoader` — один загрузчик на корзину и чекаут: **первый запрос
  сразу**, повторные — с задержкой 400 мс (иначе «+»/«−» дают запрос на каждый
  тик), та же сумма подряд не запрашивается. Отдаёт `Long?`, где `null` —
  «неизвестна». Изменение суммы **сначала стирает прежнюю цену**: доставка
  зависит от суммы позиций, и «старая цена на новую корзину» — то же
  расхождение с чеком, из-за которого задача и появилась.
- Корзина показывает разбивку «Блюда + Доставка + Итого», когда доставка
  платная; чекаут — прежней строкой `checkout_delivery_fee`. Доставка входит
  в итог, и **баланс кошелька теперь проверяется против итога с доставкой**:
  раньше денег «хватало» на позиции, а сервер отказывал.
- Только при `fulfillment = DELIVERY`: переключение на самовывоз убирает и
  строку, и цену из итога и не тратит запрос. Возврат к доставке
  переспрашивает.

**Границы и риски.**

- **Значения карты разбираются как `JsonElement`, а не `Long`.**
  `ignoreUnknownKeys` карту не страхует — у неё не бывает «неизвестных
  ключей», — поэтому одно чужое значение (`"currency":"UZS"`,
  `"deliveryAmount":null`) уронило бы разбор всего ответа, то есть убрало бы
  доставку из всех корзин. Не число под ключом — та же «доставка неизвестна».
- **Оценка блокирует оформление кошельком.** Если `delivery-fee` завысит
  доставку относительно расчёта в `POST food/orders`, человек с достаточным
  балансом получит «не хватает N» и не сможет оформить. Обратный вариант —
  проверять баланс по одним позициям — кончался бы отказом сервера после
  нажатия кнопки. Неизвестная доставка при этом не блокирует ничего.
- **Это оценка, а не счёт.** Итог заказа считает сервер; экран статуса берёт
  суммы из `GET orders/{orderId}` и с этой оценкой их не смешивает. Если
  `delivery-fee` и расчёт в `POST food/orders` разойдутся, расхождение увидит
  человек — заметить это можно только на стенде под токеном.
- **Отказ запроса — не экран ошибки.** Доставка становится неизвестной, итог
  показывается по позициям, оформление остаётся доступным. Прежняя цена при
  этом не остаётся: доставка зависит от суммы позиций (на стенде от 2 000 сум
  бесплатна), и «старая цена на новую корзину» — то же расхождение, из-за
  которого задача и появилась.
- **Ноль отдельной строкой не рисуем**: «Доставка 0» читается как ошибка
  расчёта, а не как «бесплатно». Отдельного текста про бесплатную доставку
  нет — это уже про продукт, не про контракт.
- **В корзине способа получения нет**, он выбирается на следующем экране,
  поэтому там доставка считается нужной (`CheckoutForm.method` по умолчанию —
  доставка). Для самовывоза человек увидит цену на один экран раньше, чем она
  исчезнет.
- **Одежда сюда не входит**: у `food/delivery-fee` нет ни параметра
  заведения, ни магазина, и ничто в схеме не говорит, что она считает
  доставку одежды. Комментарий у `fashion_checkout_delivery_note` остаётся
  верным.
- **Промокод сюда не входит**: поля `promoCode` у `POST food/orders` нет
  (в отличие от `POST fashion/orders`) — issue #180.

**Проверено.** `testDebugUnitTest`, `lintDebug`, `assembleDebug` — зелёные.
Новое покрыто: на `MockWebServer` — разбор `{"deliveryAmount":10000}` → 100 сум
и `itemsAmount` в тийинах, ответ без ключа, чужое значение в той же карте,
нечисловая доставка, ноль как бесплатная доставка, 500 как отказ;
`DeliveryFeeLoaderTest` — первый запрос без задержки, серия изменений одним
запросом, дедуп той же суммы, стирание прежней цены при смене суммы, пустая
корзина и самовывоз без запроса, повтор после отказа; в ViewModel — рост итога
ровно на цену доставки, самовывоз без строки и без запроса, проверка кошелька
против итога с доставкой, отказ сети без блокировки оформления. Четыре
правила проверены мутацией (обнулённая задержка, итог без доставки,
дебаунс на первом запросе, оставленная прежняя цена — все красные).

**Ревью диффа субагентом** (свежий контекст, как требует `CLAUDE.md`) нашло
пять вещей, которые здесь и исправлены: прежняя цена в итоге на время
запроса, дебаунс на первом запросе, `Map<String, Long>` как хрупкий разбор,
KDoc, обещавший дедуп там, где его нет, и три пробела в тестах. Остались
осознанными: `needed = true` в корзине (способа получения там ещё нет) и
блокировка оформления по оценке — см. риски выше.

**Что осталось.** Строку «Доставка» в корзине **никто не видел глазами**:
превью у `CartScreen` нет вовсе (как и у остальных экранов «Еды»), эмулятора
в CI нет. Счётчик путей в `docs/UI-INVENTORY.md` заодно пересчитан: было 77,
стало 81 — разошёлся он ещё до этой задачи.

---

## Этап: `AuthRepository.refresh()` убран (issue #199)

Ревью PR #196 (issue #138) заметило второй путь обновления токенов —
`DefaultAuthRepository.refresh()`, который расходился с правилами
`TokenAuthenticator.rejectsSession()`: стирал сессию только на `Unauthorized`
(игнорируя `Forbidden`/`Business`/400/422) и не слал `SessionExpiry`, то есть
после стирания сессии человек оставался внутри приложения перед 401 на каждом
запросе.

**Метод не вызывал никто.** `RootViewModel` и все прочие потребители
`AuthRepository` в проде используют только `isAuthorized`; `refresh()` был
только в интерфейсе, `DefaultAuthRepository`, `FakeAuthRepository` и
собственных тестах. Раз явной необходимости в отдельном пути на практике не
нашлось, решено не тащить в него общую логику `rejectsSession()`, а убрать
расходящуюся копию правил целиком — вместе с четырьмя тестами
(`AuthRepositoryTest`), которые проверяли только её.

`auth/refresh` как эндпоинт остаётся: его по-прежнему зовёт `TokenAuthenticator`
по 401 — контракт (`docs/API-CONTRACT.md`) не менялся.

**Заодно.** Чистый `main` (`d46efb7`) не собирался — `compileDebugKotlin`
падал на `PromotionsRepository.kt` (`Unresolved reference 'PromoCheckResult'`
и связанные ошибки в `FashionApi.kt`/`FashionCheckoutViewModel.kt`), это уже
описано отдельно как issue #300 (регрессия мерджа issue #221 поверх issue
#180). Готовый точечный фикс на 4 файла уже был в PR #302 (issue #198) —
перенесён и сюда, иначе эту задачу было бы нечем проверить сборкой.

**Проверено.** `testDebugUnitTest` и `assembleDebug` — зелёные.

**Правки по ревью.** Перенесённый promo-фикс (см. «Заодно» выше) скопировал
код из PR #302, но не его правку `docs/API-CONTRACT.md` — контракт остался
утверждать, что `FashionPlaceOrderRequestDto` не шлёт `promoCode`, хотя код
уже шлёт. Исправлено тем же текстом, что и в #302. Оставлено как есть:
auth-удаление дословно совпадает с частью PR #270 (issue #197) — оба
убирают один и тот же метод теми же строками, но #270 сверх этого чинит
`TokenAuthenticator` для #197/#198, а эта задача (#199) такой цели не
ставила. Закрывать/мёржить чужой PR или схлопывать issue #199 с #197 —
решение мейнтейнера, не этой задачи; конфликт при мердже — тривиальный
(идентичное удаление), не смысловой.

---

## issue #300 — `main` снова не собирался: `check(...)` продублирован, а не потерян

Диагноз в issue устарел к моменту работы: 4 файла точечного promo-фикса
(issue #180 поверх #221, впервые описанные как «потерянные» в #300) к этому
моменту уже попали в `main` дважды — независимо, через issue #199 (см.
раздел выше, «Заодно») и через PR #307 (issue #286). Оба переноса
скопировали один и тот же код, и обычный мердж-механизм `git` не считает
это конфликтом: `check(...)` оказался объявлен дважды в интерфейсе
`PromotionsRepository`, дважды переопределён в `DefaultPromotionsRepository`
и в `FakePromotionsRepository` — `compileDebugKotlin` падал на
`Conflicting overloads` / `Overload resolution ambiguity`, а не на
`Unresolved reference`, как в исходном описании issue.

Исправление — убрать вторые (дословно идентичные) объявление/переопределения
в `PromotionsRepository.kt` и `FakePromotionsRepository.kt`.
`PromotionsRepositoryTest.kt` и `promoCode` у `FashionPlaceOrderRequestDto`
дублирования не получили, `docs/API-CONTRACT.md` уже отражал `promoCode`
(правка ещё из issue #199) — трогать не пришлось.

**Проверено.** `testDebugUnitTest` (2677 тестов), `assembleDebug` и
`lintDebug` — зелёные.

---

## Этап: доводка issue #236 — три PR с тийинами доехали чисто, контракт врал про исходящие поля

Issue #236 завели, когда #158, #161 и #173 были ещё открыты и написаны до
ответа бэкенда про тийины (#233): один не собрался бы, два показали бы суммы в
сто раз больше. К 2026-09-18 все три смержены, правки вносились в их ветки —
**кодовой работы по issue не осталось**. Этап — аудит результата и зачистка
того, что аудит нашёл.

**Аудит `main` (только чтение, сверка с `origin/main`):**

- чтение: `SubscriptionMappers`, `BusinessMappers`, `FreelancerMappers` — все
  `*Sum` через `tiyinToSom()`/`toSomOrZero()`. Класса `WalletAmounts` в коде
  нет; имя осталось только как объяснение, почему единицу больше не выводят из
  пары (`Money.kt`, контракт, журналы);
- запись: прошёл по **всем** `*Request`-DTO проекта. Денежные поля есть у
  семь денежных полей в шести телах запросов плюс две query — итого девять
  ручек, считая `PUT` изменения услуги мастера. Все через `Money.somToTiyin`.
  Остальные
  запросы (заказы еды и одежды, бронь, кино, игровые зоны, врачи, заказ
  мастеру, подписка) денег не шлют вовсе: состав и цену считает сервер;
- метрики дашборда: `BusinessDashboard.from` делит по `MONEY_HINTS` ровно один
  раз, второго пересчёта в маппере нет;
- тесты: конвертация закреплена во всех трёх вертикалях, в обе стороны.
  Подписки — `4 900 000 → 49 000`; бизнес — `unitPrice 32 000 × 2 = 64 000
  тийинов → 640 сум` на чтении и `32 000 сум → 3 200 000` на отправке; мастера
  — `5 000 000 → 50 000` на чтении (`hourlyRate` и `priceAmount`) и
  `80 000 сум → 8 000 000` на отправке;
- открытые PR на тот же класс бага проверены: #306 (одежда) уже шлёт
  `basePrice`/`price` через `somToTiyin`, #253 денег не касается.

**Что оказалось неверным — `docs/API-CONTRACT.md`.** Документ читают перед
правкой любого `*Api.kt`, а он утверждал:

- «`somToTiyin`, сейчас это только `POST wallet/top-up`» — к этому моменту
  исходящих денежных полей было семь в шести телах запросов плюс две query,
  девять ручек всего. Формулировка подсказывала следующей вертикали, что
  переводить нечего;
- у `PaymentsApi` — «без пары `amountSom`, поэтому единицу `amount` вывести
  нечем… **проверить первым же живым ответом**». Вывод верен, обоснование —
  нет: это логика удалённого `WalletAmounts`, а вопрос закрыт ещё #149.

Оба абзаца переписаны: перечислены все исходящие поля с путями ручек,
`hourlyRate` отмечен как единственный `int32` (перевод через
`Math.toIntExact` — чтобы переполнение падало, а не обрезалось молча). Тот же
устаревший довод убран из комментария в `SubscriptionRepositoryTest`.

**Грабли.** Issue #236 сам себя пережил: пока он висел, все три PR смержились
с уже внесёнными правками, и два прошлых прогона отчитались «чинить нечего» —
верно по букве. Но исходный баг #149 родился ровно из такой же устаревшей
фразы в контракте, и она успела вырасти заново в том же файле. Проверять
стоило не только код, но и документ, который этот код описывает.

---

## Этап: карточки фильма, билета, записи и приёма — детали вместо снимка из списка (issue #183)

Четыре ручки чтения по `id` были объявлены неравномерно: `hospitals/appointments/{id}`
уже существовала в `HospitalApi` с issue #181, но ни один экран её не звал;
`cinema/movies/{id}`, `cinema/tickets/{id}` и `appointments/{id}` не были
объявлены вовсе. Экран фильма при этом искал фильм в общей афише вместо
чтения по id, а у билета, записи и приёма своих карточек не было совсем —
«мои активности» вели в никуда.

**Контракт пересверен по живому `/v3/api-docs`** (2026-09-19): все четыре
ручки существуют и требуют Bearer. Заодно найден дрейф схемы — билет теперь
называется `TicketResponse` (была `CinemaTicket`), а он сам,
`AppointmentBookingResponse` и `HospitalAppointmentResponse` получили новую
пару полей `placeName`/`placeLogoUrl`. Задокументировано в
`docs/API-CONTRACT.md`, не разобрано в этом PR — отдельная задача: если
бэкенд теперь называет заведение сам, `PlaceNameResolver` (issue #182) и
подтягивание имени врача (issue #219) могут оказаться избыточными для этих
трёх ответов.

**Что сделано:**

- `CinemaApi.movie(id)`/`ticket(id)`, `BookingApi.appointment(id)` — новые
  ручки; `HospitalApi.appointment(id)` уже была, просто не использовалась;
- `AppointmentsSource.appointment(id)` — общий метод для `BookingRepository` и
  `HospitalRepository`, тем же приёмом, что `myAppointments`/`cancel`: экран
  карточки записи один на обе вертикали;
- `MovieViewModel` теперь читает фильм по id вместо поиска по всей афише;
- заодно у карточки фильма показан постер (`MahallaAsyncImage`, кит уже умел
  грузить картинки — просто не был подключён здесь) и кнопка «Смотреть
  трейлер» — оба поля были в домене с начала вертикали, но экран их не
  показывал;
- два новых экрана — `TicketScreen` и `AppointmentScreen` (общий на обе
  вертикали, как «Мои записи»): читают по id при открытии и на
  pull-to-refresh, без отмены/переноса — эти действия остаются в списках;
- `HospitalRepository.appointment()` дотягивает имя врача тем же
  `withDoctorNames`, что и список, — третий потребитель после
  `myAppointments` и «моих активностей»;
- «Мои активности» ведут на новые карточки: `ActivityTarget.CinemaTicket`,
  `MasterAppointment`, `DoctorAppointment` — списки «Мои билеты»/«Мои записи»
  на карточки не ведут, там уже показано то же самое строкой;
- тесты на разбор, 404 и отказ сети для всех новых методов репозиториев и
  ViewModel'ей новых экранов.

**Грабли.** Задача уже была на 2026-09-10, но оркестратор упал на первом же
пункте плана — в `main` не доехало ничего, кроме самой ручки
`hospitals/appointments/{id}` (issue #181, самостоятельная задача). Контракт
пришлось сверять заново, а не доверять прошлому прогону: за девять дней он
успел разойтись (дрейф схемы выше).

## issue #228 — аналитика: доводка второго ревью, пп. 3, 5, 6 (2026-09-19)

Первый прогон на эту задачу упал на шаге прогона тестов/PR (краш
оркестратора 2026-09-13), и ветка не доехала до `main` — пп. 1, 2, 4 уже были
закрыты раньше коммитом `36b44da` (PR #227), но три оставшихся пункта
провисели неделю. Закрыты кодом, не только текстом:

- **п. 3** (флейк-тест). `AnalyticsTrackerTest` проверял, что трекер из
  `AnalyticsModule` шлёт события на настоящем `Dispatchers.IO`, опросом
  `System.currentTimeMillis()` + `Thread.sleep(5)` до 5 секунд — на
  загруженном раннере это могло покраснеть без изменений в коде, а созданная
  в тесте область никогда не отменялась. Область вынесена в отдельный
  `@Provides` (`provideAnalyticsScope`, квалификатор `@AnalyticsCoroutineScope`
  в `AnalyticsModule`), тест берёт её напрямую, ждёт доставку через
  `CountDownLatch` (сигнал, а не опрос) и отменяет область в `finally`.
- **п. 5** (пустой `storeId` в «Одежде»). Разбор показал, что бага нет:
  `FashionCart.store(storeId)` ищет точное совпадение, и на пустом id вернёт
  `null` раньше, чем дело дойдёт до отправки — корзина останется пустой,
  `CheckoutValidator` заведёт `CheckoutError.EmptyCart`, и `submit()` не
  дойдёт ни до заказа, ни до аналитики. Инвариант закреплён в KDoc
  `FashionCheckoutViewModel.storeId` (с честной оговоркой про
  `CartItemDto.toDomain` — тот тоже пишет `storeId.orEmpty()`, то есть
  строка с пустым `storeId` в принципе возможна при `storeId: null` от
  сервера, но реальная навигация не даёт второму условию совпасть) и
  регрессионным тестом на отсутствующий аргумент маршрута.
- **п. 6** (`core` тянет `data`). Интерфейс `AnalyticsRepository` переехал в
  `core/analytics/AnalyticsRepository.kt`; `data/network/analytics/AnalyticsRepository.kt`
  переименован в `DefaultAnalyticsRepository.kt` и содержит только
  реализацию. `core/analytics/AnalyticsTracker.kt` больше не зависит от
  `data`.

П. 1 (аналитика может разлогинить пользователя фоновым событием) остаётся
архитектурным вопросом — решение зафиксировано в KDoc `AnalyticsModule`,
кода не требует, решение о выделенном клиенте без `TokenAuthenticator` — за
командой.

Проверено: `testDebugUnitTest` (2854 теста, 0 падений — один ре-ран
понадобился из-за не связанного с этой правкой флейка в
`NotificationSettingsViewModelTest`, воспроизводится и без изменений из этого
коммита), `assembleDebug`, `lintDebug` — все зелёные. Ревью диффа свежим
субагентом находок-блокеров не дало.

---

## Этап: бизнес-панель одежды — товар и вариант (issue #280, продолжение #252)

Из #252 сделаны аптека (PR #260) и акция заведения (PR #279); оставалась
одежда — резалась отдельной задачей, потому что `FashionApi` целиком отмечен
в `docs/API-CONTRACT.md` как ⚠️ не сверенный, в отличие от аптеки, чей `GET`
был подтверждён живыми curl'ами до того, как в #252 туда добавился `POST`.

**Попутно найдена и исправлена регрессия сборки `main`**, не связанная с
#280: мердж `7f60ae5` («Merge branch 'main' into claude/issue-252…») вернул
`main` в состояние до issue #180 — пропали `PromotionsRepository.check()`,
поле `FashionPlaceOrderRequestDto.promoCode` и закрывающая скобка в
`PromotionsRepositoryTest.kt` (уже описано отдельно в issue #298).
`compileDebugKotlin` падал ещё до старта этой задачи. Восстановлено по уже
существующим потребителям (`FashionCheckoutViewModel`, `FakePromotionsRepository`,
старые тесты) — контракт не выдумывался заново, только собран обратно из
того, что уже ссылалось на пропавшие символы.

**Что сделано.**

- `FashionApi.createProduct()` — `POST fashion/stores/{storeId}/products`, и
  `FashionApi.createVariant()` — `POST fashion/products/{id}/variants`. Поля
  запросов выведены **по аналогии с уже подтверждёнными полями того же
  контроллера**, не угаданы по другой вертикали: товар — как у
  `ProductDetail`/`ProductSummary` (`name`, `description`, `brand`,
  `material`, `careInstructions`, `sizeGuide`, `gender`, `categoryId`,
  `basePrice`), вариант — как у `VariantResponse` (`colorName`, `colorHex`,
  `size`, `sku`, `price`, `stockQuantity`). Цены — в тийинах (issue #149),
  перевод из сум делает репозиторий.
- `NewFashionProductDraft`/`NewFashionVariantDraft` — черновики форм, тот же
  приём, что у `NewPharmacyProductDraft`: цифры цены/остатка выбираются из
  строки, а не парсятся как есть; пустая цена (`0`) — валидная (бесплатный
  образец), нечисловая — ошибка поля.
- Форма товара — на витрине магазина (`FashionCatalogScreen`), форма
  варианта — на карточке товара (`FashionProductScreen`): по одной кнопке
  «+» в каждой, видимой только владельцу/менеджеру. Категория и «кому вещь»
  выбираются чипами (категория — из уже загруженного справочника витрины,
  повторного запроса нет); повторный тап по выбранному чипу снимает выбор.
  Успешное создание закрывает шторку и перечитывает список/карточку целиком
  — новый товар или вариант получает `id` от сервера, а не собирается из
  черновика на клиенте (тот же приём, что у аптеки).
- Доступ — `MyPlace.canManageProducts` расширен на `PlaceCategory.Fashion`
  (была только аптека); категория решает адрес назначения кнопки «Управлять
  товарами» в «Моих заведениях» — `MyPlacesViewModel.manageProducts()`
  ветвится на `OpenPharmacyManagement`/`OpenFashionManagement`. Флаг `isOwner`
  едет маршрутом (`FashionCatalogRoute`, затем `FashionProductRoute`) от
  «Моих заведений», где принадлежность уже подтверждена по `places/my`, —
  тот же приём, что у `PharmacyRoute.isOwner`: у товара магазина нет своего
  `ownerId`, чтобы проверить это на месте.

**Границы и риски.**

- **Обе новые ручки не проверены живым запросом.** Обе требуют Bearer
  владельца/менеджера заведения, `CONTRACT_REFRESH_TOKEN` в песочнице не
  задан, `401` приходит до валидации тела. Риск и предположения — в
  `docs/API-CONTRACT.md` в разделе `FashionApi`; при расхождении смотреть
  туда в первую очередь, не гадать заново.
- **Ответы обеих ручек не разбираются дальше `Unit`.** Как и у аптеки —
  безопаснее не угадывать точную схему ответа (`ProductDetailDto`/
  `VariantDto` взяты по аналогии, но поля именно в ответе `POST` не
  подтверждены), успех проверяется по коду ответа, а список/карточка
  перечитываются отдельным `GET`.
- **Загрузка фото не входит** (issue #101) — как и у аптеки с акцией.
- **Превью новых шторок никто не видел глазами**: эмулятора в CI и в
  песочнице нет, превью `@ThemeLanguagePreviews` — единственная проверка
  вёрстки под четыре комбинации темы/языка.

**Проверено.** `testDebugUnitTest` (2648 тестов), `lintDebug`, `assembleDebug`
— зелёные. Новое покрыто: `NewFashionProductDraftTest`/
`NewFashionVariantDraftTest` — обязательность полей, разбор цифр, свободный
образец с нулевой ценой, повторный тап снимает выбор пола/категории;
`FashionRepositoryTest` — тело обеих ручек (конвертация сум→тийин, пустые
опциональные поля отсутствуют, а не `null`), отказ до сети на невалидном
черновике и на пустом `storeId`/`productId`; `FashionCatalogViewModelTest`/
`FashionProductViewModelTest` — форма скрыта не-владельцу, невалидный черновик
не уходит в сеть, успех закрывает форму и перечитывает список/карточку, отказ
сервера оставляет форму открытой с его текстом; `MyPlaceTest` — доступ теперь
верен и для аптеки, и для одежды, но не для остальных категорий;
`MyPlacesViewModelTest` — категория решает, какая витрина открывается;
`RoutesSerializationTest` — новый аргумент `isOwner` у обоих маршрутов одежды
совпадает с тем, что читают ViewModel'и.

**Ревью диффа субагентом** (свежий контекст, как требует `CLAUDE.md`) —
проведено перед PR.

**Что осталось.** Обе ручки — кандидаты на сверку живым curl'ом, как только
`CONTRACT_REFRESH_TOKEN` появится в песочнице (см. issue #294 — там та же
судьба у акции). `docs/UI-INVENTORY.md` в этой задаче не пересчитывался: обе
формы — не новые экраны, а действия на уже существующих.

---

## issue #280 — доводка первого круга авто-ревью PR #306

Авто-ревью нашло расхождение между кодом и собственным описанием PR: текст
выше обещал «успех проверяется по коду ответа», а `FashionRepository.createProduct`/
`createVariant` на деле разбирали тело `POST`-ответа в `ProductDetailDto`/
`VariantDto` через `.payload()`. У ручек, не сверенных живым запросом, это
двойной риск — `payload()` падает и при `success: false`, и при успешном
ответе без `data` (`ApiResponseVoid`), и при несовпадении схемы тела; клиенту
для `ApiResult<Unit>` ничего из разобранного DTO не нужно.

Исправлено по образцу `BusinessRepository`/`BusinessApi` (issue #288,
`toggleItem`/`deleteItem`): `FashionApi.createProduct()`/`createVariant()`
теперь отдают `ApiResponse<JsonElement>`, `FashionRepository` проверяет ответ
`ensureSuccess()`, а не `payload()`. Тесты (`FashionRepositoryTest`) не
менялись — они и раньше смотрели только на тело запроса. Заодно
`docs/API-CONTRACT.md` дополнен явной строкой про это для обеих ручек.

Остальные пункты ревью (else-ветка `MyPlacesViewModel.manageProducts()`,
`MAX_NAME_LENGTH` у `NewFashionProductDraft`, `verticalScroll` формы,
мёртвая ветка `ProductGender.Unknown`, счётчик в `docs/UI-INVENTORY.md`) —
не блокеры по формулировке самого ревью, оставлены как есть.

**Проверено.** `./gradlew testDebugUnitTest` и `./gradlew assembleDebug` —
зелёные.
