<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Геолокация (эпик 3.6): «рядом со мной» и время доставки. Отказ не
         блокирует приложение — город выбирается вручную. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Вход по биометрии (эпик 3.5); датчик не обязателен, фолбэк — PIN. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Пуши (эпик 11). С API 33 показывать уведомления без этого разрешения
         нельзя; спрашивается оно не на старте, а там, где человеку понятно
         зачем — в центре уведомлений и в настройках уведомлений. На API 26–32
         разрешения не существует, система выдаёт его при установке. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-feature
        android:name="android.hardware.fingerprint"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />

    <!-- Вход через Telegram-бот (issue #46). Начиная с API 30 чужие пакеты
         невидимы без этого блока: без него PackageManager отвечает «Telegram
         не установлен» на устройстве, где он есть, и приложение уводило бы
         человека на платное SMS. Схема tg: — общее определение
         Telegram-клиента (ловит и форки), список пакетов — запасной путь для
         прошивок с урезанным резолвером. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tg" />
        </intent>
        <package android:name="org.telegram.messenger" />
        <package android:name="org.telegram.messenger.web" />
        <package android:name="org.thunderdog.challegram" />
        <package android:name="org.telegram.plus" />
    </queries>

    <application
        android:name=".MahallaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mahalla"
        tools:targetApi="33">

        <!-- Отчёты о падениях (issue #74). Sentry умеет подниматься сам из
             ContentProvider по DSN в манифесте — так нельзя: DSN приходит из
             секрета сборки, отчёт обязан пройти вычистку секретов
             (CrashScrubber), а в debug сбор по умолчанию выключен. Всё это
             решает MahallaApplication, поэтому автозапуск отключён. -->
        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <!-- Пуши (эпик 11). Канал по умолчанию нужен для сообщений с блоком
             `notification`: их в фоне показывает сама библиотека, минуя
             MahallaMessagingService, и без этого значения уведомление уехало
             бы в канал «Прочее», созданный библиотекой со своим названием.
             Иконка и цвет — по той же причине: у сообщения, показанного
             библиотекой, их взять больше неоткуда. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="other" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <service
            android:name=".feature.notifications.push.MahallaMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Mahalla.Splash"
            tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep links (эпик 1.2, расширены в эпике 11). Хосты перечислены
                 в одном фильтре: у них общие action и категории, а разные
                 фильтры на ту же схему отличались бы только строкой хоста.
                 Шаблоны и их соответствие маршрутам — в DeepLinks. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="mahalla" />
                <data android:host="place" />
                <data android:host="order" />
                <data android:host="notifications" />
                <data android:host="subscription" />
            </intent-filter>
        </activity>
    </application>

</manifest>
