# Бэклог задач для Android

Спецификации задач, на которые ссылаются ссылки «создать issue в один клик»
из issue #59. Каждый раздел — одна задача: **проблема** (со ссылками на код и
на живую проверку), **что сделать**, **критерий готовности**.

Снимок сделан **2026-09-09**. Замеры в файле — не пересказ документов, а
результат прогона на этой дате; если расходятся с кодом, верен код.

- Инвентаризация экранов и путей — `docs/UI-INVENTORY.md`.
- Контракт бэкенда — `docs/API-CONTRACT.md`. Что здесь названо «есть у
  бэкенда» — снято со `https://189-74-96-232.nip.io/v3/api-docs` и, где
  указано, подтверждено живым `curl`.

---

## Что сначала: узкое место — не бэклог

Это первое, что нужно прочитать в файле, иначе задачи ниже пойдут не в ту
очередь.

Замер 2026-09-09: в репозитории **13 открытых PR примерно на 32 000 строк**, и
почти всё крупное, чего «нет в приложении», в них уже написано и покрыто
тестами:

| PR | Что внутри | Состояние |
|---|---|---|
| #164 | эпик 13: R8, подпись, скриншот-тесты темы (Roborazzi), Baseline Profile | MERGEABLE / CLEAN |
| #161 | бизнес-панель: дашборд, очередь барбера, заказы, меню (11 ручек) | MERGEABLE / CLEAN |
| #159 | FCM: каналы, разрешение, deep links | MERGEABLE / CLEAN |
| #158 | подписки: состояния, продление, история списаний | MERGEABLE / CLEAN |
| #171 | инвентаризация проекта (#36) | MERGEABLE / CLEAN |
| #156 | оплата из кошелька: идемпотентность, отказы | UNSTABLE (красная проверка) |
| #152 | «Мои активности» (#73) | **CONFLICTING** |
| #110 | «Мои активности» — **вторая попытка той же задачи**, что #152 | требует решения |
| #120, #78, #72 | PIN и app-lock (#102), соцфункции (#75), формы услуг (#71) | статус не пересчитан |
| #41 | `lintDebug` + `warningsAsErrors` | **устарел**: в `main` это уже есть |
| #38 | тестовый `HELLO.md` | закрыть |

Практический вывод: **новая волна задач сейчас увеличивает не готовность, а
объём непроверенного запаса.** Слить пять чистых PR дешевле и полезнее, чем
написать шестую фичу. Поэтому задачи ниже — узкие и намеренно не пересекаются
ни с одним открытым PR (сверено по диффам, см. «Как это проверялось»).

---

## Волна 2026-09-09

Шесть задач. Все шесть проверены по коду, четыре — ещё и живым запросом на
стенд. Ни одну не закрывает ни открытый issue, ни открытый PR.

Порядок: сверху то, что даёт больше за меньшие деньги.

---

### T14. Стоимость доставки: `GET food/delivery-fee`

**Проблема.** Заведение показывает человеку итог, который потом вырастет.

`app/src/main/java/uz/mahalla/feature/food/domain/Cart.kt:48-49` прямо
объясняет, почему доставки нет в итоге корзины:

> Скидка и доставка приезжают из ответа о заказе (`OrderView`): до оформления
> бэкенд не сообщает ни стоимости доставки заведения, ни способа приложить к
> заказу промокод.

**Это утверждение больше не верно.** Живая проверка 2026-09-09:

```
GET /api/v1/food/delivery-fee?itemsAmount=50000
X-Geo-Lat: 41.311081   X-Geo-Lng: 69.240562
→ 200 {"success":true,"data":{"deliveryAmount":10000}}
```

Ручка отвечает **анонимно**, схема — `ApiResponseMapStringLong`, то есть
`data` это карта, а не DTO с фиксированными полями. Тот же разрыв у одежды:
`feature/fashion/ui/checkout/FashionCheckoutScreen.kt:151` — «Стоимость
доставки бэкенд называет только в ответе о созданном заказе».

Цена дефекта: при доставке человек подтверждает заказ, видя сумму на 10 000
сум меньше настоящей. Для чека это не косметика.

**Что сделать.**

1. `FoodApi`: `@GET("food/delivery-fee") suspend fun deliveryFee(@Query("itemsAmount") itemsAmount: Long): ApiResponse<Map<String, Long>>`.
   `data` разбирать как карту и читать ключ `deliveryAmount`; отсутствующий
   ключ — не ошибка экрана, а «доставка неизвестна».
2. Запрашивать при выборе способа получения «доставка» и при изменении суммы
   позиций — не на каждый тик корзины (иначе запрос на каждое «+»).
3. Показать строкой в итоге корзины и в `FashionCheckoutScreen`; при отказе
   сети — прежнее поведение (доставка не показана), а не экран ошибки: чек
   должен оставаться оформляемым.
4. Убрать устаревшую часть комментария `Cart.kt:48-49` — иначе следующий агент
   снова поверит, что ручки нет.
5. Единица (сумы или тийины) — тот же открытый вопрос, что в #149; в задаче
   ссылаться на него, а не решать заново.

**Критерий готовности.** В корзине с доставкой видна её стоимость; тест на
`MockWebServer` разбирает `{"deliveryAmount":10000}` и проверяет, что итог
вырос ровно на 10 000; тест на пустую карту — итог как раньше, экран не
падает. `testDebugUnitTest`, `lintDebug`, `assembleDebug` — зелёные.

---

### T15. Промокод в чеке: `GET promotions/check`

**Проблема.** Приложение показывает промокод и не даёт его применить.

`feature/promotions/ui/PromotionCard.kt:85` рисует
`stringResource(R.string.promo_code, code)` — то есть человек видит `OSH20`.
Поля ввода промокода нет ни в `feature/food` (checkout), ни в
`feature/fashion/ui/checkout`. Выдуманный `POST places/{id}/promo` из эпика 5
убран вместе с UI — и заменён ничем.

Живая проверка 2026-09-09:

```
GET /api/v1/promotions/check?code=OSH20&placeId=<uuid>&orderAmount=50000
→ 404 {"success":false,"error":{"code":"NOT_FOUND","message":"Promo-kod topilmadi: OSH20"}}
```

`404` здесь — **подтверждение**, а не отказ: ручка маршрутизируется и
проверяет код (на пустом стенде `OSH20` и не должен найтись). Параметры
обязательны все три: `code`, `placeId`, `orderAmount`. Ответ —
`CheckResponse { valid: boolean, discountAmount: int64, finalAmount: int64, promoCode: string }`.

**Что сделать.**

1. `PromotionsApi`: `@GET("promotions/check")` с тремя `@Query`, ответ
   `ApiResponse<PromoCheckDto>` по полям выше.
2. Поле ввода кода в чеке еды и одежды: применить → показать скидку и новый
   итог из `discountAmount`/`finalAmount` (а не считать самим — иначе клиент и
   сервер разойдутся на округлении).
3. `404 NOT_FOUND` и `valid = false` — **разные** сообщения: «такого кода нет»
   и «код есть, но к этому заказу не применяется». Обе строки — в `values/` и
   `values-ru/`.
4. `PromotionCard`: код сделать нажимаемым — подставлять его в поле чека, а не
   заставлять переписывать руками.
5. Итог не должен уходить в минус, если сервер вернул `discountAmount` больше
   суммы позиций: доверяем `finalAmount`.

**Критерий готовности.** Промокод вводится и применяется; тесты на
`MockWebServer` на три случая (`valid = true`, `valid = false`, `404`); отказ
не ломает оформление заказа без кода.

---

### T16. Слоты врача: `GET hospitals/doctors/{id}/slots`

**Проблема.** Запись к врачу идёт по времени, которое приложение не спрашивало
у сервера.

`feature/hospital/domain/Doctor.kt:41-51` фиксирует причину:

> **У больниц нет ручки свободных слотов.** В `hospital-controller` их четыре
> […] Выдать эту сетку за свободные слоты значило бы обещать от имени
> больницы.

Решение было правильным на 2026-09-04. **На 2026-09-09 у
`hospital-controller` девять путей**, и один из них — ровно тот, которого не
хватало:

```
GET /api/v1/hospitals/doctors/{id}/slots?date=2026-09-15
→ 401 UNAUTHORIZED        (ручка существует и маршрутизируется, нужен Bearer)
схема ответа: ApiResponseListString
```

`ApiResponseListString` — **та же форма**, что у слотов брони
(`barber-services/places/{placeId}/slots`, issue #97), то есть подбирать
разбор не нужно: он в проекте уже написан и покрыт тестами.

Цена: `POST hospitals/appointments` обязательно требует `date` и `startTime`
(`HospitalApi.kt:70`), а человек выбирает их из сетки, придуманной клиентом.
Итог — отказ сервера на подтверждении либо запись на время, которого у врача
нет.

Заодно у той же причины есть сосед — **issue #167** (у `hospitals` появилась
своя отмена, а клиент шлёт на общую `appointments/{id}/cancel`). Обе задачи
про одно: контроллер больниц вырос, а клиент об этом не знает. Делать их лучше
одним заходом.

**Что сделать.**

1. `HospitalApi`: `@GET("hospitals/doctors/{id}/slots")` с `@Path("id")` и
   `@Query("date")` (`yyyy-MM-dd`), ответ `ApiResponse<List<String>>`.
   Требует Bearer — API уже собран на основном Retrofit, менять клиент не
   надо (`HospitalApi.kt`, комментарий про два клиента).
2. `DoctorBookingViewModel`: грузить слоты на выбранную дату и на смену
   врача; время выбирать **только** из ответа. Переиспользовать разбор слотов
   брони, а не писать второй.
3. Пустой список — честное «на этот день свободного времени нет», а не пустая
   сетка.
4. Снять устаревший комментарий `Doctor.kt:41-51` и заменить его датой новой
   сверки.
5. `GET hospitals/doctors/{id}` (карточка врача) тоже появился — если он не
   нужен экрану, так и написать в комментарии, чтобы следующий не искал.

**Критерий готовности.** Время записи выбирается из ответа сервера; тесты:
слоты на дату, смена даты перезапрашивает, пустой список показывает «нет
времени», ошибка не роняет экран. Комментарий про «нет ручки слотов» в коде
отсутствует.

---

### T17. Сверка путей как тест: ловить появление ручек, а не только расхождение

**Проблема (и корень трёх задач выше).**

T14, T15 и T16 — это один и тот же дефект процесса, найденный трижды: в коде
стоит обоснованный комментарий «у бэкенда этого нет», бэкенд обзавёлся ручкой,
и **об этом никто не узнал**. Никакая проверка в репозитории на это не
смотрит.

Замеры, из которых видно, насколько это регулярно:

| Дата | Путей у бэкенда | Операций |
|---|---|---|
| 2026-09-02 | 125 | 136 |
| 2026-09-09 | **164** | **180** |

**39 новых путей за семь дней.** При этом харнесс сверки уже есть
(`contract/lib.sh`, `contract/booking.sh`, `.github/workflows/contract-check.yml`)
и ловит **расхождение формы** у путей, которые клиент уже знает. Появление
нового пути он не ловит по построению: чего в `*Api.kt` нет, то он и не
дёргает.

**Что сделать.**

1. Скрипт `contract/paths.sh`: снять `/v3/api-docs`, вынуть список
   `метод + путь` (плейсхолдеры нормализовать: `{id}`, `{placeId}` → `{}`),
   сложить в фикстуру `app/src/test/resources/contract/paths.json`.
2. Тест, который сверяет три множества:
   - **вызовы приложения, которых нет у бэкенда** — красный: это выдуманная
     ручка (ровно то, что дважды приводило к переделке вертикали);
   - **ручки бэкенда, которых нет у клиента** — не красный, а отчёт: их
     заведомо много (на 2026-09-09 — 103, и большинство админские);
   - **новые с прошлой фикстуры** — вот это и есть находка ради которой всё.
   Список вызовов клиента брать разбором `@GET/@POST/@PUT/@DELETE/@PATCH` по
   `app/src/main/**/*Api.kt`, а не вторым списком руками: второй список
   разъедется с первым.
3. Ожидаемые «пока не наши» пути — в файл рядом с тестом, с причиной по
   каждой группе (`admin/*` — админка платформы, не мобильное приложение).
   Тогда новый путь виден как diff в этом файле, а не как красный тест.
4. Записать в `.claude/rules/network.md`: комментарий «у бэкенда этого нет»
   ставится **с датой проверки**.

Замер на сегодня для стартовой фикстуры: **все 77 вызовов приложения
совпадают с бэкендом, выдуманных ручек — ноль.** То есть тест вводится на
зелёном, а не на технадолге. Заодно это подтверждает, что **#63 («Еда» под
реальный контракт) сделана**: `food/places/{placeId}/menu`, `POST food/orders`,
`POST food/orders/{orderId}/cancel` совпадают, `places/{id}/promo` из кода
убран, модификаторы разобраны (`feature/food/domain/Menu.kt:50`).

**Критерий готовности.** `testDebugUnitTest` содержит тест сверки путей; на
текущей фикстуре он зелёный; при подмене фикстуры на «клиент зовёт то, чего
нет» — краснеет (проверить обратным прогоном, как требует `rules/testing.md`).

**Ограничение.** Пункт `vertical` в выпадающем списке
`contract-check.yml` добавляет только человек: у GitHub App агента нет прав на
`.github/workflows`. Скрипт и тест от этого не зависят — они гоняются локально
и в `checks`.

---

### T18. Фотогалерея места и удаление своей фотографии: `GET media/entity/{id}`, `DELETE media/{id}`

**Проблема.** Загрузка фотографий есть, чтения и удаления нет.

`POST media/upload` в приложении используется (`MediaApi.kt`, issue #101), а
`GET media/entity/{entityId}` и `DELETE media/{id}` у бэкенда есть и клиентом
**не объявлены** — это же зафиксировано в `docs/API-CONTRACT.md`.

Что это значит для человека: карточка места показывает ровно две картинки —
`logoUrl` и `coverUrl` из `PlaceDetailDto`
(`feature/discovery/data/CatalogApi.kt:39-58`), хотя у сущности на сервере
может лежать полный набор. А фотографию, приложенную к своему отзыву, нельзя
убрать: удаления в клиенте нет.

**Что сделать.**

1. `MediaApi`: `@GET("media/entity/{entityId}")` и `@DELETE("media/{id}")`.
   Форму ответа снять живым запросом **до** написания DTO — это тот класс
   задач, где выдуманная схема стоила эпика (см. `AGENTS.md`).
2. Галерея на карточке места: горизонтальная лента поверх `coverUrl`,
   через `MahallaAsyncImage` (issue #60) — второй загрузчик не заводить.
3. Удаление — только своей фотографии и только с подтверждением.
4. Белый список схем (issue #139) распространить на адреса из галереи:
   `content://` и `file://` в `AsyncImage` доезжать не должны.

**Критерий готовности.** Галерея показывает набор с сервера и не ломается на
пустом; своя фотография удаляется; тесты на разбор ответа, пустой набор и
отказ удаления.

---

### T19. Первые инструментальные тесты: job `emulator` в CI зеленеет впустую

**Проблема.** В CI есть проверка, которая ничего не проверяет.

`.github/workflows/ci.yml` содержит job `emulator` (KVM, api-level 34,
x86_64), запускающий `./gradlew connectedDebugAndroidTest` по метке
`emulator`. При этом:

- каталога `app/src/androidTest` в репозитории **нет**;
- в `app/build.gradle.kts` **ни одной** `androidTestImplementation`.

Задача без источников проходит как `NO-SOURCE`, то есть job **зелёный и не
проверяет ничего**. Это хуже отсутствующей проверки: отсутствующая видна.

Отдельно: PR #164 (эпик 13) добавляет скриншот-тесты **на Roborazzi**, то есть
JVM-тесты в `app/src/test` — `androidTest` он не заводит, и job `emulator`
после его мержа остаётся таким же пустым. (Поэтому issue #165, просящий
добавить в CI шаг `verifyRoborazziDebug`, не устарел, а **ждёт мержа #164**:
до него такой задачи в проекте нет.)

Compose-тесты под Robolectric в проекте уже появились — один,
`app/src/test/java/uz/mahalla/core/ui/components/MahallaAsyncImageTest.kt`
(issue #137), с `androidx.compose.ui.test.junit4` и
`debugImplementation(ui-test-manifest)`. То есть половина обвязки готова.

**Что сделать.**

1. `androidTestImplementation` на `ui-test-junit4` + runner/rules; версии
   брать из существующего `compose-bom`, новых версий в
   `libs.versions.toml` не вводить (связка стека зафиксирована в `AGENTS.md`).
2. Первые тесты — на то, что нельзя проверить на JVM и что раз за разом
   попадает в «на устройстве не проверено»:
   - приложение стартует и доходит до первого экрана, не падая;
   - онбординг проходится до конца по фейковому бэкенду;
   - `MapCanvas` поднимается на x86_64 (нативный MapKit в APK для этой
     архитектуры есть — 27.7 МБ, замер ниже) либо честно показывает
     объяснение без ключа.
3. Не тащить в `androidTest` то, что уже покрыто на JVM: 1842 теста там не
   зря, дублировать их на эмуляторе — минуты в никуда.

**Критерий готовности.** `connectedDebugAndroidTest` гоняет непустой набор;
PR с меткой `emulator` показывает пройденные тесты, а не `NO-SOURCE`; ни один
JVM-тест не переехал.

---

## Чего намеренно НЕ предлагаю создавать

Самопроверка предложения — половина этой задачи, поэтому список отказов важнее
списка задач.

| Не создавать | Почему |
|---|---|
| бизнес-панель, дашборд, очередь барбера, статусы заказов, меню заведения | **написано в PR #161** — 11 ручек, включая `walkin/barber/dashboard`, `walkin/{id}/accept‑decline‑start‑complete`, `analytics/places/{id}/dashboard` (сверено по диффу) |
| FCM, каналы, разрешение, deep links | **написано в PR #159** |
| R8, подпись, ABI/App Bundle, `versionCode`, скриншот-тесты, Baseline Profile | **написано в PR #164** (`docs/RELEASE.md`, ADR 0006 и 0007) |
| `payments/transactions`, история списаний, автопродление | **написано в PR #158** |
| «Мои активности» | **написано дважды** — PR #152 и PR #110; сначала решить, какой из них живой |
| соцфункции (лайк, «сохранить», комментарии) | PR #78 + issue #105, блокировано #88 (`saved-places` отдаёт только UUID) |
| PIN, app-lock, `session/check`, `pin-resume` | PR #120 + issue #102 |
| профиль `users/me`, аналитика `analytics/track`, `places/map-bounds`, отмена у больниц | уже открыты: #170, #169, #168, #167 |
| эпики «кошелёк», «подписки», «профиль», «push», «бизнес-панель», «качество» | открыты: #12, #13, #14, #15, #16, #17 — задачи выше сделаны их слайсами, а не конкурентами |
| «переписать `FoodApi` под контракт» (#63) | **сделано**: все 77 вызовов совпадают с бэкендом, выдуманных ручек ноль |
| `admin/*` (~15 ручек: пользователи, места, тарифы, версии) | админка платформы, а не мобильное приложение |
| `payments/payme/callback`, `payments/click/callback` | вебхуки провайдера, приложение их не зовёт по определению |
| новые вертикали | восемь написаны; девятая до мержа очереди только увеличит очередь |

---

## Что нужно от пользователя (агент не может сам)

1. **Разгрузить очередь PR** — главное. Пять MERGEABLE/CLEAN: #171, #164,
   #161, #159, #158. По #152 и #110 нужно решение, какая из двух попыток
   «Моих активностей» живая (вторую закрыть, иначе конфликты будут расти).
2. **Закрыть устаревшее:** #41 (`lintDebug` и `warningsAsErrors` уже в `main`
   — проверено), #38 (тестовый `HELLO.md`), #63 (сделано, см. T17), #28
   (замер APK сделан), #26, #36.
3. **Секреты в `env` шагов сборки.** `MAPKIT_API_KEY` проброшен только в
   `release-internal.yml:82`; `SENTRY_DSN` — **нигде**. То есть отчёты о
   падениях (#74) собраны, а не уезжают никуда. Workflow-файлы правит только
   человек: у GitHub App агента нет прав на `.github/workflows`.
4. **`CONTRACT_REFRESH_TOKEN`** — без него харнесс сверки (и T17) видит только
   анонимную половину контракта, а всё под токеном пропускает.
5. **`DESIGN_REPO_PAT`** — без него агент работает без ТЗ и макетов, и
   «вёрстка с `TZ-ANDROID.md` не сверена» останется висеть.
6. **Единица денег** (#149) — сумы или тийины. Держит T14 и T15: считать чек,
   не зная единицы, нельзя.

---

## Как это проверялось

Чтобы список задач можно было перепроверить, а не принимать на слово.

**Сборка и тесты на этой ветке, 2026-09-09:**

```
./gradlew --no-daemon testDebugUnitTest assembleDebug
BUILD SUCCESSFUL in 5m 33s
тесты: 1842 в 177 классах, 0 падений, 0 ошибок, 2 пропущено
APK debug: 125.1 МБ
```

**Объём кода:** 405 файлов `.kt` / 53 380 строк в `main`, 214 файлов /
35 909 строк тестов (отношение 0.67). 45 экранов, 46 ViewModel — **у всех 46
есть тест-класс** (проверено перебором, не документом). Заглушка осталась одна:
`feature/orders/ui/OrdersScreen.kt`, 16 строк — её закрывает PR #152/#110.

**Сверка с бэкендом.** Список `метод + путь` вынут разбором аннотаций
Retrofit по `app/src/main/**/*Api.kt` (77 операций) и сопоставлен со
`/v3/api-docs` стенда (164 пути / 180 операций) с нормализацией
плейсхолдеров:

- совпало **77 из 77**;
- вызовов, которых нет у бэкенда — **0** (на 2026-09-02 было 4, все в «Еде»);
- ручек бэкенда без клиента — **103**, из них ~60 бизнес- и админские.

**Живые запросы** (гео-заголовки `X-Geo-Lat`/`X-Geo-Lng`, как их ставит
`GeoHeaderInterceptor`; без них `403 GEO_PERMISSION_REQUIRED`):

| Запрос | Ответ | Что доказывает |
|---|---|---|
| `food/delivery-fee?itemsAmount=50000` | `200 {"deliveryAmount":10000}` | T14: ручка живая и анонимная |
| `promotions/check?code=OSH20&placeId=…&orderAmount=50000` | `404 NOT_FOUND: Promo-kod topilmadi` | T15: маршрутизируется и проверяет код |
| `hospitals/doctors/{uuid}/slots?date=2026-09-15` | `401 UNAUTHORIZED` | T16: путь есть, нужен Bearer |
| `places/map-bounds?minLat=…` | `200`, 25 заведений (`Milano Pizza`…) | каталог наполнен, #168 подтверждён |
| `places/nearby?radiusMeters=5000` | `400 MISSING_PARAMETER: lat` | клиент **прав**: `CatalogApi.kt:156` шлёт `lat`/`lng` — дефекта нет |

**Самопроверка против открытых PR.** Все шесть задач прогнаны по диффам всех
13 PR (`gh pr diff` + поиск путей): `delivery-fee`, `promotions/check`,
`doctors/{id}/slots`, `media/entity` не встречаются ни в одном PR как код.
Именно эта сверка отсекла бизнес-панель, FCM, релиз-конфиг и историю платежей:
их было бы естественно предложить, а они уже написаны.

**Две поправки к собственным прошлым выводам** — ради них сверка и делалась:

- предыдущий заход по #59 предлагал 13 задач; **10 из них уже закрыты**
  (см. приложение), то есть повторять тот список было бы прямым вредом;
- issue #165 я сначала посчитал ошибочным (`verifyRoborazziDebug` в проекте
  нет). Неверно: задачу вводит PR #164, и #165 не устарел, а **ждёт его
  мержа**. Это и есть цена неслитой очереди — по `main` не видно, что в
  проекте уже сделано.

---

## Приложение: что стало с волной 2026-08-30

Прошлый заход по #59 предложил T1–T13 и семь задач по ним были созданы. Файла
со спецификациями в `main` тогда не оказалось, поэтому ссылки в тех issue вели
на `404`. Таблица закрывает этот долг.

| | Задача | Состояние на 2026-09-09 |
|---|---|---|
| T1 | Картинки (Coil) | **закрыто** — #60, PR #109; плюс правка #137 |
| T2 | Профиль, «Выйти», мои устройства | **закрыто** — #61 |
| T3 | Кошелёк: настоящий баланс | **закрыто** — #62 |
| T4 | «Еда» под реальный контракт | **сделано** — #63 ещё открыт, можно закрывать (см. T17) |
| T5 | Room-миграции вместо `fallbackToDestructiveMigration` | **закрыто** — #64 |
| T6 | Карта: подключить `MapCanvas` | **закрыто** — #65 |
| T7 | Таб «Заказы» → «Мои активности» | открыт #73, написан **дважды**: PR #152 и #110 |
| T8 | Отчёты о падениях | **закрыто** — #74 (Sentry); DSN в CI не проброшен |
| T9 | Лайк, «Избранное», комментарии | открыт #105, PR #78, блокирует #88 |
| T10 | Оставить отзыв | **закрыто** — #76 |
| T11 | Центр уведомлений | **закрыто** — #81 |
| T12 | Экран обязательного обновления | **закрыто** — #80 |
| T13 | Размер APK | **закрыто** — #28; R8 и подпись в PR #164 |

Нумерация волны 2026-09-09 продолжена с T14, чтобы ссылки на T1–T13 из уже
созданных issue не начали означать другие задачи.
