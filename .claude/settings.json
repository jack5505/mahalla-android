{
  "permissions": {
    "deny": [
      "Bash(git push --force*)",
      "Bash(git push -f*)",
      "Bash(git reset --hard*)",
      "Bash(sudo*)",
      "Bash(rm -rf /*)",
      "Bash(docker system prune*)",
      "Bash(docker rm -f backend)"
    ]
  }
}
